# What is in `results/`

This directory holds a mix of frozen manuscript outputs, superseded earlier runs,
diagnostic probes and dead stubs from killed jobs. Nothing about a directory's
name tells you which it is, and mistaking a stub for a result has cost this
project real time. Every entry below is classified, and the classification is
based on the files' actual contents as of **2026-09-04**, not on memory.

**Four classes:**

| | meaning |
|---|---|
| **FROZEN** | backs the abstract, the poster or the paper. Do not regenerate, overwrite or "fix". |
| **SUPERSEDED** | a real, completed run that a later one replaced. Kept for the record. |
| **DIAGNOSTIC** | a probe. Answers a methods question; never quoted as a result. |
| **DEAD** | a partial stub from a killed or failed run. Carries no result. **Do not read as one.** |

---

## Primary audit runs

| directory | class | primary ISI | notes |
|---|---|---|---|
| `nsclc_v3/` | **FROZEN** | `0.318240180054566` [0.26138392827249185, 0.37633421961128716] | The NSCLC headline. Tagged `prereg-2026-08-18`. Every NSCLC number in the abstract, poster and paper comes from here. |
| `pancancer_v3/` | **FROZEN** | `0.2911443244377561` [0.2691517839720275, 0.3132…] | The pan-TCGA headline, n=7,168, 31 types. Adds `null_draws.npz` (16 signatures × 1,000 draws), which is what lets Figure 1B plot real distributions instead of an invented mean±SD band. |
| `nsclc_v2/` | SUPERSEDED | `0.318240180054566` — **identical to v3** | Same primary estimate to full double precision. v3 exists for the recovered family-level p-value and the persisted null draws, not because the estimate moved. |
| `pancancer_v2/` | SUPERSEDED | `0.2911443244377561` — **identical to v3** | Differs from v3 only by `null_draws.npz`. |
| `pancancer/` | SUPERSEDED | `0.2911443244377561`, **CI is `NaN`** | Pre-dates the patient-clustered bootstrap fix. The point estimate survived; the interval did not exist. |
| `nsclc_real/` | SUPERSEDED | `0.30304910906118654` | Before the four code-audit defect fixes. |
| `nsclc_fixed/` | SUPERSEDED | `0.2956550969930552` | Intermediate stage of those fixes. **Numerically close to HPC4's 0.2964 by coincidence — this is not evidence about A9.** |
| `demo/` | DIAGNOSTIC | `0.0379` [−0.487, …] | Tiny synthetic cohort for smoke-testing the pipeline end to end. Never a scientific result. |

All eight are git-tracked.

## Supporting analyses

| directory | class | notes |
|---|---|---|
| `ancestry/` | **FROZEN** | The ancestry supplement, including the identifiability result. 7 files, git-tracked. |
| `partition_variance_nsclc/` | **FROZEN** | A5. 6 partitions, 0 refused. `isi_sd_across_partitions = 0.008891522442932558` against `bootstrap_se = 0.029404515034384043`, so the reported interval is understated by **4.47%**; honest CI `[0.25163185377958197, 0.3720498893332982]`. This is the source for the "partition sd 0.0089" claim. |
| `partition_variance_pancancer/` | **FROZEN** | A5, pan-cancer counterpart, landed 2026-09-05 after five handoffs carried it (PID 71818, log `partition_variance_pancancer_20260905_run3.log`). 6 partitions requested, **5 usable, 1 refused**: `split_seed=5` was rejected by the degeneracy guard with `"SVD did not converge"`, so the estimand is **not computable for every valid partition** — a run that silently substituted another seed would have concealed that. `isi_sd_across_partitions = 0.006523311640647398` against `bootstrap_se = 0.011100261700020214`; reported CI `[0.27172962183254884, 0.31524184813456674]`, honest CI `[0.2682509110018769, 0.31872055896523865]`, understated by **15.99%**, partition choice = **25.67%** of total variance, CI still excludes zero. **`split_seed=0` returns `0.2911443244377561 [0.2691517839720275, 0.31327569608397826]`, identical to frozen `pancancer_v3/` including both CI bounds** — an independent reproduction of the pre-registered headline *[corrected 2026-09-17: the frozen headline; pan-TCGA was added after the protocol was tagged, `05-PRE-REGISTRATION.md` Appendix A item 3]*. Per-partition wall clocks 6,266 / 3,541 / 11,404 / 3,374 / 3,364 s (7.8 h total), the first pan-cancer per-partition timings ever measured. |
| `partition_variance_nsclc_stable24/` | **NEW 2026-09-16 — item 6 of the science list, NSCLC half. NOT frozen. Not an authority when first written; since 2026-09-17 its `derived.json` IS one (`19_check_numbers.py` `pv24_nsclc_*`), quoted in the manuscript's Results.** 24 partitions (`split_seed` 0..23) under the FIXED sorts, HPC4 job 129201, `COMPLETED`, exit 0:0, `Elapsed=01:55:36`, `RUN_ELAPSED_SECONDS=6936`, 266–365 s per partition, **0 refused**. **`isi_sd_across_partitions = 0.007658304140287514`** (as `summary.json` stores it; until 2026-09-17 this row carried `0.0076583041402875215`, a transcription one ULP away) against the frozen 6-partition `0.008891522442932582`; mean 0.3053, range [0.2922, 0.3214] — **quoted to four figures deliberately.** These are a dated observation, not an authority: the directory is not in `PROVENANCE.json` and no frozen value backs them, and `19_check_numbers.py` correctly refused a full-precision number with nothing behind it when this row first carried one. The one number here that IS full precision is the next sentence's, because it matches a frozen authority. bootstrap SE 0.0294, combined 0.0303; the reported interval is **3.3%** too narrow (was 4.5%) and partition choice is **6.4%** of total variance (was 8.4%). **The point of the exercise, quantified:** the chi-square interval for the true between-partition sd narrows from **[0.005550, 0.021807]** at 6 partitions — a factor of 3.93 — to **[0.005952, 0.010743]** at 24, a factor of 1.80. That is the sqrt(24/6) = 2x tightening the design predicted. **It does NOT yet separate the cohorts:** pan-TCGA is still at 5 partitions with an interval of [0.003908, 0.018745] (a factor of 4.80), which the NSCLC interval still sits inside. The pan-cancer half is what would settle it and was still running at handoff. **An independent confirmation this run produced for free:** its `split_seed=0` returns **0.2922124012879652**, which is the frozen macOS NSCLC stable-sort headline to one unit in the last place — a third platform-agreement check, on a different code path from job 129151's. **Deliberately NOT added to `TRACKED_DIRS`/`PROVENANCE.json` yet:** nothing quotes these numbers, so they are not an authority that can drift, and the pair should enter together when pan-cancer lands. **Do not confuse it with `partition_variance_nsclc/`, which is FROZEN, pre-fix, and 6 partitions.** **UPDATE 2026-09-17 (ledger B3/B4):** the pan-cancer half has landed (`partition_variance_pancancer_stable24/`). `derived.json` was added here by `27_partition_sweep.py derive`, which reproduces this directory's `summary.json` values exactly and adds the chi-square sd interval and the partition share's interval (6.37% [3.95%, 11.80%]). The two cohorts are compared in `partition_variance_scaling_stable24.json`. |
| `partition_variance_pancancer_stable24/` | **NEW 2026-09-17 — item 6 of the science list, pan-cancer half (ledger B3). pending** | `27_partition_sweep.py merge --cohort pancancer --expect 0-23` over four sources in priority order: `default` (array 129207, default mode), `isi_only` (array 129667), `fast_isi_only` (array 129874, shared-mask fast path) and `retry_fast_isi_only` (array 130154). Then `derive` → `derived.json`. Shard CSVs copied from HPC4 unchanged into scratch; `summary.json` names each source's origin, not the local path. **RESULT: all 24 partitions computed, none missing** (`not_computed.csv` is header-only). Rows by mode: default 11, isi_only 4, fast_isi_only 8, retry_fast_isi_only 1. Seeds 3, 4 (two sources) and 19 (three) are **bitwise equal** across sources. Seed 0 is 0.29684911595367525, the corrected-ordering primary bit for bit (ledger B2). **sd across partitions 0.0072255199249013306, chi-square 95% [0.005616, 0.010136]**; mean 0.3007, range [0.2807, 0.3165]. Bootstrap SE 0.011073 and combined 0.013222, so the reported interval understates total uncertainty by **19.4%**. Partition choice is **29.9% [20.5%, 45.6%]** of total variance. The history of each seed before it was computed (default mode: 8 timeouts at 4 h and 5 LinAlgErrors; seed 12 timed out again with --isi-only before the fast path) is recorded in `summary.json`'s `sources`. Runtimes per partition: default mode 7,399–14,135 s; isi_only 8,297–12,270 s; fast path 5,360–14,326 s, the two slowest (13,403 and 14,326 s) on a node shared with four other shards; the three retry runs 6,071–6,285 s. |
| `partition_variance_scaling_stable24.json` | **NEW 2026-09-17 — ledger B8/B11, the two 24-partition sweeps compared** | `27_partition_sweep.py compare`. The bootstrap SE falls from NSCLC to pan-cancer by **2.6520**, 0.962 of the √patients prediction (2.7556). The between-partition sd does **not**: its ratio is **1.0599**, F 95% **[0.6971, 1.6115]**, which excludes √patients and √sites (3.0171) and includes 1. The two sd chi-square intervals overlap; the partition shares' intervals ([3.95%, 11.80%] and [20.46%, 45.59%]) do not. Supersedes the 6-and-5-partition quicksort ratio 1.3630. |
| `pancancer_v3_stablesort/` | **FROZEN — the A9/A10 sensitivity run for pan-TCGA, and the item that was blocked longest in this project** | The pre-registered pan-cancer configuration *[corrected 2026-09-17: the frozen configuration; pan-TCGA was added after the protocol was tagged, `05-PRE-REGISTRATION.md` Appendix A item 3]* re-run under the FIXED sorts, written 2026-09-16 by `09_run_pancancer.py --outdir results/pancancer_v3_stablesort` **on HPC4 (`cpu-845`, Linux x86_64, Python 3.12.14, scipy-openblas 0.3.27)** as Slurm job 129159, `State=COMPLETED`, `ExitCode=0:0`, `Elapsed=03:04:54`, `MaxRSS=24562284K` (23.4 GiB). **`primary_excess = 0.29684911595367525 [0.274920163364846, 0.31893289976112255]`** against the frozen `0.2911443244377561 [0.2691517839720275, 0.31327569608397826]` — a shift of **0.005704791515919128**, which is **0.87× the pan-cancer partition sd** and falls **inside** the honest partition-variance CI `[0.2682509110018769, 0.31872055896523865]`. **16/16 signatures still beat their null and the CI still excludes zero**, so no qualitative conclusion changes. **It does not replace `pancancer_v3/`, it is reported beside it** — option B, as for NSCLC. **Runtime 11022.42 s (3.06 h).** **Why a cluster number is quotable here, which is the question this row has to answer:** the rule "cluster numbers are never manuscript numbers" was written when the platforms DISAGREED under the shipped sort — that disagreement is A9. Before this run, job 129151 re-ran the NSCLC stable-sort configuration on the same node type and reproduced the macOS frozen value to **one unit in the last place** (0.2922124012879651 vs 0.2922124012879652, with the lower CI bound bitwise identical). S9 had shown that on a synthetic array; 129151 showed it on the real 944-patient cohort. **The config was diffed against `pancancer_v3/config.json` and the only difference is `run_legacy_venet_null`, a field that did not exist when the frozen run was written** — the frozen run computed that null too, as its own `venet_null.csv` shows, so the configurations are behaviourally identical. **The three inputs were sha256-verified byte-identical to the macOS copies before the run (`meta.parquet 86305b9f7c97950f`, `X.npy 6048e461aab97f87`, `expression_hugo.parquet 2a36f4a3d7f36a43`).** Ten of its eleven files stage into the deposit, taking a rebuild from 173 to 183; `predictions.csv.gz` does not. **NOT yet in `PROVENANCE.json`** — see the next session's task list. |
| `nsclc_v3_stablesort/` | **FROZEN — the A9/A10 sensitivity run** | The pre-registered NSCLC configuration re-run under the FIXED sorts (`splits.py:177` and `signatures.py:212` both `kind="stable"`), written 2026-09-05 by `05_run_nsclc.py --outdir results/nsclc_v3_stablesort` with defaults that match `nsclc_v3/config.json` exactly. This is what option B promised: **it does not replace `nsclc_v3/`, it is reported beside it.** `primary_excess = 0.2922124012879651 [0.23552139004939532, 0.3527272668764091]` against the frozen `0.318240180054566 [0.26138392827249185, 0.37633421961128716]` — a shift of **0.026028**, which is **2.93× the NSCLC partition sd** and falls **inside** the honest partition-variance CI `[0.2516, 0.3720]`, so the partition-variance analysis already anticipated a move of this size. **16/16 signatures still beat their null and the CI still excludes zero**, so no qualitative conclusion changes. **Runtime 227.55 s**, against `nsclc_v3`'s stored 1739.65 s for the same work — 7.6×, and evidence that the 1,739.6 s figure carried forward as "idle" was measured under contention. |
| `nsclc_v3_pinned/` | **ROW WRITTEN BEFORE THE RUN, with the literal word `pending`. THE PINNED-SPLIT RUN (A12 / B-12, ledger H86), reported BESIDE `nsclc_v3/` and `nsclc_v3_stablesort/`, replacing neither.** The pre-registered NSCLC configuration re-run with the SHIPPED code at `a3a06dc`, where `splits.stable_group_kfold` pins the random-patient split's group order (and the site-prediction control's), by `05_run_nsclc.py --outdir results/nsclc_v3_pinned` on macOS 27.0 / arm64. Intent: make ledger D1's in-process patch a permanent, portable secondary result. Expectation, written before: the ISI (preserved-site, which the pin does not touch) equals `nsclc_v3_stablesort/`'s 0.2922124012879651 [0.23552139004939532, 0.3527272668764091] bit for bit; `secondary_delta_mae` and every per-signature column equal D1's `macos_pinned` run bit for bit (0.004284906287348778 [0.0013828565937186866, 0.0070256075863230905]); the site AUROC table, which D1 did not store, is new and may move from `nsclc_v3_stablesort/`'s. **RESULT, read from the run's own files: the expectation held exactly.** The ISI is `nsclc_v3_stablesort/`'s bit for bit (0.2922124012879651 [0.23552139004939532, 0.3527272668764091]); `secondary_delta_mae` = **0.004284906287348778 [0.0013828565937186866, 0.0070256075863230905]**, bit for bit D1's `macos_pinned`, as are all 14 per-signature columns D1 stored and its `immune_excess.csv` — so D1's measurement of the pin (and its HPC4 twin, job 130005, which agreed to 9e-17) is now the shipped code's. The null-beating and outcome-arm counts are those of `nsclc_v3_stablesort/`, unchanged. `decomposition.csv`, `three_way.csv`, `venet_null.csv`, `null_draws.npz` and `config.json` are byte-identical to `nsclc_v3_stablesort/`'s (the pin reaches none of them). **New:** the site-prediction control's median one-vs-rest AUROC over the same 15 sites is **0.9906433445573468**, against 0.9915909090909092 unpinned on macOS — the control uses the pinned patient folds too; the post-ComBat figure is unchanged and, as before, not interpretable. Runtime 422.28 s, beside the baseline suite. Hashed in PROVENANCE (222 → 233, eleven files added, none changed). |
| `pancancer_v3_pinned/` | **ROW WRITTEN BEFORE THE RUN, with the literal word `pending`. THE PINNED-SPLIT PAN-CANCER RUN (A12 / B-12, ledger H86 step 4), reported BESIDE `pancancer_v3/` and `pancancer_v3_stablesort/`, replacing neither.** `09_run_pancancer.py --outdir results/pancancer_v3_pinned` with the shipped code at `870e7cb` (fast path on, as in the corrected-ordering reproduction), **on macOS 27.0 / arm64, not HPC4**: the ledger named HPC4, but the campus VPN was down for session 60 (GlobalProtect appeared to wait for a sign-in), and the pinned split is the one that D1 measured to give the same secondary quantities on both platforms. The risk is memory — the corrected-ordering run's MaxRSS was 23.4 GiB on HPC4 against this laptop's 24 GiB, and a macOS pan-cancer run was SIGKILLed once on 2026-09-06 — so a memory sampler runs beside it. Launch state: AC and charged, up 2 h 23 min since a reboot, swap 0 MB, about 9 GiB available, load 3.3, another session's jobs idle. Expectation, written before: the ISI equals `pancancer_v3_stablesort/`'s 0.29684911595367525 [0.274920163364846, 0.31893289976112255] to within about one ULP (that reference ran on Linux; macOS and Linux agree to one ULP under the corrected ordering, A9's validation); every random-patient secondary is new. Runtime: several hours (17,118 s on macOS before the fast path). **RESULT, read from the run's own files.** It completed: `Written to results/pancancer_v3_pinned  (97.1 min)`, `runtime_s` 5,825.25, no traceback, beside session 60's self-test and mid-session suite. **The ISI expectation was wrong in its last digits and is kept as written:** 0.2968491159536749 [0.27492016336484587, 0.31893289976112205] against the HPC4 reference's 0.29684911595367525 [0.274920163364846, 0.31893289976112255] — equal to 5e-16 (up to nine units in the last place, not one), so the two platforms agree to round-off on this cohort too but not bit for bit; per-signature `immune_excess.csv` agrees to 9e-15. **The pinned secondaries:** `secondary_delta_mae` = **0.009265838536153103 [0.008271740109471818, 0.01022153374323296]** (frozen 0.0088 [0.0078, 0.0098]; corrected ordering 0.0096 [0.0086, 0.0106]); median Δr +0.0230 (frozen +0.0199); median covariate-only r 0.6576; embedding over covariates −0.0012 (frozen −0.0000); median site AUROC over the same 98 sites 0.998794 (frozen 0.998477); The null-beating and outcome-arm counts are the frozen run's. No conclusion changes. Memory, from the sampler: peak resident 6.2 GB, swap up to 6.5 GB, no kill — nowhere near HPC4's 23.4 GiB MaxRSS, which counted the whole address space. Hashed in PROVENANCE (236 → 246). |
| `scorer_sensitivity_ssgsea_stable/` | **COMPLETE 2026-09-06 02:43.** Verified by the presence of `summary.json` and `per_signature.csv`, not by a log tail; PID 6503 is gone from `ps`. | The A10 ssGSEA recomputation: `13_scorer_sensitivity.py --arms ssgsea__uncorrected ssgsea__disattenuated`, PID 6503, detached and verified (PPID 1, PGID 6503, STAT `Rs`), launched 2026-09-05 23:58. Recomputes the uncorrected ssGSEA contrast the paper reports as **+0.180 [0.139, 0.216]**, which was produced by the PRE-FIX rank table whose tie convention is now known to move the underlying scores by 0.61 of a score sd. **The two `mean_z` arms are deliberately NOT run** — that scorer never touches the rank table, so no headline number is in question and the run costs 2 arms rather than 4. **How to tell it finished: `summary.json` must exist in this directory.** The log goes silent for long stretches between arms, so a tail proves nothing; steady %CPU in the 300-900% band is what shows it alive. **RESULT:** ssGSEA uncorrected excess **0.17377773241035724 [0.13325771984752488, 0.21018161815588407]**, 15/16 signatures above their null, against the pre-fix **0.1799529379910222 [0.1391910971170491, 0.2159114981000094]**, also 15/16. The shift is **0.00617520558066495** — 16.1% of the pre-fix interval's own half-width — so the A10 defect does not reach anything reported. `ssgsea__disattenuated` is `null` here for the same reason it is null pre-fix (the reliability estimator collapses, not the sort), and `arms_complete` is `false` BY DESIGN because only 2 of 4 arms were requested. Read as an AUTHORITY beside `scorer_sensitivity_local/`, not as a replacement for it. |
| `scorer_sensitivity_plage/` | **SENSITIVITY — ledger E6, NEW 2026-09-17. ROW WRITTEN BEFORE THE RUN; RESULT: pending.** | `scripts/32_third_scorer.py` on HPC4 (NEW directory `aacr27-s49e6`): a third scorer, PLAGE (`signatures.score_plage`), through script 13's own functions, on NSCLC with the current code. Arms: mean_z uncorrected (the like-for-like control), plage uncorrected, plage disattenuated (expected undefined, as under ssGSEA), and split-half reconstructions for mean_z and plage. Expectation: none stated; the question is whether PLAGE sides with mean-z or with ssGSEA. **RESULT (job 130215, 1,440 s after a 594 s smoke run at n_null 10): it sides with neither.** Uncorrected: mean_z +0.2963 [0.2442, 0.3514] 16/16, plage +0.1353 [0.0889, 0.1834] 13/16 (stored pinned ssGSEA +0.1738, 15/16). Disattenuated: NOT COMPUTED under plage, as under ssGSEA — the reliability estimator assumes a mean composite. Split-half reconstruction: mean_z +0.2919 16/16, plage **+0.0772 11/16**, against ssGSEA's −0.1333 0/16, so ssGSEA's sign reversal does not generalise to another non-mean scorer. Authorities `e6_*`; the checker refuses a file whose disattenuated plage arm stops being null. |
| `site_auroc_stress/` | **SENSITIVITY — ledger E16, NEW 2026-09-17** | `cluster/e16_site_auroc_stress.py`, HPC4 arrays 130207 and 130212 (tasks 0 and 1 of 130207 failed with `KeyError: 'auroc'` where a subsample left no qualifying site — a latent bug in `models.site_prediction_control`, which sorts an empty table; the diagnostic now records "no site evaluated" and the reruns are 130212). The registered negative control recomputed on random patient subsamples, in two arms: `registered` (sites of at least 20 patients, fractions 1, 0.5, 0.25, 0.1, 0.05) and `small` (at least 5, fractions 1 and 0.25); fractions below 1 drawn three times each. **RESULT: the median AUROC does not fall in any evaluable configuration.** Pan-cancer 0.9985 (98 sites) -> 0.9976 -> 0.9940; NSCLC 0.9894 (15) -> 0.9805 -> 0.894–0.972. At 10% and 5% NO site keeps 20 patients, so the limit reached is site eligibility. At a 5-patient threshold: 320 pan-cancer sites, median 0.9981, 97% above 0.9; 47 NSCLC sites, median 0.9867, 94%. The NSCLC full-cohort median is 0.9894 here against the frozen macOS 0.9916 — A12 reaches this control too (F6.3). Authorities `e16_*`. |
| `omega_reliability/`, `omega_reliability_20260917.log` | **SENSITIVITY — ledger E8, NEW 2026-09-17. ROW WRITTEN BEFORE THE RUN; RESULT: pending.** | `scripts/33_omega_reliability.py` on macOS, detached: McDonald's omega beside Cronbach's alpha for the 16 NSCLC sets and 25 null draws each, on the registered residualised items, and the disattenuated excess reconstructed with each (stored null draws of `nsclc_v3_stablesort`). Gate: its alpha must equal the registered `alpha_observed` to 1e-10. Expectation: omega ≥ alpha, with a larger lift for the null sets than for the curated ones. **RESULT (PID 96745, 586 s): the expectation was WRONG in direction for the null sets.** Gate passed at 1.1e-16. Curated medians are equal (alpha 0.9773, omega 0.9773); the random sets' omega COLLAPSES (0.8358 -> 0.2270), so the curated-random gap widens from 0.1264 to 0.7333 and the reconstructed index falls from 0.2917 [0.2584, 0.3251] to 0.2147 [0.1739, 0.2554], 15/16 still above null. Read as a bound: one-factor omega is interpretable where one factor fits, which for random sets after axis removal it does not. Authorities `e8_*`; the checker refuses a file whose gate fails. |
| `scorer_sensitivity_local/` | **FROZEN — this is A7** | The four-arm macOS scorer-sensitivity run (PID 69478, finished 2026-09-03 04:51). `per_signature.csv` + `summary.json`. The run's acceptance check passed **bit-identically**: `arms.mean_z__disattenuated` = `0.318240180054566 [0.26138392827249185, 0.37633421961128716]`, equal to `nsclc_v3`'s `primary_excess` including both CI bounds. Arms: mean-z uncorrected `+0.3191678769197761`; ssGSEA uncorrected `+0.1799529379910222` (15/16); **ssGSEA disattenuated is `null` — the registered estimand is UNDEFINED under ssGSEA**, 1000/1000 null draws dropped by the reliability guard. This is the source for every A7 number in the paper and in `14-SCIENCE-AUDIT.md`. **Provenance:** produced by the pre-`--arms` build, so `summary.json` lacks `arms_requested` / `arms_complete`; `git diff 9e909eb..bde5514` touches only the docstring, `ARMS` and `main()`, so the computation is unchanged and the numbers are quotable. |
| `scorer_sensitivity_hpc4/` | DIAGNOSTIC — **not quotable, and now explained** | A7's four ssGSEA arms, cluster job 116899, 24 min. Real and complete, but produced on HPC4, where the run's own control arm failed to reproduce the stored +0.3182 (it returned +0.2964 — that failure is what surfaced A9). **The macOS counterpart above now demonstrates the cause directly:** the same script with the same arguments passes its acceptance check on macOS and fails on HPC4, so the discrepancy is A9 and not a scorer effect. Its arm-by-arm contrast against `scorer_sensitivity_local/` is itself an A9 measurement. Still not quotable as a scorer result. |
| `session48_diagnostics/` | DIAGNOSTIC — **copied 2026-09-16 (session 49) from HPC4 so the evidence for session 48's findings lives in this repository, not only on the cluster.** Never quoted as a result; excluded from the public snapshot by name because several files carry the cluster home path. `job129294_svd_refusal/`: the four failing seeds' tracebacks and retries (`diag_seeds_{6,11,12,15}.json`) and the BLAS thread bench (`thread_bench.json`) behind the A5 correction; the 30-36 MB failing matrices stay on HPC4. `jobs129663-129665_partition_cost/`: the fold census (`folds.json`: zero clean folds in 24 partitions) and the per-signature null profiles for seeds 7 and 4 (`profile_seed_7.json`, `profile_seed_4.json`). `job129666_isi_only_equiv/`: the NSCLC `--isi-only` gate (GATE_BITWISE True). `job129737_verify_fast_path/`: the shared-mask fast path on real seed-7 folds on Linux (ALL_BITWISE True). `job129151_nsclc_stablesort_hpc4/`: the HPC4 NSCLC corrected-ordering run's `summary.json`, `per_signature.csv` and `config.json`, which A12 compares against `nsclc_v3_stablesort/`. `array129207_pancancer_shard_logs/`: all 24 shard logs of the default-mode pan-cancer sweep. `failed_jobs_129222_129308-129310_129695/`: the logs of five failed jobs, kept as the record of how they failed. |
| `nsclc_v3_tumouronly/` | **SENSITIVITY — A16, NEW 2026-09-17. NOT frozen, NOT the reported primary.** | The pre-registered NSCLC configuration (`05_run_nsclc.py`, unchanged code at commit `8442dfe`) on TUMOUR-ONLY expression: `00_build_interim.py --tumour-only` keeps TOIL sample code 01 only, so the 51 patients whose frozen profile was an adjacent normal (48), a recurrence (2) or normal-only (1, dropped) now carry their tumour profile; 943 patients. HPC4 job 130002, 10 min 42 s. **ISI 0.34483787011597666 [0.27313849328844386, 0.4079193236965104]**, all 16 signatures above their null, against the corrected-ordering 0.2922124012879652 on the same machine (job 129151): a shift of +0.0526, with 14 of 16 per-signature excesses moving up. Δ-MAE 0.004844181456732431 [0.0015110694030102093, 0.008192416360684516]. In the survival arm one of the 32 signature-by-adjustment comparisons (hypoxia, axis-residualised) now beats its null, against none before. The secondary quantities are Linux values (A12). |
| `nsclc_sensitivity/`, `nsclc_sensitivity_20260917.log` | **SENSITIVITY — ledger E1, E3, E4, E5. ROW WRITTEN BEFORE THE RUN; RESULT: pending.** `scripts/29_nsclc_sensitivity.py` on macOS: the NSCLC index under one changed choice at a time (global rather than within-type axis; size-only null matching; 400 or 1,000 null draws inside the bootstrap; 3 or 10 folds), each in the ISI-only configuration, with a `reference` variant that must reproduce `nsclc_v3_stablesort/` bit for bit before any other variant is read. Expectation: the gate holds; the variants are measurements, with no expected direction. **RESULT (2026-09-17, PID 6311, 2 h 25 min at load 25–100 from a foreign process): the gate held** (`reference_reproduced_bitwise: true`, 0.2922124012879651 [0.2355, 0.3527], 465 s). The rest, NSCLC ISI [95% CI], signatures above null: global axis −0.0096 [−0.0630, 0.0466], 1/16, with 10 of 16 nulls degenerate (492 s); size-only null 0.3041 [0.2434, 0.3536], 16/16 (2,252 s); 400 null draws [0.2336, 0.3512] and 1,000 draws [0.2333, 0.3511], estimate unchanged; 3 folds 0.2188 [0.1673, 0.2722], 15/16 (181 s); 10 folds 0.3154 [0.2607, 0.3711], 16/16 (5,209 s). Authorities (`sens_ns_*`) for the Results section "Sensitivity to other analysis choices". `predictions.csv.gz` in each variant is not deposited. |
| `pancancer_sensitivity/` | **SENSITIVITY — ledger E1, pan-cancer, NEW 2026-09-17** | `global_axis/` is `cluster/e1_global_axis.py`'s output from HPC4 job 130189 (cpu-848, 1,217 s, fast path), copied unchanged except `predictions.csv.gz` (not kept). `summary.json` names the reference (`pancancer_v3_stablesort`, which the ISI-only configuration reproduces bit for bit) and records the variant: 0.3512 [0.2692, 0.4353], 15/16 above null, 0 degenerate nulls, axis variance explained 0.106. Authorities `sens_pan_*`. |
| `null_reliability/` | **DIAGNOSTIC and AUTHORITY-free — ledger E9, NEW 2026-09-17** | `cluster/e9_null_reliability.py`, HPC4 array 130185 (task 0 NSCLC 258 s, task 1 pan-cancer 4,148 s; cpu-848). For every signature it records the null reliabilities and \|r/sqrt(α)\| the disattenuation guards see. No draw is dropped in either cohort; the smallest α is 0.0586 in NSCLC and 0.172 pan-cancer. Each task's index equals its cohort's corrected-ordering primary bit for bit. `README.json` is the provenance note. Quoted in 14-SCIENCE-AUDIT.md (E9), not the manuscript. |
| `pancancer_tumouronly/` | **SENSITIVITY — ledger A16, pan-cancer, NEW 2026-09-17. NOT the reported primary.** | `12_partition_variance.py --isi-only --shard` at split_seed 0 on `00_build_interim.py --tumour-only` inputs, HPC4 job 130155 (cpu-845, commit 8442dfe, 6,293 s); `per_partition.csv` copied unchanged, `summary.json` written here with the provenance. 7,128 patients (the 40 without a tumour sample dropped), 619 sites: 0.2937175047151571 [0.2701, 0.3149], shift −0.0031 from the corrected-ordering primary. Authority `a16_pan`. |
| `session49_diagnostics/` | DIAGNOSTIC — session 49's evidence, excluded from the snapshot by name. | `d1_pinned_groupkfold/`: the NSCLC run with sklearn's GroupKFold tie order pinned, on macOS (control and pinned) and on HPC4 (job 130005), with both comparisons as JSON — the D1 memo's data. `c1_c2_render_probe_job129926/`: the Linux supplementary renders under both CSV parsers (A15, C1). `deposit_pytest_168/`: the deposit suite at the 168-test tree (39 failed, 121 passed, 8 skipped). `d7_nsclc_fastpath_macos/`: the count showing NSCLC never takes the fast path. `job_logs/`: every HPC4 job log of this session (129667 and 129874 shards, 129875, 129926, 129998, 130002, 130003, 130005). |
| `session60_diagnostics/` | DIAGNOSTIC — session 60's evidence, excluded from the snapshot by name. Never quoted as a result. | `audit_sweep_20260924.tsv`: ledger H89's classification of every one of the 1,072 literals `--coverage` listed as unchecked in `14-SCIENCE-AUDIT.md` at `a79fcb0` — its line there, the literal, the mutation verdict (CAUGHT or not), a class (bound in session 60 with its authority key, or a record, design constant, unhashed diagnostic or unbound result) and the line's text. The audit's block "Session 60: the 1,072 unchecked numbers" summarises it. |
| `figures/` | **FROZEN** | 15 files, manuscript figures, last regenerated 2026-08-19. |
| `figures-poster/` | **RETIRED 2026-09-24 — REGENERABLE, NOT A RESULT, NOT COMMITTED** | *[2026-09-24: `--poster` no longer writes here. TODO-PRINT-6 was decided that day and `10_make_figures.py --poster` now draws the four poster figures for their A0 boxes and writes them to `poster/figures/`, which is committed and which the poster embeds; see `poster/README.md`. What follows describes the retired option (a).]* Absent unless someone has run it. `10_make_figures.py --poster [SCALE]` (session 53, TODO-PRINT-6 option (a)) re-renders the same figures from the same frozen inputs with every type size multiplied by SCALE, default 2.0, for the A0 poster; nothing else changes, not dpi, spines, grid or fonttype. It writes HERE rather than into `figures/`, and the committed figures were verified byte-identical across a `--poster` run. Gitignored, because these are regenerable renders and the project does not stage figure files. **Read it as a print proof, never as a result** — the numbers it draws are the same ones `figures/` draws. |

## Small derived results

| file | class | notes |
|---|---|---|
| `outcome_power_nsclc.json`, `outcome_power_pancancer.json` | **AUTHORITY for three Results numbers (E10), NEW 2026-09-17** | Written by `scripts/30_outcome_power.py` from the frozen `outcome_arm.csv` of each cohort, with the same normal approximation that gives the MDE (reproduced: NSCLC 0.046). Median power over the 32 comparisons at C-index advantages 0.02-0.06: NSCLC 0.229, 0.446, 0.682, 0.861, 0.956; pan-cancer 0.920 at 0.02 and above 0.999 from 0.03. Regenerable from the deposit; a test compares the committed NSCLC file with a fresh computation. |
| `type_only_concordance.json` | **AUTHORITY for one Methods number (E13), NEW 2026-09-17** | Written by `scripts/28_type_only_concordance.py` from `data/interim`. Harrell's C of cancer type alone on pooled pan-TCGA PFI, unstratified and in sample: 0.6764550508960626 with each type's event proportion as the score (the manuscript's 0.676), 0.678 and 0.681 under two other type-only scores; 7,168 patients, 7,068 with PFI, 2,442 events. Replaces a literal the run notes print for every cohort. |
| `outcome_category_test.json` | **NEW 2026-09-17 — ledger E11. pending** | `31_outcome_category_test.py`: exact enumeration of whether the five pan-cancer outcome winners' composition (all inside proliferation, development and pathway) is beyond chance. **RESULT:** all 5 winners are inside the 6-signature group; P(all inside) = 6/4,368 = 0.0014; the group's mean axis-residualised excess exceeds the rest's by 0.0672, and no other relabelling of the 16 does better (one-sided P = 1/8,008). Both are DESCRIPTIVE: the grouping was named after the winners were known, and correlated signatures (G2M, E2F) make the true tail heavier. The pre-specified contrast stays immune vs other. |
| `control_c_calibration/` | **NEW 2026-09-17 — ledger E14; an AUTHORITY for the Control C paragraph** | `cluster/e14_control_c_calibration.py`, HPC4 array 130200 (task 0 pan-cancer 1,369 s, task 1 NSCLC 55 s), NEW directory `aacr27-s49e14`. Script 11's A4b calibrates Control C for ONE probe signature; this reruns `decomposition.permutation_calibration` (within cancer type, 200 permutations, seed 0) for all 16, on the frame script 11 builds. **Gate: each file's probe (allograft rejection) reproduces the stored A4b value bit for bit (both True).** Median calibrated site R² **0.0468** pan-cancer (range 0.0279–0.0602; 6 of 16 at p ≤ 0.05) and **0.0702** NSCLC (0.0182–0.1051; 15 of 16). The frame covers every site, not Control C's sites of at least 10 patients (median crude R² 0.485 and 0.177). Read by `19_check_numbers.py` (`e14_*`), which refuses a file whose gate is False. |
| `sort_fix_fold_change.json` | **NEW 2026-09-17 (session 50) — ledger F1.1; an AUTHORITY for Limitation 8's yardsticks and fold census** | Written by `scripts/34_sort_fix_fold_change.py` from the stored `predictions.csv.gz` folds (scheme `preserved_site`) of `nsclc_v3` against `nsclc_v3_stablesort` and `pancancer_v3` against `pancancer_v3_stablesort`, the four partition-variance `summary.json` files, the 24-partition `per_partition.csv` files, and `scorer_sensitivity_hpc4/summary.json` (the Linux shipped-sort ISI, `0.2963918792070625`). Nothing is recomputed from data. **Why it exists:** Limitation 8 explained the smaller pan-cancer sort-fix move with an argument — a larger cohort's ties "perturb a correspondingly smaller share of the partition" — and nothing had measured the share. **RESULT, the opposite:** the fix reorders tied sites only (no patient in an untied site moved; every fold kept its size, both cohorts), pan-TCGA has 600 of 619 sites tied (79.5% of patients) against NSCLC's 51 of 68 (38.1%), and the fix moved 3,812 of 7,168 pan-cancer patients (53.2%; adjusted Rand index 0.131) against 216 of 944 NSCLC patients (22.9%; ARI 0.537). Against the 24-partition sweeps, the NSCLC move (0.0260) is matched or exceeded by 3 of 276 partition pairs and the pan-cancer move (0.0057) by 153 of 276. The yardstick ratios, to full precision: platform gap 2.457 / 2.853 partition sds (six / 24), NSCLC move 2.927 / 3.399, pan-cancer 0.875 / 0.790, contrast 3.347 / 4.305. The manuscript had carried 2.4 for 2.457 and 3.4 for 3.347; both corrected. The script asserts its site derivation against `split_determinism_darwin_arm64.json` (68 sites, 51 tied, 216 moved) and refuses if a patient in an untied site moved. Authorities: `19_check_numbers.py` `sffc_*`, each with a self-test row. |
| `linalg_census/` | **NEW 2026-09-17 — ledgers D3 and D8, DIAGNOSTIC (HPC4 only)** | `cluster/d3_d8_linalg_census.py`. For each split_seed, it records which covariate-baseline folds (random-patient split) and which decomposition rungs raise LinAlgError, using the audit's own functions. Array 130202 on HPC4: `nsclc_0_23.json` (no failure in 24 seeds, 430 s), `pancancer_0_7.json` (covariates fail for seed 6), `pancancer_16_23.json` (seed 20, covariates fold 2). Task 1 (seeds 8–15) stopped at seed 15, where the decomposition failed as session 48 found. The census's own wrapper then returned the wrong sample size and the ladder's equal-n guard raised. Its log shows seed 11 fold 2 and seed 12 fold 1 failing in the covariates. Rerun with the fixed wrapper as job 130211 (directory `aacr27-s49d3`, 751 s) into `pancancer_8_15.json`: **RESULT, all 48 seed-cohort pairs:** NSCLC none. Pan-cancer covariates fail for seeds 6, 11, 12 and 20 (fold 2, 2, 1, 2; design 7,168 × 649 before fold selection). The decomposition fails for seed 15 only: MYC targets, rung `Mfull_+image`, 7,168 × 651. That is exactly session 48's five failing seeds and no others. Seed 0, every frozen primary's partition, passes both sites. The earlier shards' records are valid: the wrapper defect only acts after a decomposition failure, and they recorded none. The macOS half was not run: the local smoke test (NSCLC seeds 0–1) was stopped after 73 min at load 70–100 from a foreign process. |

## Empty directories

`scorer_sensitivity/`, `tables/` and `logs/` are **empty**.
`scorer_sensitivity_local/` **is no longer empty** — it is now A7's frozen
artefact, listed above. Note for future runs: `13_scorer_sensitivity.py` writes
`per_signature.csv` and `summary.json` only after **all four arms finish**, so an
empty destination directory during a run means "not finished", not "failed".

## A9 platform probes

| file | class | notes |
|---|---|---|
| `alpha_margin_macos.log` / `.npz` | **DIAGNOSTIC — the real A9 result** | `16_alpha_margin.py`, completed on macOS in 19,123 s over all 80 signature-fold ridge-alpha selections. Ends with `VERDICT` and `ALPHA DIGEST fc299c9c0f47466e`. Margins: `min 1.034e-04, p10 7.917e-04, median 5.099e-03, max 1.125e-02, 0/80 below 1e-06`. **This is what killed A9's leading hypothesis.** |
| `alpha_margin_fast_macos_run2.log` / `alpha_margin_fast_macos.npz` | **DIAGNOSTIC — COMPLETE, and it closes elimination #6** | `17_alpha_margin_fast.py`, the same question without `run_audit`. Finished 2026-09-03 03:05 (PID 52573), 19,402 b, **80/80 rows**, and printed `ALPHA DIGEST fc299c9c0f47466e` — **identical to `16_alpha_margin.py`'s digest over all 80 alphas**. Two independent code paths, one taking 19,123 s and one ~1 s of setup, agree bit-for-bit. That converts `17`'s docstring fidelity caveat (that it residualises the 16 observed columns stacked, where `run_audit` stacks one observed column with that signature's nulls) from an argued-away risk into a **measured no-op**. |
| `alpha_margin_fast_macos.log` (no `_run2`) | **DEAD** | The *failed* first run of `17`, ending `no finite margins -- nothing to report`. Kept deliberately as the record of the NaN-axis bug: a bare `.npy` axis cache dropped the `patient_id` index, every residual went NaN, and all 80 alphas came back NaN **silently**. |
| `bisect_macos.log` / `bisect_macos.npz` | **DIAGNOSTIC — COMPLETE, awaiting its other half** | `18_bisect_platform.py --tag macos --n-null 8`, the macOS half of the stage-by-stage platform diff. Finished 2026-09-03 02:04 (PID 79467), **2,182,700 b**, 16/16 signatures, 8,525.3 s, pooled undisattenuated z-excess `0.32133084670199263`. Holds raw float64 per stage — residualised scores, fold alphas, out-of-fold ridge predictions, `corr_ci` r values, Fisher-z excess — plus **S0**, a standalone bootstrap probe on deterministic synthetic input. **It is half an experiment.** It becomes an answer only when the identical command runs on HPC4 and `--compare results/bisect_macos.npz results/bisect_hpc4.npz` names the first stage whose max absolute difference leaves the 1e-12 round-off floor. **No longer blocked — its other half arrived 2026-09-06, see the row below.** |
| `bisect_hpc4.npz` | **DIAGNOSTIC — the other half; its stage reading is SUPERSEDED** | *(Its `S3_fold_alpha` conclusion below was wrong about causation — see the `split_determinism_*.json` row. The measurements here are correct; the inference from them was not.)* The HPC4 half, from cluster job **121439** (`cluster/bisect_platform.sbatch`, 50.2 s of compute, node cpu-845). **2,182,700 b — byte-for-byte the same size as the macOS dump**, which is the cheapest available check that the two files have identical structure and the run was not truncated. Pooled undisattenuated z-excess `0.2988636473916485` against macOS's `0.32133084670199263`. `--compare` on the pair names **`S3_fold_alpha`** as the first diverging stage: 11 of 80 per-fold penalties differ, each by exactly one grid step. `S0` (bootstrap) and `S2` (residualisation) agree to round-off and are thereby eliminated. This file is a diagnostic, not a manuscript number — cluster values never are. |
| `supplementary/` | **RENDERED — regenerate, do not hand-edit** | Nine supplementary tables as TSV plus `MANIFEST.md` and `CAPTIONS.md`, written by `25_build_supplementary.py` from the frozen artefacts. `CAPTIONS.md` (added 2026-09-05) holds the journal-style standalone captions — `MANIFEST.md`'s one-liners are internal provenance notes and are not submittable as captions. The build **asserts** every table in `TABLES` has an entry in `CAPTIONS`, so adding a table without a caption stops the build rather than producing a silently incomplete package. Every cell traces to a source named in the manifest, and `--check` re-reads the rendered files and compares five named values against `19_check_numbers.py`'s `authority()` — verified able to fail by injecting a wrong value. `S6_partition_variance.tsv` is **NSCLC-only** until the pan-cancer run lands; re-run the script then. Safe to delete: it regenerates in ~2 s. |
| `split_determinism_darwin_arm64.json` / `split_determinism_linux_x86_64.json` | **DIAGNOSTIC — A9's actual answer, one file per machine** | `24_split_determinism.py --write`, run on macOS arm64 and on HPC4 x86_64 (login node, ~2 s each). Each file is written by a real run on the machine it names — neither is transcribed. Together they show that with **byte-identical inputs** (`hash_site_index`, `hash_sizes` and the embedding matrix all agree) the two architectures produce **different fold assignments**: `hash_fold_shipped` is `e55ee4e8887b5167` on macOS and `a40c862b12d9a391` on HPC4, because `np.argsort`'s default quicksort is not stable and **51 of 68 NSCLC sites are tied on size**. Under `kind="stable"` both machines give `9f31e33c2caaf1e3`. The synthetic-ISI fields show the same thing end to end: shipped `1.036173699004058` (macOS) vs `0.9752094760755513` (HPC4), but stable-sorted macOS returns `0.9752094760755506` — the Linux value to 15 significant figures. **This supersedes the `S3_fold_alpha` reading of the two rows above:** the penalty flips are a consequence of the differing partition, not a cause. Registered in `19_check_numbers.py`'s `authority()`; diagnostic, never a manuscript number. **Both files predate the 2026-09-05 fix and are kept as the pre-fix record — do not regenerate them.** `hash_fold_shipped` and `n_patients_fold_changed` (216) describe the code as it was; since the fix, `24_split_determinism.py --check` reports the shipped fold hash as `9f31e33c2caaf1e3` and 0 of 944 patients moving. |
| `sort_audit_darwin_arm64.json` / `sort_audit_linux_x86_64.json` | **DIAGNOSTIC — A10's answer, one file per machine** | `26_sort_audit.py --write`, run on macOS arm64 and on HPC4 x86_64 (cluster job **121782**, `cluster/sort_audit.sbatch`, 2 m 50 s, COMPLETED exit 0). Each written by a real run on the machine it names. These establish that `signatures.py:212` was a **second** instance of A9's defect class, and a larger one. With byte-equivalent inputs — both machines count the same **944 of 944** tied samples and **35,394,448 of 38,747,424** tied rank entries — the ssGSEA depth table hashes **differently** (`0bca973cfa26da89` macOS vs `b98fb11e701e8e98` HPC4) under the default quicksort, while `hash_depth_stable` is `096172c056cc7a34` on **both**. The difference survives into the scores: 15,060 of 15,104 cells and all 16 signatures, max \|Δ\| 1.2824e-02 (macOS) / 1.3040e-02 (HPC4) against a mean per-signature score sd of ~2.11e-02, i.e. **0.61 of a standard deviation**. `signatures_pins_stable` is `true` in the macOS file and `false` in the HPC4 one, correctly: the cluster deliberately keeps the pre-fix `src/aacr27/` and nothing there was overwritten. **None of this touched the frozen headline results**, which run `scorer="mean_z"` and never reach the rank table. Registered in `19_check_numbers.py`'s `authority()`, which additionally asserts the stable hashes agree across the two files **and** that the quicksort hashes differ — so the claim is falsifiable in both directions. Diagnostic, never a manuscript number. |
| `groupkfold_order_darwin_arm64.json` / `groupkfold_order_linux_x86_64_login.json` | **DIAGNOSTIC — A12's per-machine record (ledger C9)** | `26_sort_audit.py --library-order OUT` (case F), 2026-09-17: on macOS arm64, and on the HPC4 login node from the NEW directory `aacr27-s49c9` (a compute-node copy from job 130190 is kept on the cluster and in `session49_diagnostics/`). Case F records scikit-learn 1.6.1 `GroupKFold`'s unstable group order (A12): the all-ties argsort hashes (`cb54ec6dc2893b62`/`9085a1f89f19e541` macOS; `84f9a4114100edfa`/`db52d4be6eabc0f2` HPC4, n = 944/7,168), and the fold assignment of a 944-patient permutation under the library order and under a stable one. The stable assignment agrees (`caf92a3f83ddb5b9` on both) and the library one does not (`8fdee48c0ac8a457` vs `8f4427a6cc4567e6`). `19_check_numbers.py` asserts both halves. Verdict `DOCUMENTED, NOT PINNED`: the pin is the author's open decision (A12, D1), and nothing here changes a split. Deliberately a separate file from the A10 `sort_audit_*.json` artefacts, which `--library-order` refuses to write. Diagnostic, never a manuscript number. |
| `gene_set_sizes.csv` | **DEPOSITED FIGURE INPUT — 2026-09-07** | The 16 post-filtering panel sizes figure 2 plots against, 379 b, written by `10_make_figures.py --write-gene-sizes` from `data/interim/expr_nsclc.parquet` (41,046 expressed genes) and the Hallmark GMT. It exists because those two inputs are the ONLY route to figure 2's x-axis and **`pipeline/data/` is excluded from the public snapshot on purpose**, so `10_make_figures.py` **exited 1 at figure 2 for every reader reproducing from the deposit** — the paper's Data Availability Statement was false a second way, found the same day as the `null_draws.npz` gap and by the same method: *running* the figure script against a staged snapshot rather than reading the builder. Recomputation stays authoritative when `data/` is present, and the two are **compared**, so a stale deposit fails loudly on the one machine able to notice. Hashed in `PROVENANCE.json` for a reason none of the other entries share: no document quotes it, but it is the only figure input that is a **copy** of a quantity living elsewhere, and a snapshot reader has no `data/` with which to detect drift. |
| `_cache_global_axis_nsclc.npz` | CACHE | The NSCLC PC1 global axis with its patient ids, checked against the cohort on every load. Turns a ~13-minute setup into ~0.7 s. Safe to delete; it will be recomputed. |
| `_cache_global_axis_nsclc.npy` | CACHE, legacy | The unchecked bare-array form that caused the NaN-axis bug. Readers upgrade it to `.npz` on the way past, with a length check. Kept only so PC1 need not be recomputed. |
| `alpha_probe_macos.log`, `alpha_probe_macos_detached.log` | **DEAD** | Stubs from killed runs of `15_alpha_probe.py`. 807 b and 63 b. `15` is superseded by `16` and `17` anyway — it asks whether the alphas *differ*, which needs both machines. |

## Run logs

| file | class | notes |
|---|---|---|
| `nsclc_v3_run.log`, `pancancer_v3_run.log` | **FROZEN** record | 29.0 min and 285.3 min respectively. |
| `nsclc_v2_run.log`, `nsclc_real_run.log`, `nsclc_fixed_run.log`, `pancancer_run.log`, `pancancer_v2_run.log` | SUPERSEDED record | Complete runs; each ends with its own "written to" line. |
| `partition_variance_nsclc.log` | **FROZEN** record | Complete. |
| `reproduce_check_20260904.log` | **DIAGNOSTIC — the first complete `reproduce.sh --check`** | `bash reproduce.sh --check`, PID 4763, detached, 2026-09-04 21:37:49-21:39:01. **72 s end to end**, and this path had never been run to completion before. Contains the version check, the provenance manifest (90/90), `86 passed in 54.62s`, figure regeneration, the abstract recount and the number check, ending `CHECK PASSED`. The wall clock is now recorded in `reproduce.sh`'s own header. |
| `full_suite_20260904.log` | **DIAGNOSTIC — a contention measurement, not a suite record** | `pytest tests/ -q --durations=10`, PID 46388, finished 2026-09-04. `86 passed in 1305.22s`. **That is 24x slower than the same 86 tests measured at 54.62 s and 56.09 s on an idle machine hours later**, and the difference is NOT caching — the global-axis caches were untouched (mtimes 2026-09-02) across all three runs. It was measured while PID 51122 held a large working set on a 99%-full disk. Quote it as the cost under contention, never as the suite's cost. **The "86" here is what that log FILE contains, not the current test count** (89 as of 2026-09-05); correcting it would falsify the record of a measurement. <!-- numcheck: ignore: historical record of what full_suite_20260904.log contains; the suite has since grown to 89 tests and retro-correcting this would falsify a measurement --> |
| `scorer_local_detached.log` | **COMPLETE** record | The macOS A7 run (PID 69478), all four arms, finished 2026-09-03 04:51. Arm 1: `mean_z__disattenuated 14200s pooled +0.3182 [+0.2614, +0.3763] finite 16/16 beats null 16` — the control arm **passing** against `nsclc_v3`'s stored value. Its durable artefact is `scorer_sensitivity_local/`, above; prefer the JSON to this log, which prints only four decimals. |
| `partition_variance_pancancer.log` | **DEAD — killed, 0 partitions** | `12_partition_variance.py --cohort pancancer --partitions 6 --n-null 1000 --n-boot 1000`, PID 51122, detached, started 2026-09-04 19:50. **It died without completing a single partition.** The file is 756 b and its last write was 19:50 — the header line (`7168 patients, 619 sites, 16 signatures, 6 partitions`) and nothing else. It was alive at 20:32 (321% CPU) and gone by 21:34, with no traceback, which is the signature of a SIGKILL rather than an exception. **The memory-pressure hypothesis was TESTED on 2026-09-05 and does not hold.** Loading the identical pan-cancer working set and measuring peak RSS directly gives **6.944 GiB against 24 GiB of RAM** — `expr` is 2.193 GiB, `X` 0.041 GiB, and `compute_global_axis` adds 1.6 GiB over 94.9 s. A 6.9 GiB working set on a 24 GiB machine is not, on its own, what a memory-pressure SIGKILL looks like — so "this job was too big" is **eliminated**. But a second measurement the same day supplies what was missing: `sysctl vm.swapusage` reports **total 22528 MB, used 21747 MB, free 780 MB**, against 16 GiB of free disk. **The machine as a whole is swapping almost 22 GB**, and swap cannot grow because the volume is 99% full. So the refined and now-measured explanation is neither of the previous two: the job's working set is modest, but there is **no headroom left on the machine**, and a 7 GiB allocation arriving when swap has 780 MB free is killed regardless of how reasonable it is in isolation. The cause is the machine's total memory commitment plus a full disk, not this job's footprint. **Freeing disk space is therefore a prerequisite for this run, not housekeeping.** **Carries no result. Do not read the header as one.** Needs ≥3 usable partitions or it exits 1 writing nothing; it prints each partition as it completes, so a *later* killed run would leave usable numbers — this one did not get that far. |
| `scorer_local.log` | **DEAD** except one line | A killed run. Its one salvageable line is a *second, independent* pass of the same control arm at 11,613 s, also `+0.3182`. Together with the 14,200 s run above, the macOS control arm has now passed **twice**. |
| `scorer_local_rerun.log`, `scorer_sensitivity.log` | **DEAD** | Partial stubs from killed runs. Both end mid-setup at the mean-z drift line. No arm completed. |
| `armtest.log` | **DEAD — a throwaway** | A smoke test of the `--arms` flag, kept only because rule 8 forbids deleting logs. It was never a run and carries no result. Named in every handoff since #22 as "a throwaway smoke test" but classified in this map for the first time on 2026-09-05, when `test_results_readme_classifies_every_artefact_that_exists` went red on it. |
| `alpha_margin_macos.npz` | **DIAGNOSTIC — COMPLETE** | The durable artefact of `16_alpha_margin.py`, the slow path through `run_audit` (19,123 s). Its 80 alpha selections hash to `ALPHA DIGEST fc299c9c0f47466e`, **identical to `alpha_margin_fast_macos.npz`** from the fast path. This pair is what closes A9 elimination #6: the 80 ridge alphas cannot differ across platforms, margin min 1.034e-04, median 5.099e-03, 0/80 below 1e-06. Prefer the digest to the raw file. |
| `nsclc_v3_stablesort_run.log` | **COMPLETE — the A9/A10 sensitivity run** | `05_run_nsclc.py --outdir results/nsclc_v3_stablesort`, PID 16832, detached and verified (PPID 1, PGID 16832, STAT `Rs`), 2026-09-05. Ran to completion in **227.55 s** on an otherwise idle machine and wrote `results/nsclc_v3_stablesort/`. Ends with the `Written to` line and the headline block, so the log itself is sufficient evidence of completion here — unlike the partition-variance logs, this script prints its result. |
| `nsclc_v3_pinned_run.log` | **ROW WRITTEN BEFORE THE RUN, with the literal word `pending`.** The log of `05_run_nsclc.py --outdir results/nsclc_v3_pinned` (ledger H86 step 3), detached under a detached `caffeinate`, launched beside session 60's baseline suite (a clean clone elsewhere), so its wall clock measures that contention and is not an idle figure. **RESULT: COMPLETE** — PID 12555, launched 01:12:32 CDT 2026-09-24 on AC, ended with the `Written to results/nsclc_v3_pinned  (7.0 min)` line and the headline block, no traceback; the script's own runtime 422.28 s (227.55 s for `nsclc_v3_stablesort` on an idle machine). |
| `pancancer_v3_pinned_run.log`, `memsample_20260924_run60_pancancer.log` | **ROWS WRITTEN BEFORE THE RUN, with the literal word `pending`.** The pinned pan-cancer run's log (ledger H86 step 4) and a 60 s `vm_stat`/swap sampler beside it, both detached under a detached `caffeinate`. **RESULT: COMPLETE** — PID 32820, launched 03:21:26, log last written 05:01:47 CDT 2026-09-24, ending with the headline block and `Written to results/pancancer_v3_pinned  (97.1 min)`; no traceback. The sampler took 102 readings and ended `run 32820 gone` at 05:02:30: peak resident size 6,242,704 KB, swap used at most 6,520.75 MB of 7,168, never a kill. |
| `science_gaps_stablesort.log` | **COMPLETE** | `11_close_science_gaps.py --results results/nsclc_v3_stablesort`, PID 48221, detached (PPID 1, PGID 48221), 2026-09-05. Adds the multiplicity columns (`p_two_sided`, `q_bh`, `beats_null_bh`) that `05_run_nsclc.py` does not itself emit — the asymmetry Supplementary Table S1 records between the two frozen cohorts. Run against the **sensitivity** directory only; `nsclc_v3/` is frozen and was not touched. A first attempt was run in the foreground and was killed by a 2-minute tool timeout (exit 143); this is the detached re-run under a fresh log name. |
| `science_gaps_stablesort_run2.log` | **FAILED — three of five stages died, and the script still exited 0.** Keep it: it is the evidence for the defect. | `11_close_science_gaps.py --results results/nsclc_v3_stablesort`, PID 98216, detached (PPID 1, PGID 98216), 2026-09-06 11:42-11:43:33, **~90 s**. The FIRST run of the A11 cohort-scope fix on a cohort where the patient filter is not a no-op. **A4 died with `KeyError: 'patient_id'` and A4b cascaded off A4's unset `cols`.** Cause: `DataFrame.join(how="inner")` preserves the left index's NAME only when the two indexes are identical — measured on pandas 2.2.3, a subset, a superset, a partial overlap and even a reordering all drop it to `None` — so `reset_index()` produced a column called `index`. The A11 fix had been verified only against pan-cancer, where the filter is a measured no-op, and the KeyError is upstream of the filter and cohort-independent, so **the one case checked was the one case that could not show it**. Superseded by `..._run3.log`; the `science_gaps.json` this wrote is NOT a result. |
| `science_gaps_stablesort_run3.log` | **See the tail before trusting this row** — it was launched 2026-09-06 11:49:54 and was still in A4b at the time of writing | `11_close_science_gaps.py --results results/nsclc_v3_stablesort`, PID 14226, detached and verified (PPID 1, PGID 14226, STAT `Rs`), the re-run after the `join_scores_to_meta` fix. A4 now reports **944 of 7,168 joined patients used, 16 signatures, 31 sites, median R² of site on the label 0.1453** — genuinely NSCLC, against pan-cancer's 202 sites and 0.4438, which is what the pre-fix run wrote into this directory. **How to tell it finished:** the log ends with a `Written to .../science_gaps.json` line AND `science_gaps.json` carries a `stage_failures` key that is an empty list. The script now returns 1 when any stage fails, so a green exit is meaningful for the first time. |
| `selftest_20260906.log` | **COMPLETE — PASSED at 111 rows.** Superseded; kept as the pre-A10-rows baseline. | `19_check_numbers.py --self-test`, PID 21990, detached (PPID 1, PGID 21990), 2026-09-06 11:54. Detached deliberately: the self-test now exceeds the 2-minute foreground tool timeout. Every row must read `CAUGHT`; a row reading `MISSED` means a claim pattern cannot detect a wrong number and is worth more than any result in this directory. |
| `selftest_20260906_run2.log` | **COMPLETE — FAILED, 1 of 122 rows not caught.** Keep it: this is the record of the checker catching a real drift. | Same command, PID 27219, 2026-09-06 11:59, run after the eleven A10 rows were added. The uncaught row was **`test count (live pytest collection)`** — the two tests added this session took collection to 101 while eight documents still said 100, so the injected substitution had nothing correct to corrupt. Fixed by updating the eight documents, not by touching the row. |
| `selftest_20260906_run4.log` | **COMPLETE — PASSED, all 125 injected errors caught.** This is the current self-test of record; run3 is superseded. | `19_check_numbers.py --self-test`, PID 34240, detached. Adds four rows for A11's corrected NSCLC Control C and for Limitations 9's pinned value. One of them injects the PAN-CANCER median R² in place of the NSCLC one, so it pins the ability to tell the two cohorts apart rather than merely the ability to notice a changed digit. |
| `selftest_20260906_run3.log` | **COMPLETE — PASSED, all 122 injected errors caught.** This is the current self-test of record. | Same command, PID 29888, 2026-09-06 12:02. 111 rows → 122: the eleven new rows cover the A10 pinned ssGSEA arm, and each targets a DIFFERENT pattern from its pre-fix counterpart, because the whole point of the pair is that the two numbers can move independently. |
| `full_suite_20260906_run3.log` | **COMPLETE — 101 passed in 291.95 s (4 m 52 s), ZERO failures.** The cleanest suite wall clock since the idle baseline, but still NOT an idle one. | `pytest tests/ -q --durations=5`, PID 30602, detached and verified (PPID 1, PGID 30602, STAT `Rs`). **The suite held 101 at the time** — a historical count, not the live one, which the checker reads from a live pytest collection. NOT an idle timing: at launch the load average was 12.72/20.70/18.75 on 14 cores with an unrelated `python -m theia.analysis.transportability` at 228% CPU (PID 30293, PPID 1009, not this repo), and swap was 13,119 of 14,336 MB used, and the load stayed in the 12-24 band throughout. Against the 48.7 s the suite took idle when it held 94 of them, 291.95 s at 101 is still ~6x the idle baseline, so this measures contention as well as the suite -- but it is 4.2x faster than the 1,216.85 s the same suite took last session at load 38-71, which is the useful comparison. Slowest tests: `test_run_audit_computes_the_primary_when_axis_supplied` 30.78 s, `test_residual_collapses_when_image_cannot_see_immune_module` 29.68 s. **Note for anyone monitoring this job:** the parent sits at 0.0% CPU in STAT `Ss` for minutes at a time while a child does the work -- check the process GROUP (`awk '$3==PGID'`), not the PID, or a live run reads as dead. |
| `full_suite_20260906_run4.log` | **DIAGNOSTIC — post-merge verification** | `pytest tests/ -q`, PID 40245, detached, 2026-09-06 19:56, after removing four dead `ssgsea_stable_*` authority keys. **Check the tail for the `N passed` line.** Named `run4` because `run3` was ALREADY TAKEN and committed: this session reused it, `detach.py`'s `O_TRUNC` destroyed the 101-passed record, and it was recovered only because it had been committed. **Check `ls results/` before choosing a log name, not just memory.** Run with `/opt/anaconda3/bin/python3` explicitly — bare `python3` resolved to Xcode's 3.9 in one shell this session and failed with `ModuleNotFoundError: scipy`, which looks exactly like a broken edit. |
| `full_suite_20260907.log` | **DIAGNOSTIC — the suite as it stood that session, and a CONTENTION measurement** | `pytest tests/ -q --durations=5`, PID 86641, detached and verified (PPID 1, PGID 86641, STAT `Rs`), 2026-09-07 ~01:35. **103 passed, 1 failed in 412.84 s (6 m 53 s).** The single failure was `test_results_readme_classifies_every_artefact_that_exists`, failing because THIS LOG did not yet carry the row you are reading — the same self-referential failure `full_suite_20260906.log` records, and the test working as designed. Everything else was green at that session's count. **NOT an idle timing:** four unrelated detached `python -m theia.engine.train` / `theia.analysis.external_grounding` jobs (PIDs 66208, 66210, 71010, 71012 — not this repo) held ~913% CPU throughout, and swap was 16.9 of 18.4 GB used with ~1.5 GB free. Against the 48.7 s idle baseline (measured back when the suite was smaller) this is ~8.5x, so it measures the machine as much as the suite. Slowest: `test_number_checker_is_not_vacuous` 103.45 s, `test_run_audit_computes_the_primary_when_axis_supplied` 49.46 s. **A genuinely idle wall clock has still never been obtained** — seven sessions running. |
| `selftest_20260907.log` | **COMPLETE — PASSED, all 126 injected errors caught.** This is the current self-test of record; `selftest_20260906_run4.log` is superseded. | `19_check_numbers.py --self-test`, PID 50483, detached (PPID 1, PGID 50483), 2026-09-07 15:41. 125 rows → 126: the added row injects the historical `83` into the abstract's headroom sentence, which is now a live-derived authority rather than a stored one. Run to verify the count carried forward in handoff #33 as "expect 126, NOT re-counted" — it is 126. |
| `selftest_20260908.log` | **COMPLETE — PASSED, all 129 injected errors caught, `SELFTEST_EXIT=0`, `ELAPSED_SECONDS=262`.** The self-test of record. Row was written BEFORE the run finished, deliberately, then corrected from the log. Re-run because `19_check_numbers.py` changed: session 40 added two authorities (`pancancer_min_excess_lo`, `pancancer_hypoxia_excess_lo`) and their two injection rows, so the expected row count rises from 127 to **129**. Supersedes `selftest_20260907_run7.log`, which is kept — the timed runs of this path are the only evidence of its spread. | `19_check_numbers.py --self-test`, detached with the timing wrapper, 2026-09-08 ~19:1x CDT, run in sequence with `full_suite_20260908_run2.log` in one wrapper so the two do not contend with each other. **CONTENDED** — the author's `theia.analysis.faithfulness` job was resident throughout. Six contended figures exist for this path (134 / 107 / 111 / 117 / 117 s) and still no idle one — and **262 s is a new worst, roughly 2x the previous 134 s**, with the author's `theia.analysis.faithfulness` job at ~300% CPU throughout. It measures the machine, not this path. Note `grep -c CAUGHT` on this log returns one MORE than the true count: line 6 is a header containing both CAUGHT and MISSED. |
| `full_suite_20260908_run9.log` | **COMPLETE — `146 passed in 205.13s (0:03:25)`, ZERO failures, `SUITE_EXIT=0`, `ELAPSED_SECONDS=206`.** **THE GREEN SUITE OF RECORD.** Row was written BEFORE the run finished, deliberately, then corrected from the log — in the hyphenated form, which is what `_run8` failed to do. A 146-test run, and it was expected to be GREEN because: `_run8` went red only on the historical-run-row guard, which has since been satisfied by rewriting two rows in this file into the hyphenated form. No test was added or removed between the two runs. | `pytest tests/ -q --durations=15`, detached with the timing wrapper, 2026-09-08 22:52:09-22:55:35 CDT. **CONTENDED** — one Blender render resident. **205.13 s at the largest test count yet.** Fourteen timed runs now span 191 / 205 / 217 / 226 / 231 / 234 / 239 / 250 / 281 / 297 / 309 / 368 / 591 / 852 / 1,145 s across 113-to-146-test runs — a 6.0x spread on ONE machine, which the durations breakdown explains: a handful of tests dominate. Still NOT idle. The self-test was NOT re-run: `19_check_numbers.py` is unchanged since `selftest_20260908_run3.log`. |
| `wrapper_20260908_run42e.log` | **DIAGNOSTIC — the fifth timing wrapper's own log, not a result.** Single-stage (`pytest` alone), so its total and the suite figure should agree to a second or two. Row written before the run. | `tools/detach.py` + `run42e.sh`, 2026-09-08 ~22:3x CDT. |
| `full_suite_20260908_run10.log` | **COMPLETE — `146 passed in 209.21s (0:03:29)`, ZERO failures, `SUITE_EXIT=0`, `ELAPSED_SECONDS=210`.** Row was written BEFORE the run finished, deliberately, then corrected from the log — including its launch time, which the row had guessed as `~23:55` and the wrapper records as `23:51:17`. **THE FIRST GENUINELY IDLE SUITE WALL CLOCK**, a figure wanted since session 29 — and its value is the timing, not the pass/fail, which was expected green because the tree was unmodified and at the same commit as `_run9`. **THE MEASUREMENT FALSIFIES THE ASSUMPTION IT WAS RUN TO CONFIRM.** An idle 209.21 s against `_run9`'s 205.13 s at the SAME 146-test count with a Blender render resident at ~210% CPU: the idle run was **4.08 s SLOWER**, i.e. the difference is run-to-run noise and the Blender load cost this suite nothing measurable. Ten sessions of handoffs have attributed the 6x spread to "contention" as though any load explained it; what this bounds is that a ~210% CPU load on a 14-core machine does not. The genuinely slow runs (591 / 852 / 1,145 s) must be explained by something heavier or different in kind — the `theia.analysis.faithfulness` job at ~300% CPU that coincided with the 262 s self-test is the documented candidate, and swap pressure is the other, since swap ran at 80-90% used throughout this session. **Do not quote "contention" as the explanation for a slow suite without naming the process and its CPU share.** **IDLE PER-TEST DURATIONS, the first ever recorded without load.** `test_number_checker_is_not_vacuous` alone is **115.65 s of 209.21 s — 55% of the suite** — because it shells out to the entire `--self-test`. The slowest 15 of that **146-test** run are 197.75 s, i.e. **94.5%** of the wall clock, so **the remaining 131 cost 11.46 s between them.** Compare the contended figure of 121.70 s for the same test: it is only **5% faster idle**, which is the same conclusion as the wall clock reached independently. **Decision recorded, not taken: the 121 s test STAYS in the default suite.** The friction is now measured rather than argued — removing it would take the suite from ~209 s to ~94 s — but it is the guard proving the number checker is not vacuous, and the checker HAS been vacuous once, reporting "62 claims OK" while checking nothing. A 3.5-minute suite does not justify weakening the guard that catches the project's worst failure mode. Revisit only if the suite passes ~10 minutes, and if it ever moves behind a marker, `reproduce.sh --check` must still run it. | `pytest tests/ -q --durations=15`, detached with the timing wrapper (PID 51430, verified PPID 1, PGID == PID, STAT `Ss`), 2026-09-08 23:51:17-23:54:47 CDT. **IDLE, and this is the first row in this file that can claim it.** Verified from `ps` at launch: the Blender render resident for `_run8`/`_run9` had exited, no `pytest` was running anywhere on the machine, and the only other user process of consequence was the Codex desktop app at 1.6% CPU. **Sixteen timed runs now span 191 / 205 / 209 / 217 / 226 / 231 / 234 / 239 / 250 / 281 / 297 / 309 / 368 / 591 / 852 / 1,145 s across 113-to-146-test runs.** |
| `full_suite_20260909.log` | **COMPLETE and RED — `3 failed, 145 passed in 215.78s`, `SUITE_EXIT=1`, `ELAPSED_SECONDS=216`. KEPT DELIBERATELY: all three failures are TRUE POSITIVES and two of them caught the session's own defect.** Row was written before the run, expecting green, and is corrected here from the log. (1) `test_historical_run_rows_state_no_live_test_count` fired on the `_run10` row **in this file, written an hour earlier by the session that was quoting the guard's own rule at the time** — the durations sentence stated a count in the live form. That is the FOURTH instance of this defect committed by a session enforcing the convention, after session 42's three. (2) and (3) the number checker and the vacuity scan went red because the suite grew and **nine documents still stated the old size**; that is the checker doing its job, not a defect. The count moved because session 43 added two mutation-proven tests: `test_committed_pdf_figures_embed_a_real_font_and_are_not_type3` and `test_the_pdf_font_check_can_fail`. Superseded by `full_suite_20260909_run2.log`. | `pytest tests/ -q --durations=15`, detached with the timing wrapper, 2026-09-09 00:07:12-00:10:48 CDT. Load: no Blender, no foreign `pytest`; Codex resident. |
| `wrapper_20260909_run43b.log` | **DIAGNOSTIC — the seventh timing wrapper's own log, not a result.** Single-stage (`pytest` alone); `TOTAL_ELAPSED_SECONDS=216` against the suite's `215.78s`. Row written before the run, because a wrapper writes its own log into `results/` and that log needs a row too. | `tools/detach.py` + `wrap43b.sh`, 2026-09-09 00:07:12-00:10:48 CDT. |
| `full_suite_20260909_run44.log` | **COMPLETE — `152 passed in 233.96s (0:03:53)`, ZERO failures, `SUITE_EXIT=0`, `TOTAL_ELAPSED_SECONDS=235`.** **THE GREEN SUITE OF RECORD.** Row was written BEFORE the run finished, deliberately, with the expectation `152 passed / SUITE_EXIT=0` and no invented numbers, then corrected from the log. **A timing datum for the unexplained 6x spread, and it does not fit the tests-added story:** 233.96 s at the 152-test size against `_run2`'s 215.86 s at the 148-test size — **+18.1 s for four tests that together cost under 3 s**. The machine was as close to idle as this project has measured (no Blender, no theia, no foreign pytest — the wildfire_ai suite had exited ~2 min before launch; the only heavy process was this suite at 1,170% CPU), so **CPU contention cannot be the explanation**. What differed from the 209.21 s idle run is memory: the available floor was 6.18 GiB across this session against 7.69 GiB in #43, with swap flat at 77.5%. **Swap and memory pressure remain the only untested candidate and this run is consistent with them.** The first run at the **152-test** size: session 44 added `test_historical_run_rows_state_no_live_duration_tally` and `test_the_duration_tally_check_can_fail` (a running tally of elapsed sessions inside a historical run row goes stale with the CALENDAR rather than with any event — the sibling defect to the live-test-count guard), and `test_rendered_figure_text_uses_american_spelling` and `test_the_figure_spelling_check_can_fail` (the US-spelling guard covered four documents and NOT the figure generator, so every axis label and schematic caption in the five deposited figures was unguarded — figure0's poster panel read "Residualise the SIGNATURE"). Both new guards were mutated against the REAL file until red and restored. It is the only run covering session 44's edits to `10_make_figures.py`, `22_build_public_snapshot.py`, `tests/test_pipeline.py`, the manuscript's Limitations 8 and Data Availability, `17-CPTAC-PLAN.md`, and the nine documents whose test count moved 148 -> 152. | `pytest tests/ -q`, detached with the timing wrapper, 2026-09-09 ~10:0x CDT. **Load to be recorded from `ps` at launch.** |
| `wrapper_20260909_run44a.log` | **DIAGNOSTIC, not a result.** The timing wrapper's own log for `full_suite_20260909_run44.log`. Single-stage, so its `TOTAL_ELAPSED_SECONDS` should match that run's pytest figure to within a second or two; a larger gap means interpreter startup or collection was unusually slow and is itself worth reading. | `bash` wrapper printing START, END and ELAPSED_SECONDS around one `pytest` invocation, 2026-09-09. |
| `full_suite_20260909_run45.log` | **COMPLETE and RED — `4 failed, 151 passed in 243.92s (0:04:03)`, `SUITE_EXIT=1`, `TOTAL_ELAPSED_SECONDS=245`. KEPT DELIBERATELY: all four were TRUE POSITIVES, and three of them were one defect.** Row was written BEFORE the run finished, deliberately, with the INTENT and the EXPECTATION and no invented numbers, then corrected from the log. **What it caught:** writing the row you are reading, this session quoted the offending phrase from session 44's defect verbatim inside a run row, which re-instantiated the very defect being described — `test_historical_run_rows_state_no_live_test_count` went red, and with it the number checker (`:108`, 1 mismatch) and `test_no_claim_pattern_is_vacuous_on_the_real_tree`, three failures from one line. The fourth was independent and also this session's: the rewritten deposit-failure table in `pipeline/README.md` named `07-ABSTRACT-DRAFT.md` with the disclaimer "not a deliverable", which is not one of the five phrasings `_NOT_DEPOSITED_MARKER` accepts, so deposited prose pointed a reader at an undeposited document. Both fixed; see `_run45b`. **A guard catching its author is the guard working.** Intent: the first suite run covering session 45's edits — the fix to this file's own row 106 (session 44 committed it RED: the row asserting its suite was green stated its size in the bare live form — an unhyphenated count followed by the word "tests" — which `test_historical_run_rows_state_no_live_test_count` exists to catch and was failing at commit `6b6e6b8`, invisible only because the recorded size happened to equal the live count. Writing THIS row reproduced the same defect a third time, by quoting the offending phrase verbatim; the guard caught it in the first suite run and the quotation was replaced by this description); `snapshot/README.md` added to the number checker's `DOCS` and `COVERAGE_FLOOR`; a reconciliation in `19_check_numbers.py` that refuses a document scanned without a floor; `_prose_documents()` extended by three files after measuring zero added noise from both the bracket and bold scans; `_US_SPELLING_DOCS` extended with `snapshot/README.md` and `CAPTIONS.md`; and three new guards for the NINTH axis (`test_every_prose_document_is_classified_for_spelling`, `test_every_prose_document_is_scanned`, `test_the_prose_classification_checks_can_fail`), all mutated against the REAL tree until red and restored. Expectation: `SUITE_EXIT=0`, ZERO failures, at the **155-test** size. | `pytest tests/ -q`, detached with the timing wrapper, 2026-09-09 ~10:2x CDT. **Load: available-memory floor 4.38 GiB over 20 samples this session, ceiling 7.84, ZERO samples above 8 GiB; swap flat at ~76.7%.** |
| `full_suite_20260909_run45b.log` | **COMPLETE — `155 passed in 228.64s (0:03:48)`, ZERO failures, `SUITE_EXIT=0`, `TOTAL_ELAPSED_SECONDS=229`.** **THE GREEN SUITE OF RECORD.** Row was written BEFORE the run finished, deliberately, with the INTENT and the EXPECTATION and no invented numbers, then corrected from the log. **A second timing datum for the unexplained spread, and it points the same way as `_run44`'s:** 228.64 s here against `_run45`'s 243.92 s — the SAME tree at the SAME size, 42 minutes apart, differing by 15.3 s (6.7%) with no test added or removed and no foreign process in either window. Whatever moves the wall clock is not the test count and not CPU contention from another repository; the available-memory floor this session was 4.38 GiB with ZERO of 20 samples above 8 GiB, and swap sat flat at ~76.7%. Intent: re-run after fixing the two defects `_run45` caught, to establish the green suite of record at the **155-test** size. | `pytest tests/ -q`, detached with the timing wrapper, 2026-09-09 ~10:3x CDT. **Load: same session as `_run45`; available-memory floor 4.38 GiB over 20 samples, ZERO above 8 GiB.** |
| `wrapper_20260909_run45b.log` | **DIAGNOSTIC, not a result.** The timing wrapper's own log for `full_suite_20260909_run45b.log`. Single-stage. | `bash` wrapper printing START, END and ELAPSED_SECONDS. |
| `full_suite_20260909_run45c.log` | **COMPLETE — `155 passed in 246.01s (0:04:06)`, ZERO failures, `SUITE_EXIT=0`, `TOTAL_ELAPSED_SECONDS=247`.** **THE GREEN SUITE OF RECORD**, superseding `_run45b`. **A THIRD timing on an unchanged tree at the same size, and the spread did not narrow:** 243.92 s (`_run45`), 228.64 s (`_run45b`), 246.01 s (`_run45c`) — a 17.4 s span, 7.6%, across three runs in 27 minutes with no test added or removed and no foreign pytest in any window. Test count and cross-repository CPU contention are both excluded by these three points; memory and swap pressure remain the only untested candidate. Row was written BEFORE the run finished, deliberately, with the INTENT and the EXPECTATION and no invented numbers, then corrected from the log. Intent: confirm green after one further fix that `_run45b` could not have caught, because the defect arrived from outside this session. `_tracked_prose()` was walking the FILESYSTEM despite its name, and a 228 KB `poster/poster_preview_tmp.html` appeared in the working tree at 10:41 from another process — several Claude sessions and an external review run against this repository — which the new classification guard correctly reported and wrongly owned. Rescoped to `git ls-files`: a document becomes the project's when it is staged, which is also when someone can be asked to classify it. The stray was left untouched and removed itself. Expectation: `SUITE_EXIT=0`, ZERO failures, at the **155-test** size. | `pytest tests/ -q`, detached with the timing wrapper, 2026-09-09 ~10:4x CDT. |
| `wrapper_20260909_run45c.log` | **DIAGNOSTIC, not a result.** The timing wrapper's own log for `full_suite_20260909_run45c.log`. Single-stage. | `bash` wrapper printing START, END and ELAPSED_SECONDS. |
| `full_suite_20260909_run45d.log` | **COMPLETE — `155 passed in 229.23s (0:03:49)`, ZERO failures, `SUITE_EXIT=0`, `TOTAL_ELAPSED_SECONDS=229`.** **THE GREEN SUITE OF RECORD**, superseding `_run45c`, and the last run of session 45. **A FOURTH timing on the same 155-test suite:** 243.92 / 228.64 / 246.01 / 229.23 s across four runs in 38 minutes. The span is 17.4 s (7.6%) and the values do not trend — they alternate. Whatever moves this wall clock is neither the test count nor another repository's CPU load; both are excluded by these four points on an essentially unchanged tree. Row was written BEFORE the run finished, deliberately, with the INTENT and the EXPECTATION and no invented numbers, then corrected from the log. Intent: confirm green after the AUTHORSHIP change, which arrived mid-session and is the largest documentation change in weeks — the project stopped being a sole-author submission on 2026-09-09 and six documents were rewritten (`09-PAPER-DRAFT.md`, `submission/COVER-LETTER.md`, `11-SUBMISSION-PACK.md`, `submission/ABSTRACT-PORTAL-DRY-RUN.md`, `CITATION.cff`, `poster/poster.html`). Three of those are DEPOSITED and two are covered by the US-spelling guard, which already caught one British `licence` inside a multi-line HTML comment the guard strips only per line. Expectation: `SUITE_EXIT=0`, ZERO failures, at the **155-test** size. | `pytest tests/ -q`, detached with the timing wrapper, 2026-09-09 ~10:5x CDT. |
| `wrapper_20260909_run45d.log` | **DIAGNOSTIC, not a result.** The timing wrapper's own log for `full_suite_20260909_run45d.log`. Single-stage. | `bash` wrapper printing START, END and ELAPSED_SECONDS. |
| `selftest_20260915_run46.log` | **ROW WRITTEN BEFORE THE RUN, deliberately, with the INTENT and the EXPECTATION and no invented numbers; corrected from the log afterwards.** Intent: `selftest_20260908_run3.log` stopped covering `19_check_numbers.py` when session 45 changed that script twice — `snapshot/README.md` was added to `DOCS` as the fifteenth scanned document, and the `COVERAGE_FLOOR`/`DOCS` reconciliation was added to the floor check. A self-test of record that predates the script it tests is not a self-test of record. Expectation: PASSED with `SELFTEST_EXIT=0`; the injected-error row count may rise, because a document was added to the scan AND a new reconciliation was added to the floor path, which is a different situation from session 42's document-only addition. **RESULT: PASSED, all 129 injected errors caught, `SELFTEST_EXIT=0`, 136 s.** The expectation was wrong and the reason is worth keeping: the count did NOT move, because the self-test injects errors into the AUTHORITY rows, and session 45 added a document and a reconciliation — neither of which is an authority. Adding a scanned document adds claims, not injectable patterns. The ninth contended figure for this path. Note `grep -c CAUGHT` over this log returns one more than the tool's own summary, because the explanatory header contains the word too; the tool's count is the authority, and this is the line-versus-occurrence trap in miniature. | `19_check_numbers.py --self-test`, first of two stages in one wrapper, 2026-09-15 22:15:23–22:17:39 CDT. **Load: no Codex, no foreign `pytest`, no Blender; two `esbuild` processes from an unrelated repository at ~2.5% CPU each.** |
| `full_suite_20260915_run46.log` | **ROW WRITTEN BEFORE THE RUN, deliberately, with the INTENT and the EXPECTATION and no invented numbers; corrected from the log afterwards.** Intent: establish the suite baseline at the committed state `70f8d46` at the START of session 46, before any edit — because sessions 44 and 45 BOTH committed red, and in both cases the red was invisible to the session that caused it. A baseline run is the only way to tell a defect this session introduced from one it inherited. Expectation: `SUITE_EXIT=0`, ZERO failures, at the **155-test** size that session 45 left behind. **RESULT: `155 passed in 265.22s (0:04:25)`, ZERO failures, `SUITE_EXIT=0`.** **THE GREEN SUITE OF RECORD**, and it answers the question it was run to answer: **session 45's final commit `70f8d46` is GREEN.** Two sessions running had committed red, so the inherited state could not be assumed — it had to be measured, and it is sound. The tree here is `70f8d46` plus the three run rows this session wrote before launching, which the write-the-row-first rule requires and which the suite reads. **A FIFTH timing on the 155-test tree, and the SLOWEST** — the previous four spanned 228.64 s to 246.01 s within one 38-minute window, and this one sits 7.8% above the top of that span, six days later. The spread therefore widens rather than settles, and this datum kills one more candidate: it was taken with no Codex, no foreign `pytest` and no Blender resident, which is a LIGHTER load than any of session 45's four, and it is slower than all of them. Swap, however, had tripled in total size and sat at 93.9% used. Memory pressure is now the only surviving hypothesis on the list and the first one this session can point at rather than merely fail to exclude. | `pytest tests/ -q`, second of two stages in one wrapper, 2026-09-15 22:17:39–22:22:04 CDT. Same load as `selftest_20260915_run46.log`. |
| `full_suite_20260915_run46b.log` | **ROW WRITTEN BEFORE THE RUN, with the INTENT and the EXPECTATION and the literal word `pending` where the result goes.** Session 46 wrote a plausible test count, wall clock and injected-error count into the `_run46` rows before running anything, caught it on re-reading and replaced them with `pending` — #43 committed that same defect and did not catch it, so the word is now part of the convention rather than a habit. Intent: the run after session 46's LAST edit, which is what the rule actually requires — #44 and #45 both ran their final suite before their final edit and both committed red. Expectation: `SUITE_EXIT=0`, ZERO failures, at the **157-test** size, up two from the inherited size: `test_supplementary_shapes_match_an_independently_derived_expectation` and its can-fail companion. **RESULT: `157 passed in 608.60s (0:10:08)`, ZERO failures, `SUITE_EXIT=0`, `TOTAL_ELAPSED_SECONDS=609`.** **THE GREEN SUITE OF RECORD.** **And the timing is the most important number this session produced: 608.60 s against 265.22 s twenty minutes earlier — 2.3×, on a tree differing by two cheap tests that together run in 1.65 s.** That is by far the largest same-size spread ever recorded here; the previous widest was 7.6% across four runs in 38 minutes. **What was measured immediately afterwards, at 22:55:50:** available memory **4.80 GiB**, *below* the floor of 5.90 from a twenty-sample window taken 25 minutes earlier; swap total **resized by the OS from 19,456 MB to 17,408 MB** mid-session, at 94.1% used; and **no new user process** — the heaviest consumers were WebKit, WindowServer and `cameracaptured`, all present and unremarkable during the fast run too. **This is one correlated pair, not a controlled measurement, and it must not be quoted as the explanation.** But it is the first time a session has caught a slow run and measured the machine within five minutes of it, and every non-memory candidate was excluded by that measurement rather than by argument. **Workstream 2 task 2 — sampling `vm_stat` DURING a run and correlating against per-test durations — is now the highest-value compute task in the project.** | `pytest tests/ -q`, detached with the timing wrapper (PID 59189, verified PPID 1, PGID == PID, STAT `Ss`, child at 857% CPU), 2026-09-15 22:43:46–22:53:55 CDT. Load: no Codex, no VM, no foreign `pytest`, no Blender; two `esbuild` processes from an unrelated repository. |
| `wrapper_20260915_run46b.log` | **DIAGNOSTIC, not a result.** The timing wrapper's own log for `full_suite_20260915_run46b.log`. **Single-stage**, unlike `_run46a`, so its `TOTAL_ELAPSED_SECONDS` should match the suite's own wall clock to within a second or two. Row written before the run. | `bash` wrapper printing START, END and ELAPSED_SECONDS. |
| `ssgsea_sort_bench_20260916.log`, `ssgsea_sort_bench.json` | **ROW WRITTEN BEFORE THE RUN, with the literal word `pending`. DIAGNOSTIC — a measurement, not a pipeline result.** Intent: item 4 of the science list, "measure whether the A10 fix changes ssGSEA runtime, or drop the claim". The figure it replaces is a pair of single 6.5-second micro-benchmarks taken inside much longer runs on contended machines, whose ratio confounds the algorithm with the machine — and which points the WRONG WAY for the change actually made: `kind="stable"` selects a mergesort where the default is an introsort, so on a tie-dense array it should be SLOWER, not faster. **Method, built to avoid the defect it replaces:** both orderings in ONE process on the SAME real 944 x 41,046 NSCLC rank table, A/B INTERLEAVED so mid-run drift penalises both equally, an untimed warm-up pass so page faults are not charged to whichever runs first, seven repetitions reporting the MEDIAN and the full spread, and the power source recorded. **RESULT: the A10 fix makes the sort ~1.7x SLOWER, not faster.** Median stable/quicksort ratio **1.7381**, worst-case bracket [1.6710, 1.8838] over 7 interleaved repetitions; 1.2002 ms/row quicksort against 2.0860 ms/row stable. **Run TWICE, from two locations, on differently loaded machines: ratios 1.6808 and 1.7381.** Same direction, same magnitude, and the seven repetitions inside each run vary by under 2% — so this is an algorithm effect and not a machine one, which is exactly what the figure it replaces could not distinguish. **What this settles:** any reading of the old pinned-vs-pre-fix pair as the fix having made ssGSEA ~2.2x FASTER is wrong; that ratio was load. The fix costs runtime, as a mergesort replacing an introsort on a tie-dense array should. **What it does NOT overturn:** no submitted or deposited document ever quoted that ratio — it lived only in the handoff chain — so nothing in the manuscript needed correcting. **Tie profile, independently reconfirming two manuscript numbers:** 944 of 944 rows carry ties and **32,893,533** entries are tied by this benchmark's excess-per-group definition, which reconciles exactly to the manuscript's **35,394,448 of 38,747,424** under the project's own `count_row_ties` definition (full group size) — the two differ by the 2,500,915 tied groups. | `python3 tools/ssgsea_sort_bench.py 7 200`, detached, 2026-09-16. |
| `reproduce_check_20260917_run49.log`, `memsample_20260917_run49d.log` | **ROWS WRITTEN BEFORE THE RUN, with the literal word `pending`.** Intent: ledger C6, `bash reproduce.sh --check` timed end to end on this laptop with the load and power recorded, after the 2026-09-17 restructure (the full path now fetches first; `--check` is unchanged in what it runs). Session 47's only figure, 1,283 s, was contended. Launched on AC power with desktop apps running and no other job of this project on the machine; the sampler records whether that is quiet. Expectation: exit 0. **RESULT: RED, KEPT -- `CHECK_EXIT=1` after `TOTAL_ELAPSED_SECONDS=307` (START 00:56:56, END 01:02:03 CDT, AC, battery 38% to 40%, load1 5.86 at launch and 9.29 at the end). Every stage before the suite passed; the suite reported `1 failed, 174 passed in 291.80s`: `test_prose_documents_have_balanced_delimiters` caught an inline code span this session had split across a line break in `snapshot/README.md` (committed in `34e4b8d`, whose gate ran selected tests only). A true positive, fixed. 307 s is a TIME-TO-FAILURE: the stages after the suite never ran. The figures re-rendered without a diff.** | `bash reproduce.sh --check`, detached, 2026-09-17. |
| `reproduce_check_20260917_run49b.log` | **ROW WRITTEN BEFORE THE RUN, with the literal word `pending`.** Intent: the re-run after that one-line fix, same machine state. Expectation: exit 0 and the first uncontended-by-this-project end-to-end `--check` figure since 2026-09-07. **RESULT: `CHECK PASSED`, `CHECK_EXIT=0`, `TOTAL_ELAPSED_SECONDS=509` (START 01:03:23, END 01:11:52 CDT), AC power, battery 40% to 42%; the suite inside it `175 passed in 289.52s`. THE --check FIGURE OF RECORD: 509 s with nothing else of this project running, but NOT idle -- desktop applications kept load1 between 4.8 and 15.6 (sampled). Memory sampler: 30 samples 00:56:56-01:11:26 spanning both runs; the 17 inside `_run49b` read 3.151-6.478 GiB available (mean 3.959, 0 above 8 GiB), load1 4.76-15.58, power ['AC_Power'].** | `bash reproduce.sh --check`, detached, 2026-09-17. |
| `full_suite_20260917_run49d.log`, `wrapper_20260917_run49d.log` | **ROWS WRITTEN BEFORE THE RUN, with the literal word `pending`.** Intent: the re-run after `_run49c`'s two fixes (one word, four removed file copies). Same machine state as `_run49c` minutes later: AC power, desktop apps only. Expectation: `SUITE_EXIT=0`, zero failures, 174-test size. **RESULT: `174 passed in 277.52s (0:04:37)`, ZERO failures, `SUITE_EXIT=0`, `TOTAL_ELAPSED_SECONDS=278`, START 00:44:20, END 00:48:58 CDT, AC, battery 34% to 35%. Slowest: `test_number_checker_is_not_vacuous` 157.90 s (57% of the run). THE GREEN SUITE OF RECORD at the 174-test size.** | `pytest tests/ -q --durations=25`, detached behind the power-stamping wrapper, 2026-09-17. |
| `full_suite_20260917_run49c.log`, `wrapper_20260917_run49c.log`, `memsample_20260917_run49c.log` | **ROWS WRITTEN BEFORE THE RUN, with the literal word `pending`.** Intent: session 49's second checkpoint, after the interim builder, the A15/C1 supplement fix, the C2 FreeType guard, the F-item tests, D5 and E13. Also the first timing in this session on a QUIETER machine: AC power, the foreign MPS job gone, load1 about 7 from desktop apps at launch -- recorded by the sampler, not assumed idle. Expectation: `SUITE_EXIT=0`, zero failures, 174-test size. **RESULT: RED, KEPT -- `4 failed, 170 passed in 280.02s (0:04:40)`, `SUITE_EXIT=1`, `TOTAL_ELAPSED_SECONDS=280`, START 00:38:11, END 00:42:51 CDT, AC, battery 33% to 34%. Two causes, both this session's and both true positives: a British spelling in a sentence written that evening in the paper's staged-file note, and four generated CAPTIONS.md/MANIFEST.md copies committed inside `session49_diagnostics/` that the prose-classification guards (three tests) correctly refused as unclassified documents. Both fixed; `_run49d` is the re-run. Slowest: `test_number_checker_is_not_vacuous` 157.50s. Memory sampler (it also ran through most of `_run49d`): 22 samples 00:38:11-00:48:41; the 10 inside this suite read 2.939-7.158 GiB available (mean 5.728, 0 above 8 GiB); swap total ['6144'] MB, peak use 75.2%; load1 6.27-8.84 inside the suite; power ['AC_Power'], battery 33% to 34%.** | `pytest tests/ -q --durations=25`, detached behind the power-stamping wrapper, 2026-09-17. |
| `selftest_20260916_run49.log` | **ROW WRITTEN BEFORE THE RUN, with the INTENT, the EXPECTATION and the literal word `pending`.** Intent: prove the three rows added on 2026-09-16 for ledger items E13 and D4 can fail: cancer type alone on pooled PFI, the pan-cancer patients without a type, and the index's complete-case count. Expectation: `SELFTEST_EXIT=0` with 134 injected errors caught (131 in `selftest_20260916_run48.log`, plus three). Launched while two macOS NSCLC diagnostic runs (ledger D1) held load1 above 100, so its duration measures that load. **RESULT: `SELF-TEST PASSED: all 134 injected errors were caught.`, `SELFTEST_EXIT=0`, `TOTAL_ELAPSED_SECONDS=505`, START 21:42:47, END 21:51:12 CDT, on BATTERY 39% to 24% (the laptop had been unplugged since the checkpoint suite). The count matches the prediction. THE SELF-TEST OF RECORD.** | `19_check_numbers.py --self-test`, detached behind the power-stamping self-test wrapper, 2026-09-16. |
| `full_suite_20260924_run60c.log` | **ROW WRITTEN BEFORE THE RUN, with the literal word `pending`. SESSION 60's CLOSING SUITE** over `37e7731`, after the session's last edit to anything the suite reads — the pinned pan-cancer run and its documentation (H86 steps 4–6) on top of everything `_run60b` covered. `pytest tests/ -q --durations=10`, detached under a detached `caffeinate`, in a clean clone with `pipeline/data` symlinked, log in the scratchpad and copied here. Expectation: green at the 203-test size; the self-test step, now about 1,012 rows, near 800–1,100 s on a quiet machine (the pan-cancer run has finished), and the suite near 1,300–1,700 s. Launch state is recorded in the result. **RESULT: GREEN — `203 passed in 1393.34s (0:23:13)`**, zero failures, zero skips, zero warnings (PID 41356 in a clean clone at `9f918c2` — `37e7731` plus this row's pending text — launched 05:16:53, last written 05:40:07 CDT 2026-09-24 — 1,394 s by the file clock, so no sleep; AC, 100% and charged; launch load 4.34 / 4.23 / 8.96, swap 3.5 of 5.1 GB, nothing else of this project running). **THE GREEN SUITE OF RECORD FOR SESSION 60**, superseding `_run60b`. **The self-test step's expectation was wrong and is kept as written:** it took **1,196.74 s** — 97 s above the band's top — at about 1,012 rows, 1.18 s a row even on a quiet machine, because the audit's 288 bindings each rescan a 3,169-literal document. The self-test is 85.9% of the suite. The 2,400 s limit stands at **2.01×** and was not changed. |
| `full_suite_20260924_b21.log` | **ROW WRITTEN BEFORE THE RUN, with the literal word `pending`.** The suite over `70e41fa`, B-21's cut: the manuscript body 6,620 → 4,980 words, five Results passages and the site control's stress test lifted word for word into `SUPPLEMENTARY-NOTE-2.md`, the note added to the checker's documents, and `B21_SUMMARY_SPEC`'s 25 bindings with their generated self-test rows. `pytest tests/ -q --durations=10`, detached under a detached `caffeinate`, in a clean clone with `pipeline/data` symlinked, log in the scratchpad and copied here. Expectation: green at the 203-test size; the suite minus the self-test already passed in the working tree (202 in 218.87 s), so what this run adds is the self-test over the moved claims, expected near 1,200–1,300 s on a quiet machine. **RESULT: RED — `1 failed, 202 passed in 1381.36s (0:23:01)`** (PID 85779, launched 10:12:33 CDT 2026-09-24 on BATTERY, 85% at launch, load 4.66 / 6.71 / 5.76; the self-test step 1,164.11 s). The failure is the self-test, and it is a true positive about this session's own edit: 3 injected errors not caught. Two are `B21_SUMMARY_SPEC` rows whose shapes ended in `$` — the self-test substitutes against WHOLE-FILE text, where `$` matches only at the end of the file, so both found nothing to inject into; `--strict` could not see it because claims are matched line by line. The third is the hand-written row "A10 pinned ssGSEA value (Limitations 9)": the shortened Limitation 9 wrapped its anchor "and at +0.174 under the pinned" across a line break. Fixed by anchoring both shapes on the neighbouring `0.3152] to [` and re-wrapping Limitation 9 so every checked phrase sits on one line; afterwards all 1,033 rows find their text and all 25 B-21 rows are CAUGHT in the self-test's own loop run alone. The re-run is `_b21b`. |
| `full_suite_20260924_b21b.log` | **ROW WRITTEN BEFORE THE RUN, with the literal word `pending`.** The re-run after `_b21`'s red self-test, over the commit that fixes its three rows (the two `$`-anchored `B21_SUMMARY_SPEC` shapes and Limitation 9's wrapped anchor). Same procedure: `pytest tests/ -q --durations=10`, detached under a detached `caffeinate`, clean clone with `pipeline/data` symlinked. Expectation: green at the 203-test size, the self-test step near `_b21`'s 1,164 s. **RESULT: GREEN — `203 passed in 1403.76s (0:23:23)`**, zero failures, zero skips (PID 35197 in a clean clone at `3f11869`, launched 11:04:07 CDT 2026-09-24 on BATTERY at 66%, load 3.06 / 3.10 / 2.85; the self-test step 1,194.00 s). **The wall clock is not the run time:** the laptop entered Maintenance Sleep on battery at 11:30:43 and again at 11:46:44 (`pmset -g log`) — `caffeinate -i` prevents idle sleep only — and the run finished near 12:21. pytest's timer does not advance during sleep, so the 1,403.76 s it reports is the working time, in line with `_b21`'s 1,381.36 s. **THE GREEN SUITE OF RECORD FOR B-21.** |
| `full_suite_20260924_run60b.log` | **ROW WRITTEN BEFORE THE RUN, with the literal word `pending`. SESSION 60's MID-SESSION SUITE** over `db7b45a` — the pinned split and its NSCLC run (H86 steps 1–3, 5–6), the enumeration-row fix, the print-size figures and the poster (H87, H88, H91), and the audit sweep (H89, H92) — `pytest tests/ -q --durations=10`, detached under a detached `caffeinate`, in a clean clone with `pipeline/data` symlinked, log in the scratchpad. It is NOT the closing suite: the pinned pan-cancer run (H86 step 4) is still computing beside it, and its documentation will follow, so a final suite comes after. Expectation: green at the grown, 203-test size, the self-test step about 1,004 rows — a standalone self-test of `db7b45a` took 1,619 s under this same contention, against the 2,400 s limit, so the step should land near that and the suite near 1,900–2,300 s. Launch state is recorded in the result. **RESULT: GREEN — `203 passed in 2909.40s (0:48:29)`**, zero failures, zero skips, zero warnings (PID 37784, launched 04:17:28, last written 05:05:57 CDT 2026-09-24 — 2,909 s by the file clock, so no sleep; AC, 100%; launch load 24.66 / 27.93 / 27.43, swap 6.4 of 7.2 GB, beside the pinned pan-cancer run at ~1,200% CPU). **The timing expectation was wrong and is kept as written:** the self-test step took **1,391.61 s at 1,004 rows** — faster than the standalone run's 1,619 s, and the suite 600 s over its band because every other test ran slow under the same load (the ten slowest non-self-test tests took 93–194 s each, against tens of seconds quiet). 1.39 s a row. The 2,400 s limit stands at **1.72×**, the narrowest ever recorded, and was not changed. |
| `full_suite_20260924_run60.log` | **ROW WRITTEN BEFORE THE RUN, with the literal word `pending`. SESSION 60's BASELINE and ledger H90**: `pytest tests/ -q --durations=10`, detached, under a `caffeinate -i -w` that is itself detached to PPID 1, in a CLEAN CLONE of `20e6d8b` (session 59's last commit, which carries H80–H85) with `pipeline/data` symlinked, its log written in the scratchpad OUTSIDE the clone (#55) and copied here. Intent: the FIRST full suite over H80–H85 — the HPC4 Δ-MAE hashed and bound with the manifest's EXCLUDE_NAME_RE fix, the LICENSE split and US spelling, the n_boot default, the three B-19 code fixes, the cover-letter trim, Supplementary Note 1 and the table renumbering — none of which has had one; `_run59c` at `e470fe8` predates them all. Launch state, measured at 01:04 CDT 2026-09-24: **AC, 35% and charging**, SIX MINUTES AFTER A REBOOT (boot 00:58:15 CDT); swap 0 MB; about 9.3 GiB available at a 16,384 B page; load 4.48 / 21.82 / 14.64 (load1 was 84 two minutes after boot, from indexing); `pgrep -ifl theia` empty; another project's pytest (a different repository) had just run. Expectation: green, zero failures, zero skips, zero warnings, at the test size H81 and H82 grew the suite to (three and one tests added, re-measured from the log, not carried); the self-test has gained H80's two rows over `_run59c`'s 704, so its step should sit near 530–770 s depending on the load, and the suite 800–1,100 s. A red here is a finding about H80–H85, not about this session. **RESULT: RED — `1 failed, 197 passed in 697.55s (0:11:37)`**, zero skips, zero warnings (PID 9997, launched 01:04:54, last written 01:16:32 CDT 2026-09-24 — 698 s by the file clock, so no sleep; AC, 35% and charging; `caffeinate` PID 9999 detached). **The failure is a true positive about session 59's `eeb95f7` (H83), which no suite had run over:** `test_number_checker_is_not_vacuous` — the checker's `--self-test` reported six rows NO SUCH TEXT, the cover letter's paragraph enumeration (the submission, finding, immune-signal and scope-limits paragraph counts, their sum and the letterhead scaffolding). H83's re-wrap of the letter put line breaks after "paragraphs are" and inside "in all", and those six rows' injection shapes matched literal spaces; the WRAP-scoped PATTERNS they test kept passing `--strict`, so every gate of chosen checks stayed green. Fixed in `19_check_numbers.py` (whitespace in the shapes is now `\s+`); a standalone `--self-test` in the working tree then caught all seven enumeration rows (`selftest60a`, 706 rows, 463 s; its one other miss, the snapshot file count, was an artefact of its launch moment — the pinned NSCLC run's directory existed and the documents did not yet say 311). The self-test step here took 471.49 s at 706 rows, the suite's time otherwise inside its band. **Not the green of record**: a closing suite must follow the fix. | `pytest tests/ -q --durations=10`, detached, 2026-09-24. |
| `full_suite_20260923_run59c.log` | **ROW WRITTEN BEFORE THE RUN, with the literal word `pending`. THE FINAL CLOSING SUITE OF SESSION 59**, because the author asked for two more changes after `_run59b`: the AI-use wording in the manuscript's Acknowledgments and the cover letter ("AI (Claude) was used for code editing and manuscript editing", as in the abstract) and B-34's clarification in Results, which moved the manuscript's body count and added two bound claims and two self-test rows. Method: `pytest tests/ -q --durations=10`, detached under a detached `caffeinate`, in a clean clone of `cede547` with the edited files copied in (checked byte-identical to the commit made after it) and `pipeline/data` symlinked; log outside the clone. Expectation: green, zero failures, skips and warnings, at the same test size; a self-test step of about 704 rows near `_run59b`'s 534.69 s if the foreign load is similar. Launch state, measured at 23:39 CDT 2026-09-23: **BATTERY, 84% and discharging**; swap 1.9 GB of 3.0 GB used; load 8.49 / 5.41 / 4.23 — and another project's `fit_interval_coverage.py` was running, which pushed load1 to 28 within two minutes. **RESULT: GREEN — `194 passed in 1065.00s (0:17:45)`, zero failures, zero skips, zero warnings** (PID 67441, launched 23:39:23, last written 23:57:09 CDT 2026-09-23 — 1,066 s by the file clock, so no sleep; BATTERY, 84% down to 53%; `caffeinate` PID 67443 detached). The seven edited files in the clone were compared with the working tree after the run and are byte-identical, so this run covers the commit that follows it. **THE GREEN SUITE OF RECORD FOR SESSION 59**, superseding `_run59b`. **The timing expectation was wrong and is kept as written:** the self-test step took **766.99 s at 704 rows** — 232 s over `_run59b` for two more rows — because the foreign load (load1 16–28 for the whole run) was far heavier than at `_run59b`'s launch. 1.09 s a row. The self-test is 72.0% of the suite. The 2,400 s limit stands at **3.13×**, the narrowest margin since session 53's, and was not changed. | `pytest tests/ -q --durations=10`, detached, 2026-09-23. |
| `full_suite_20260923_run59b.log` | **ROW WRITTEN BEFORE THE RUN, with the literal word `pending`. THE CLOSING SUITE OF SESSION 59**: `pytest tests/ -q --durations=10`, detached, under a `caffeinate -i -w` that is itself detached to PPID 1, in a clean clone of the session's last commit to the tree under test (H75's) with `pipeline/data` symlinked, its log written in the scratchpad OUTSIDE the clone and copied here. Intent: the suite of record for session 59, run after its last edit to anything the suite reads — because a gate of chosen tests is not the suite (#57). The self-test has grown from the baseline's 633 rows by the session's bindings (two one-unit rotation-p rows, five N-of-M rows, 13 for the audit's A16 paragraph, 18 small counts, 31 results-README counts). Launch state is recorded in the result below. Expectation: green, zero failures, zero skips, zero warnings, at the same test size as the baseline. The baseline's step took 538.45 s at 633 rows under heavy foreign load; the new rows each rewrite one document, so the step should sit near that plus about 0.8 s a row if the load is similar — **500–650 s**, and the suite **800–1,000 s**; much less if the THEIA campaign has stopped. Launch state, measured at 21:25 CDT 2026-09-23: **AC, 100% and charged**; swap 2.5 GB of 4.0 GB used; about 7.0 GiB available; load 6.13 / 9.88 / 9.05; `pgrep -ifl theia` EMPTY — the THEIA campaign had stopped. **RESULT: GREEN — `194 passed in 824.91s (0:13:44)`, zero failures, zero skips, zero warnings** (PID 11821, launched 21:25:42, last written 21:39:28 CDT 2026-09-23 — 826 s by the file clock, so no sleep; AC, 100% throughout; `caffeinate` PID 11823 detached and gone once the suite exited), on `11a53da`, after the session's last edit to the tree under test. **THE GREEN SUITE OF RECORD FOR SESSION 59.** The self-test step took **534.69 s at 702 rows**, inside the predicted band — but the machine did not stay quiet: about six minutes in (21:32), another project's `fit_interval_coverage.py --jobs 10` and a model fit at ~540% CPU started beside it, and load1 reached 46.11, so the step and the suite measure that load. 0.76 s a row. The self-test is 64.8% of the suite. The 2,400 s limit stands at **4.49×** and was not changed. | `pytest tests/ -q --durations=10`, detached, 2026-09-23. |
| `full_suite_20260923_run59.log` | **ROW WRITTEN BEFORE THE RUN, with the literal word `pending`. SESSION 59's BASELINE**, before any edit to the tree under test: `pytest tests/ -q --durations=10`, detached, under a `caffeinate -i -w` that is itself detached to PPID 1, in a CLEAN CLONE of `cd728b6` (session 58's final commit, which carries the author's decisions of `e2a936a`) with `pipeline/data` symlinked, and its log written in the scratchpad, OUTSIDE the clone (#55), then copied here. Intent: the FIRST full suite over the author's decisions — the AI sentences, cut 4a, B-30's median scoping, the BCS01 category and the seven-author byline — which session 58 launched at 18:09 and stopped by PID within a minute on an 11% battery. `_run58c` at `0f34ba8` predates those edits and is not their green. Launch state, measured at 19:10 CDT 2026-09-23: **AC, 51% and charging**; swap **5.9 GB of 7.0 GB used**; about 8.8 GiB available at a 16,384 B page; load 8.72 / 7.58 / 7.67, up 19 h 34 min — **NOT quiet**: a THEIA campaign queue (another project) is running two `replay_draws.py` workers at about 250% and 110% CPU. Expectation: green, zero failures, zero skips, zero warnings, at the same test size as `_run58c`. The self-test is smaller than `_run58c`'s 668 rows (session 58's gate over `e2a936a` counted 633, after the AI-variant tables were retired), and foreign load cost about 14% at fixed rows (#53), so the step should sit near **360–480 s**, and the suite **500–680 s**. A red here is a finding about `e2a936a`, not about this session. **RESULT: GREEN — `194 passed in 856.60s (0:14:16)`, zero failures, zero skips, zero warnings** (PID 68825, born 19:11:39, last written 19:25:56 CDT 2026-09-23 — 857 s by the file clock as by pytest's, so no sleep; AC throughout, 51% up to 62% and charging; `caffeinate` PID 68827 detached and gone once the suite exited). **The author's decisions of `e2a936a` are GREEN, measured by session 59** — the first full suite over them. **The timing expectation was wrong, and is kept as written:** the self-test step took **538.45 s at 633 rows**, 58 s above the top of the predicted band, and the suite 177 s above its band. The foreign load grew during the run rather than holding at its launch level: load1 read 29.86 three minutes in and 17.82 at the end (five-minute average 23.12), against 8.72 at launch, with swap still at 5.8 GB of 7.0 GB — so this pair is AC-and-HEAVILY-loaded, the heaviest launch state yet recorded for a suite, and it measures that load, not the row count. 0.85 s a row. The self-test is 62.9% of the suite. The 2,400 s limit stands at **4.46×** and was not changed. | `pytest tests/ -q --durations=10`, detached, 2026-09-23. |
| `full_suite_20260923_run58c.log` | **ROW WRITTEN BEFORE THE RUN, with the literal word `pending`. THE FINAL CLOSING SUITE OF SESSION 58**, because `_run58b` at `eb69e41` was followed by five more bindings (the summary blocks' BH survivors and the six-partition understatement, one new authority from the stored outcome-arm BH column) — a gate of chosen tests is not the suite (#57). Same method: `pytest tests/ -q --durations=10`, detached under a detached `caffeinate`, clean clone of `0f34ba8` with `pipeline/data` symlinked, log outside the clone. Launch state, measured at 11:39 CDT 2026-09-23: **BATTERY, 36% and discharging**; swap 0 MB; about 7.4 GiB available; load 7.04 / 5.68 / 4.99 — **NOT quiet**: another session is working in the THEIA tree (a `ticker.py` from it is running, and a pytest of its own ran a minute earlier), and `pgrep -fl theia` did NOT see it, because its path is upper-case `THEIA`; `pgrep -ifl theia` does. Expectation: green, zero failures, zero skips, zero warnings, at the same test size as `_run58b`; a self-test step at 668 rows near `_run58b`'s 410.02 s, which carried a sweep beside it, so **330–460 s** depending on the foreign load, and the suite **480–640 s**. **RESULT: GREEN — `194 passed in 560.30s (0:09:20)`, zero failures, zero skips, zero warnings** (PID 5876, born 11:39:31, last written 11:48:51 CDT 2026-09-23 — 560 s by the file clock as by pytest's, so no sleep; BATTERY, 36% down to 31%; `caffeinate` PID 5878 detached). **THE GREEN SUITE OF RECORD FOR SESSION 58**, run on `0f34ba8`, after the session's last edit to the tree under test. The self-test step took **404.60 s at 668 rows**, 5.42 s under `_run58b`'s 410.02 s at 663 rows beside a sweep: the foreign load here (load1 7.0 at launch, 1.95 three minutes in) cost about what the sweep did. 0.73 s a row over the baseline's 524. The self-test is 72.2% of the suite. The 2,400 s limit stands at **5.93×** and was not changed. | `pytest tests/ -q --durations=10`, detached, 2026-09-23. |
| `full_suite_20260923_run58b.log` | **ROW WRITTEN BEFORE THE RUN, with the literal word `pending`. THE CLOSING SUITE OF SESSION 58**: `pytest tests/ -q --durations=10`, detached, under a `caffeinate -i -w` that is itself detached to PPID 1, in a clean clone of `eb69e41` with `pipeline/data` symlinked, its log written in the scratchpad OUTSIDE the clone and copied here. One test more than the baseline (H65's dropped-cells test) and a self-test grown from 524 to 663 rows: 22 for the audit's A16 block (H64), 54 small counts (H66) and 63 checklist figures (H67). Intent: (a) the suite of record for session 58, run after its last edit to the tree under test — because a gate of chosen tests is not the suite (#57); (b) the first full run of the three new specs inside `test_number_checker_is_not_vacuous`. Launch state, measured at 11:14 CDT 2026-09-23: **BATTERY, 53% and discharging**; `pgrep -fl theia` empty; swap 0 MB; about 6.6 GiB available; load 3.69 / 3.63 / 4.27. Expectation: green, zero failures, zero skips, zero warnings. The new rows each rewrite one document, and three standalone self-tests this session took 339 s at 546 rows, 382 s at 600 (beside a sweep) and 409 s at 663, so the in-suite step should sit near the baseline's 299.78 s plus about 0.65 s a row — **350–450 s**, and the suite **500–600 s**. A step above 480 s would say the new rows cost more than one document's rescan each. **RESULT: GREEN — `194 passed in 570.49s (0:09:30)`, zero failures, zero skips, zero warnings** (PID 72677, born 11:14:50, last written 11:24:21 CDT 2026-09-23 plus the summary — the file clock agrees with pytest's, so no sleep; BATTERY, 53% down to 47%; `caffeinate` PID 72679 detached and gone once the suite exited). **THE GREEN SUITE OF RECORD FOR SESSION 58**, run on `eb69e41`. The self-test step took **410.02 s at 663 rows**, inside the predicted band — but NOT on a quiet machine: from 11:17 a mutation sweep of `16-YOUR-TASKS.md` ran beside it in another clone, one `--strict` every few seconds, so the step carries that load and is an upper figure for the row count, not a clean one. 139 rows for +110.2 s over the baseline's 299.78 s is 0.79 s a row under that load. The self-test is 71.9% of the suite. The 2,400 s limit stands at **5.85×** and was not changed. | `pytest tests/ -q --durations=10`, detached, 2026-09-23. |
| `full_suite_20260923_run58.log` | **ROW WRITTEN BEFORE THE RUN, with the literal word `pending`. SESSION 58's BASELINE**, before any edit to the tree under test: `pytest tests/ -q --durations=10`, detached, under a `caffeinate -i -w` that is itself detached to PPID 1, in a CLEAN CLONE of `f8efbaa` (session 57's final commit) with `pipeline/data` symlinked, and its log written in the scratchpad, OUTSIDE the clone (#55), then copied here. Intent: (a) session 58's own measurement of the tree it inherits, so that `_run57c`'s green is not read as this session's; (b) the 524-row self-test on the one launch state not yet measured at that size: **battery-and-quiet**. Launch state, measured at 09:57 CDT 2026-09-23: **BATTERY, 100% and discharging** (macOS estimate 10:05 remaining); `pgrep -fl theia` empty; swap 0 MB; about 8.2 GiB available at a 16,384 B page; load 2.82 / 2.01 / 2.09, up 10 h 21 min since the reboot session 57 recorded. Expectation: green, zero failures, zero skips, zero warnings, at the same test size as `_run57c`, since no file has changed since it. Battery-and-quiet cost about 4% over AC at 463 and 480 rows (H52, S57-1), so the self-test step should sit near 318 s — **280–350 s**, and the suite **440–540 s**. A step above 370 s would say more than the power covariate moved. **RESULT: GREEN — `193 passed in 452.73s (0:07:32)`, zero failures, zero skips, zero warnings** (PID 69047, born 09:57:36, last written 10:05:09 CDT 2026-09-23 — 453 s by the file clock as by pytest's, so no sleep; BATTERY throughout, 100% down to 97%; `caffeinate` PID 69049 detached and gone once the suite exited). **Session 57's `f8efbaa` is GREEN, measured by session 58.** The self-test step took **299.78 s at 524 rows** — inside the predicted band, but on the FAST side of it: 5.69 s (1.9%) UNDER `_run57c`'s AC-and-quiet 305.47 s at the same rows, although this session ran the checker, the sort-audit self-test and several scripts beside it. **The expectation was wrong in sign and is kept as written:** the ~4% battery cost measured at 463 rows (H52) and 480 rows (S57-1) did not appear at 524 rows with the battery near full. One pair is not a trend; battery's cost against AC now reads +4.2% (H52, whose AC run was also loaded), +4.3% (S57-1) and −1.9% here, and whatever separates them — charge level, thermal state, or the post-boot tail `_run57c` still carried — is not recorded. The self-test is 66.2% of the suite. The 2,400 s limit stands at **8.01×** and was not changed. | `pytest tests/ -q --durations=10`, detached, 2026-09-23. |
| `full_suite_20260923_run57c.log` | **ROW WRITTEN BEFORE THE RUN, with the literal word `pending`. THE RE-RUN OF SESSION 57's CLOSING SUITE**, after `_run57b`'s two true-positive failures and one warning were fixed: the demo authority hashed (PROVENANCE 220 → 221, its own commit), H62's seven live A16 keys added to the fast-table test's `live` set, and the docstring escape. Same method: `pytest tests/ -q --durations=10`, detached under a detached `caffeinate`, clean clone with `pipeline/data` symlinked, log outside the clone. Same machine state minutes later: AC, about 99%, no foreign compute. Expectation: green, zero failures, zero skips, **no warnings**, at the 193-test size, and a self-test step within a few seconds of `_run57b`'s 304.06 s at 524 rows, since no row changed. **RESULT: GREEN — `193 passed in 463.03s (0:07:43)`, zero failures, zero skips, zero warnings** (PID 39077, born 00:47:41, last written 00:55:24 CDT 2026-09-23 — 463 s by the file clock as by pytest's, so no sleep; AC, 99% to 100%; `caffeinate` PID 39079 detached at PPID 1 and gone once the suite exited). **THE GREEN SUITE OF RECORD FOR SESSION 57**, run on `d8b978b`. The self-test step took **305.47 s at 524 rows**, 1.41 s from `_run57b`'s 304.06 s at the same rows — the prediction held — and is 66.0% of the suite. The 2,400 s limit stands at **7.86×** and was not changed. | `pytest tests/ -q --durations=10`, detached, 2026-09-23. |
| `full_suite_20260923_run57b.log` | **ROW WRITTEN BEFORE THE RUN, with the literal word `pending`. THE CLOSING SUITE OF SESSION 57**: `pytest tests/ -q --durations=10`, detached, under a `caffeinate -i -w` that is itself detached to PPID 1, in a clean clone of `3d5b880` with `pipeline/data` symlinked, and its log written in the scratchpad, OUTSIDE the clone, then copied here. The 193-test size — two ORCID tests added this session (H60) — and a self-test grown by 44 rows: 11 for the dry run's prose re-quotations (H58), 14 for A16's extent (H62), and 19 for the deposited README's ComBat median, demo table and sort-fix figures (H63). Intent: (a) the suite of record for session 57, run after its last edit to the tree under test; (b) proof that the 44 new rows and the rewritten coverage bookkeeping (H59) survive a full run, not only a standalone `--self-test` — the last standalone one covered the first 25 of them and passed; (c) the first suite to run the new live A16 authority inside `test_number_checker_is_not_vacuous`. Launch state, measured at 00:35 CDT 2026-09-23: **AC, 99% and finishing its charge**; `pgrep -fl theia` empty; swap 0 MB; about 9.7 GiB available; load1 4.20. Expectation: green, zero failures, zero skips. The rows are rescan-only (H40) and each new one rewrites a single document, so the self-test step should cost near the baseline's 276.16 s plus the new authority's ~2 s per full authority build — **260–330 s**, and the suite **420–520 s**. A step above 360 s would say the new rows cost far more than one document's rescan each, which would be a finding about the new rows. **RESULT: RED, KEPT — `2 failed, 191 passed, 1 warning in 461.94s (0:07:41)`** (PID 37124, born 00:36:01, last written 00:43:19 CDT 2026-09-23 plus the summary, so no sleep; AC throughout, 99%; `caffeinate` detached at PPID 1 and gone once the suite exited). **Both failures and the warning are this session's own, and all three are true positives from guards doing their job.** (1) `test_every_file_the_checker_reads_as_an_authority_is_hashed` refused `demo/immune_excess.csv`: H63 made it an authority and nothing hashed it, which is exactly the F3.4 rule — fixed by the tool's own documented remedy, adding it to `21_provenance_manifest.py`'s `TRACKED_FILES` and `--write`, in its own commit: 220 → 221, zero removed, zero changed. (2) `test_a_claim_whose_authority_vanished_is_reported_not_skipped` builds the FAST table and expects exactly the live-measured keys to be missing; H62's seven A16 keys are live-measured in the slow path beside the site count, so they belong in that test's `live` set — added, with the reason. (3) The warning was a `SyntaxWarning: invalid escape sequence '\d'` in H62's new docstring, a future SyntaxError in a published script; escaped, and every file this session touched was compiled with warnings as errors. The standalone self-tests had passed because neither of those two tests is the self-test, and the targeted gates had not selected them — a gate of chosen tests is not the suite. **The self-test step took 304.06 s at 524 rows**, inside the predicted 260–330 s: 44 rows for +27.9 s over the baseline's 276.16 s at 480, about 0.63 s each — one document's rescan apiece, as H40 says. The 2,400 s limit stands at 7.89×. `_run57c` is the re-run. | `pytest tests/ -q --durations=10`, detached, 2026-09-23. |
| `full_suite_20260922_run57.log` | **ROW WRITTEN BEFORE THE RUN, with the literal word `pending`. SESSION 57's BASELINE**, before any edit to the tree under test: `pytest tests/ -q --durations=10`, detached, under a `caffeinate -i -w` that is itself detached to PPID 1, in a CLEAN CLONE of `f1ce857` (session 56's final commit) with `pipeline/data` symlinked, and **its log written in the scratchpad, OUTSIDE the clone** (#55), then copied here. Intent: (a) **the first suite ever run against session 56's `e9169c8`** — session 56 could not run one on a 13% battery and said so in its G1, so its commit has been carried untested; and (b) the one launch state at the **480-row** self-test size this project has not yet measured: **AC power and no foreign compute** (`_run55b` was battery-and-quiet at 480 rows; `_run54b` was AC-and-loaded at 463). Launch state, measured at 23:43 CDT: **AC, 46% and charging**; `pgrep -fl theia` empty; swap **0 MB**; about **9.9 GiB** available at a 16,384 B page, the most of any macOS-27 launch; **but the laptop REBOOTED at 23:36, seven minutes earlier**, and load1 was 5.65 with the five-minute average at 18.49 — post-boot work (Safari, a PDF service, WindowServer), not this project. Expectation: green, zero failures, zero skips, at the 191-test size. If post-boot load has settled, AC-and-quiet should put the self-test step at or below `_run55b`'s 288.11 s — so **240–300 s**, and the suite **400–480 s**; a step above 320 s would mean the post-boot load had not settled, and would be a statement about launch state, not about the step. **RESULT: GREEN — `191 passed in 443.03s (0:07:23)`, zero failures, zero skips** (PID 4340, born 23:43:47, last written 23:51:10 CDT 2026-09-22 — 443 s by the file clock as by pytest's, so no sleep inside it; AC throughout, battery 46% up to 60%; `caffeinate` PID 4342 detached at PPID 1 and gone once the suite exited). **Session 56's `e9169c8` is GREEN**, measured now rather than inherited. The self-test step took **276.16 s at 480 rows**, inside the predicted band and 11.95 s under `_run55b`'s battery-and-quiet 288.11 s at the same row count — so, at 480 rows, AC-and-quiet is the fastest launch state yet recorded, and the power covariate is about 4% here as it was at 463 rows (H52). Not quite an idle machine: while the suite ran, this session regenerated `ENVIRONMENT.md` in the working tree and ran several short greps and AST walks, so the step carries a few seconds of this session's own load on top of the post-boot tail (load1 fell from 5.65 at launch to 3.46 just after). The suite as a whole took 443.03 s against `_run55b`'s 448.91 s, and the self-test is 62.3% of it. The 2,400 s limit stands at **8.69×** and was not changed. | `pytest tests/ -q --durations=10`, detached, 2026-09-22. |
| `full_suite_20260922_run55b.log` | **ROW WRITTEN BEFORE THE RUN, with the literal word `pending`. THE CLOSING SUITE OF SESSION 55**: `pytest tests/ -q --durations=10`, detached, under a `caffeinate -i -w` that is itself detached to PPID 1, in a clean clone of `918fb3a` with `pipeline/data` symlinked. The **191-test** size, self-test at **480 rows**, up from 463 at the baseline. **Its log is written OUTSIDE the clone**, which is the one thing this session's baseline did wrong: `_run55` put its log inside the tree it was auditing and the artefact-classification guard correctly refused a file the committed map could not mention. Intent: (a) the suite of record for session 55, and (b) proof that the seventeen rows added this session for the cover letter's science and its paragraph enumeration survive a full run, not just a standalone `--self-test`. Launch state, measured: **BATTERY and falling — about 25%**, no foreign compute (`pgrep -f theia` empty), load1 near 4. Expectation: green, zero failures, zero skips, and a self-test step near **300 s** — the baseline measured 288.22 s at 463 rows on this same machine state, and 17 more rows at that rate is about 11 s more — so **270–360 s** and the suite **450–700 s**. A step above 400 s would mean the added rows cost far more than the rows already there, which would be a finding about the new rows rather than about the loop. **RESULT: GREEN — `191 passed in 448.91s (0:07:28)`, zero failures, zero skips** (PID 36736, born 21:51:52, last written 21:59:21 CDT 2026-09-22, so no sleep inside it; BATTERY 25% down to 19%; `caffeinate` detached at PPID 1, verified by assertion rather than assumed, and gone once the suite exited). **THE GREEN SUITE OF RECORD FOR SESSION 55**, run on `918fb3a`. **The seventeen rows added this session cost essentially nothing: the self-test step took 288.11 s at 480 rows against the baseline's 288.22 s at 463** — 17 more rows for −0.11 s, which is inside the noise. That is the rescan-only-what-changed loop (H40) behaving exactly as its equivalence proof said it would: each new row rewrites ONE document, so it costs one document's rescan rather than the whole tree's. The suite as a whole took 448.91 s against the baseline's 466.69 s, and the self-test is 64.2% of it. The 2,400 s limit stands at **8.33×** and was not changed. Against session 54's closing suite (481.10 s, self-test 276.68 s at 463 rows on AC beside a foreign job at about 395% CPU), this run was quieter, on battery, and slightly faster overall. | `pytest tests/ -q --durations=10`, detached, 2026-09-22. |
| `full_suite_20260922_run55.log` | **ROW WRITTEN BEFORE THE RUN, with the literal word `pending`. SESSION 55's BASELINE**, before any edit to the tree under test: `pytest tests/ -q --durations=10`, detached, with a `caffeinate -i -w` that is itself detached (PPID 1) so it outlives this shell session, in a CLEAN CLONE of `f04367d` (session 54's final commit, the tag `session-54-2026-09-21`) with `pipeline/data` symlinked. The **191-test** size, self-test at **463 rows**, on macOS 27.0 / arm64. Intent: (a) establish that the inherited commit is green before this session edits anything, and (b) a covariate measurement this project has never made. Every previous run at the **463-row** self-test size ran on AC; the only figure at that size is `_run54b`'s **276.68 s**, taken on AC beside a foreign THEIA job at about 395% CPU. This launch inverts both covariates at once and they push opposite ways: the machine is QUIET (`pgrep -f theia` empty — the foreign job that ran through sessions 53 and 54 is gone; load1 3.41), but it is on **BATTERY, 46% and discharging, 2:47 estimated remaining**, which no run at this size has been. Also recorded rather than assumed: swap in use **2,124.81 MB** (session 54's baseline had none) and about **5.2 GiB** available at a 16,384 B page, the tightest launch memory of any macOS-27 run. Expectation: green, zero failures, zero skips. If power is not a material covariate the quieter machine should put the self-test step at or below `_run54b`'s 276.68 s, so **200–320 s** and the suite **400–650 s**; a step materially ABOVE 320 s on a machine with no foreign compute would mean battery throttling costs this project more than foreign load does, which would be a finding about the COVARIATE axis rather than about the step. **RESULT: RED, KEPT, AND THE RED IS THIS RUN'S OWN DOING — `1 failed, 190 passed in 466.69s (0:07:46)`** (PID 6350, born 21:27:27, last written 21:35:14 CDT 2026-09-22, so no sleep inside it; BATTERY throughout, 46% down to 35%). The single failure is `test_results_readme_classifies_every_artefact_that_exists`, and it names exactly one file: **this run's own log**. `detach.py` was pointed at `results/` INSIDE the clone, so the suite created an artefact in the tree it was auditing, and the map it audited against is the one committed at `f04367d`, which cannot mention a file that did not exist when that commit was made. **Diagnosed by measurement, not by argument:** copying this working tree's `results/README.md` — the file carrying this very row, and nothing else — into the clone makes that test pass in **1.20 s**, with no other artefact unclassified. **So the inherited commit `f04367d` is GREEN, and the UNCLASSIFIED-MEMBER guard (#45) fired correctly on a file the harness itself planted.** The lesson is one the project already holds in another form — *a file count taken while something writes into the tree is contaminated* — and the fix for successors is one word of path: **write a detached suite's log OUTSIDE the tree under test.** The covariate this run was launched to measure is answered: the self-test step took **288.22 s at 463 rows** on a quiet machine on battery, against `_run54b`'s **276.68 s** at the same row count on AC beside a foreign job at about 395% CPU — **+4.2%**, inside the predicted 200–320 s band — and the suite 466.69 s against 481.10 s. **Battery is not a material covariate for this project at this size**: a quiet machine on battery is worth about as much as a loaded one on AC, and the two effects very nearly cancel. The 2,400 s limit stands at **8.33×** and was not changed. | `pytest tests/ -q --durations=10`, detached, 2026-09-22. |
| `full_suite_20260921_run54b.log` | **ROW WRITTEN BEFORE THE RUN, with the literal word `pending`. THE CLOSING SUITE OF SESSION 54**, `pytest tests/ -q --durations=10`, detached, with a `caffeinate -i -w` that is itself detached (PPID 1), in a clean clone of `47bec21` with `pipeline/data` symlinked. The **191-test** size (two added this session: the finished-but-unfilled row check and its can-fail), and the self-test at **463 rows**, up from 343 at the baseline. Intent: the suite of record for session 54, and the first suite to run the self-test in its new form, which rescans only the documents each row changed. Launch state, measured: **AC power, battery 100%**; load1 11.0, because a foreign THEIA job (`theia.engine.train`, ~395% CPU, started during the session) is running — the COVARIATE this project records rather than assumes. Expectation: green, 191 passed, zero skips. The self-test step measured 4 min 36 s (276 s) at 463 rows in this session's gate under lighter load, against 1,190.83 s at 343 rows in the old form at the baseline; so the step should land near **250–450 s** and the suite, whose tests other than the self-test took about 160 s together at the baseline, near **450–700 s** — roughly a third of the baseline's 1,350.96 s despite 120 more self-test rows. A step above 1,000 s would mean the rescan-only-what-changed loop is not doing what the equivalence run says it does. **RESULT: pending.** **RESULT: GREEN — `191 passed in 481.10s (0:08:01)`, zero failures, zero skips** (PID 46516, 02:13:36–02:21:37 CDT 2026-09-21, AC 100%, the foreign THEIA job running throughout, `caffeinate` detached at PPID 1). **THE GREEN SUITE OF RECORD FOR SESSION 54**, run on `dd9c289`, which is `47bec21` plus this row. The self-test step took **276.68 s at 463 rows** — inside the predicted band, **2,123.3 s under the 2,400 s limit, 8.67×** — against 1,190.83 s at 343 rows in the old form at this session's baseline. The suite as a whole took 481.10 s against the baseline's 1,350.96 s, **2.8× faster with 120 more self-test rows and two more tests**, and the self-test is now 57.5% of it rather than 88%. The limit stays: nothing measured argues for changing it, and a lower one would be a new decision. | `pytest tests/ -q --durations=10`, detached, 2026-09-21. |
| `full_suite_20260921_run54.log` | **ROW WRITTEN BEFORE THE RUN, with the literal word `pending`. SESSION 54's BASELINE**, before any edit: `pytest tests/ -q --durations=10`, detached, with a `caffeinate -i -w` that is itself detached (PPID 1) so it outlives this shell session, in a CLEAN CLONE of `9da6137` (session 53's final commit, the tag `session-53-2026-09-20`) with `pipeline/data` symlinked. The **189-test** size, self-test at **343 rows**, on macOS 27.0 / arm64. Intent: establish that the inherited commit is green before this session edits anything. Launch state, measured rather than assumed: **AC power, battery 49% and charging**; the laptop REBOOTED at 23:49:53 CDT 2026-09-20 (the app was quit mid-session), so it is 20 minutes up; load1 3.3; `pgrep -fl python` shows only small resident services and `pgrep -fl theia` is empty — no foreign compute, the quietest launch state of any macOS-27 run. Expectation: green, 189 passed, zero skips; with no foreign load the self-test step should sit at or below `_run53d`'s **1,318.64 s** (the quietest macOS-27 figure at 343 rows), so **1,150–1,400 s**, and the suite **1,350–1,600 s**. A step above 1,500 s on a quiet machine would be a finding about the step, not the load. **RESULT: pending.** **RESULT: GREEN — `189 passed in 1350.96s (0:22:30)`, zero failures, zero skips** (PID 5225, 00:11:40–00:34 CDT 2026-09-21, AC, `caffeinate` detached at PPID 1 and holding). **The inherited commit `9da6137` is green on macOS 27.** The self-test step took **1,190.83 s at 343 rows, 1,209.17 s under the 2,400 s limit, 2.02×** — inside the predicted band and 127.8 s (9.7%) faster than `_run53d`'s 1,318.64 s at the same row count and OS, the quietest figure until now. One covariate to record rather than hide: from about 00:21 this session's own mutation sweep and checker runs shared the machine (single-process, ~1.7 cores of 14, for roughly ten minutes), so this is a quiet-machine figure with a light, self-inflicted load on top — which makes it, if anything, an upper bound on the quiet step. | `pytest tests/ -q --durations=10`, detached, 2026-09-21. |
| `full_suite_20260920_run53c.log` | **ROW WRITTEN BEFORE THE RUN, with the literal word `pending`. THE FINAL CLOSING SUITE OF SESSION 53, superseding `_run53b`, which is now a mid-session checkpoint rather than the suite of record** — session 53 continued after it, closing the last two BLOCKED items and the agent half of three BLOCKED-AUTHOR ones (H25–H30). `pytest tests/ -q --durations=10`, detached under `caffeinate -i -w`, in a CLEAN CLONE of **`b79e7c7`** with `pipeline/data` symlinked. **This row was first written against `68becd3` and that run was deliberately killed three minutes in**, because the session then made further edits (H31) and a closing suite that predates the last edit is not a closing suite; the partial log was deleted rather than kept, since it recorded nothing a reader could use. Still the **189-test** size — no test was added — while the self-test grew **335 → 343 rows** (six for the deposited README's results table, two for the cover letter's AACR-required manuscript counts). Intent: the suite of record for session 53's final commit, and the first suite to cover the folded Limitation 8, the cut cover letter, the changed RRID, the bound README table and the new `--poster` flag. Expectation: green, 189 passed, zero skips. `_run53b` measured 1,338.74 s total with a 1,170.17 s self-test step at 335 rows on a quiet machine; eight more rows at the 5.4–12.1 s/row range this session bracketed predicts **+43 to +97 s**, so **1,215–1,270 s for the step and 1,380–1,440 s for the suite** — provided the machine stays quiet, which is the covariate that broke last time and must be recorded, not assumed. Launch state, recorded rather than assumed: **AC at 100%, no foreign compute (`pgrep theia` empty), load1 9.7** — higher than `_run53b`'s 5.6 at launch, but that is this session's own gate work draining, not a second job. A result outside the band is a finding about the machine, not about the rows. **RESULT: GREEN but CONTAMINATED, and superseded by `_run53d`. 189 passed in 2430.46s (0:40:30), zero failures, zero skips** — but the self-test step reads **2,253.39 s against its 2,400 s limit, a margin of 146.6 s (1.065×)**, which contradicts every other measurement (2.5× at 314 rows, 2.1× at 321, 2.05× at 335) and **must not be quoted as the guard's margin.** Two covariates broke under it, both established from the machine rather than inferred. (1) **The laptop slept for about 4.9 hours mid-run.** The log was born 11:52:18 and last written 17:27:13 — 5 h 35 m of wall clock for a run pytest measured at 2,430 s — because `caffeinate -i -w <pid>` was held by the *shell session* that launched it, and that session ended. A NEW form of the idle-sleep failure: the holder must outlive the session, not just the command. (2) **The machine was upgraded from Darwin 25.5.0 to 27.0.0 and rebooted at 17:49**, so this run executed on the OLD OS while competing with upgrade work, and nothing in this tree had then been tested on the current one. Kept, not deleted, because the lesson is worth more than the timing: a duration measured across a sleeping machine is not a duration, and a limit that looks nearly exhausted should be re-measured before it is believed. | `pytest tests/ -q --durations=10`, detached, 2026-09-20. |
| `full_suite_20260920_run53e.log` | **ROW WRITTEN BEFORE THE RUN, with the literal word `pending`. THE CLOSING SUITE OF RECORD FOR SESSION 53**, re-run after `_run53d` went RED and both of its failures were fixed. `pytest tests/ -q --durations=10`, detached under a fully detached `caffeinate` (both at PPID 1), in a clean clone of `7d09a9f` with `pipeline/data` symlinked; the **189-test** size, self-test at **343 rows**, on macOS 27.0 / arm64. Intent: prove the tree is green on the new OS after the two fixes — the results row rewritten into the hyphenated convention, and `ENVIRONMENT.md` regenerated and renamed so its title stops claiming to describe the machine that produced the frozen results. Both fixed tests were re-run individually and pass. Expectation: green, 189 passed, zero skips, and a self-test step near `_run53d`'s **1,318.64 s** — the machine was believed quieter at launch — **and that belief was WRONG, corrected here before the result landed.** The pre-launch check ran `pgrep -f theia.analysis` and reported none, so the row first said the foreign job had finished. Four minutes in, load1 hit 32.5 and there were **TWO** foreign THEIA processes taking ~5.2 of 14 cores: `theia.engine.train` (18 min old at that point) and `theia.analysis.lesion_run`. The first does not match `theia.analysis`, so the grep answered a narrower question than the one being asked and its negative was believed. **A negative from a too-narrow pattern is not a negative** — the same class as this project's vacuity defects, in a one-line shell check. The honest expectation is therefore a CONTAMINATED and slower run: the step likely well above `_run53d`'s 1,318.64 s, and the suite above 1,550 s. What this run can still establish, which is its actual purpose, is GREEN on macOS 27 after the two `_run53d` fixes. Anything near 2,250 s would mean the sleep diagnosis was wrong and the 2,400 s limit is genuinely tight. **RESULT: GREEN — 189 passed in 1900.76s (0:31:40), zero failures, zero skips** (PID 44241, 22:08–22:40 CDT 2026-09-20, AC, macOS 27.0 / arm64, load1 8.5–32.5, two foreign THEIA jobs taking ~5.2 of 14 cores, `caffeinate` detached at PPID 1 and holding). **THE GREEN SUITE OF RECORD FOR SESSION 53.** The self-test step took **1,496.20 s at 343 rows**, leaving **903.8 s against the 2,400 s limit, 1.60×** — under the heaviest foreign load this project has measured on a run that was not contaminated, and nowhere near 2,250 s. Together with `_run53d`'s 1,318.64 s (1.82×) on the same OS and row count with only Spotlight competing, that is **+177.6 s, +13.5%, for ~5 cores of foreign work** — which replicates session 53's baseline finding of +14% at load1 8–14 almost exactly, and confirms from a second direction that foreign load costs this step a modest fraction rather than doubling it. **The 2,400 s limit is adequate and stays**, now on two independent measurements at the largest row count this project has run, on the new OS, one of them adverse. `_run53c`'s 2,253.39 s is fully accounted for as sleep and should never be quoted. | `pytest tests/ -q --durations=10`, detached, 2026-09-20. |
| `full_suite_20260920_run53d.log` | **ROW WRITTEN BEFORE THE RUN, with the literal word `pending`. THE CLOSING SUITE OF RECORD FOR SESSION 53, and the FIRST RUN OF THIS PROJECT ON macOS 27.** `pytest tests/ -q --durations=10`, detached under a `caffeinate` that is itself fully detached (PPID 1) rather than held by this shell — the fix for what contaminated `_run53c`. Clean clone of the session's final commit with `pipeline/data` symlinked; the **189-test** size, self-test at **343 rows**. Intent: (a) a trustworthy closing suite, and (b) the platform question the upgrade opened. The frozen results were computed on macOS 15 / arm64 and this project's central finding is that a platform difference of 0.0218 came from a non-stable sort, so an OS major-version change is not cosmetic here. Already measured before launch, and all three held: provenance **220/220** — every one of the 220 frozen artefacts hashes identically on the new OS — `--strict` 954 claims exit 0, and the stable-sort partition still hashes to `9f31e33c2caaf1e3`. Expectation: green, 189 passed, zero skips, and a self-test step back in the **1,200–1,400 s** range (343 rows at the 5.4–12.1 s/row this session bracketed, from `_run53b`'s 1,170.17 s at 335 on a quiet machine), restoring a ~1.8× margin. **If it instead lands near 2,250 s again, the margin finding is real and the 2,400 s limit must be raised** — that is the question this run answers. **RESULT: pending.** *[Filled 2026-09-21 by session 54, from the log, because session 53 never wrote it back — the row read `pending` for a run that had finished RED, in the one file whose job is to say what each log is.]* **RESULT: RED — `2 failed, 187 passed in 1508.40s (0:25:08)`**, log born 21:36:29 and last written 22:01:37 CDT, so no sleep inside it. Both failures are TRUE POSITIVES and both were fixed before `_run53e`: `test_historical_run_rows_state_no_live_test_count` (a row this session wrote in the forbidden live-count form) and `test_environment_md_matches_a_fresh_render_on_this_platform` (the OS upgrade reaching `ENVIRONMENT.md`). The margin question it was launched to answer is answered: the self-test step took **1,318.64 s at 343 rows, 1,081.36 s under the 2,400 s limit, 1.82×**, so the sleep diagnosis of `_run53c` holds and the limit was NOT raised. *[Corrected 2026-09-21: "the frozen results were computed on macOS 15 / arm64" above is wrong — by the laptop's own install record they were computed on macOS 26.5.2 (Darwin 25.5.0) / arm64; see 14-SCIENCE-AUDIT.md, session 54.]* | `pytest tests/ -q --durations=10`, detached, 2026-09-20. |
| `full_suite_20260920_run53b.log` | **ROW WRITTEN BEFORE THE RUN, with the literal word `pending`. A MID-SESSION CHECKPOINT OF SESSION 53 (superseded by `_run53c`; it was the closing suite when written, and the session then continued), run after the last edit to anything the suite reads.** `pytest tests/ -q --durations=10`, detached under `caffeinate -i -w`, in a CLEAN CLONE of the commit that carries this row, with `pipeline/data` symlinked to this tree's. Written at the **189-test** size; no test was added this session, so the size is unchanged and the self-test grew from **321 rows to 335** (three for the manuscript abstract's body, Significance and their sum; six for the poster's five outcome excesses and its multiplicity margin; five for the manuscript's copy of those same five). Intent: the suite of record for session 53's commit `7170cd0`. Machine state was INTENDED to match this session's baseline in every recorded covariate — AC at 100% and the same foreign THEIA job running — so that unlike session 52's pair this one would vary ONLY the row count. **CORRECTED BEFORE THE RESULT LANDED, and the correction is the point: the THEIA job finished between the two runs.** `pgrep -f theia.analysis` returned nothing at launch, against 330–354% CPU throughout the baseline, and load1 had fallen to 6.9 before this suite's own work raised it. So this pair varies **row count AND foreign load, in opposite directions** — 14 more rows should make it slower, a freed ~3.5 cores faster — and it therefore CANNOT isolate the per-row cost. Written down rather than quietly re-used, because an expectation built on a covariate that has already changed is how F2.2 got stuck in the first place. Expectation: green, 189 passed, zero skips. The baseline measured 1,142.03 s for the self-test step at 321 rows on this machine, and session 52 measured **5.39 s per row** on a quiet one; 14 more rows at that rate predicts **+75 s → about 1,217 s**, and the whole suite about **1,480–1,600 s** against the baseline's 1,419.31 s — but that was computed while THEIA was still running. With the foreign job gone the two effects oppose, and the honest prediction is a WIDE band: somewhere between session 52's quiet-machine 1,001 s scaled up for 14 rows (~1,076 s) and the loaded estimate (~1,217 s), so **1,050–1,250 s for the self-test step and 1,300–1,550 s for the suite**. A result inside that band confirms nothing about per-row cost and should not be reported as if it did; what it can still do is confirm the commit is green and put a third point on the load/row surface. Either way the step stays far inside the 2,400 s limit. **RESULT: GREEN — 189 passed in 1338.74s (0:22:18), zero failures, zero skips** (PID 72887, 09:08–09:31 CDT 2026-09-20, AC at 100%, load1 5.6–8.5, no foreign compute, `caffeinate -i -w` held). Inside the corrected band on both figures. **The self-test step took 1,170.17 s at the 335-row size.** THE GREEN SUITE OF RECORD for session 53's commit `7170cd0`. **What this does and does not measure, stated plainly because the confound was predicted before the run:** against the baseline it is +28.14 s for +14 rows AND one freed multicore job — two effects in opposite directions, so it yields no per-row cost. Worse, the three available pairings disagree about what that cost even is: baseline→closing implies ~5.4 s/row only if the freed THEIA job is assumed to have saved ~47 s, while session 52's quiet 1,001.21 s at 321 rows against this run's 1,170.17 s at 335 implies **12.07 s/row** — and neither pairing holds load constant, since session 52 sat at load1 3.2–6.0 and this run at 5.6–8.5. **So session 52's "5.39 s per row" should be read as one estimate from one confounded pair, not a constant**, and a successor predicting a self-test duration from a row count should expect to be wrong by a factor of two. The margin is what actually matters and it is unambiguous: **1,229.83 s spare against the 2,400 s limit, 2.05×**, at the largest row count this project has ever run. The limit stays. Isolating the per-row cost needs a pair run minutes apart on the same load with only rows changed, which no session has yet managed — the open form of F2.2. | `pytest tests/ -q --durations=10`, detached, 2026-09-20. |
| `full_suite_20260920_run53.log` | **ROW WRITTEN BEFORE THE RUN, with the literal word `pending`. SESSION 53's BASELINE (ledger L0.6), taken at the inherited commit `9e1d34e` before any edit.** `pytest tests/ -q --durations=10`, detached under `caffeinate -i -w`, in a CLEAN CLONE of `9e1d34e` with `pipeline/data` symlinked to this tree's. Intent: establish that the inherited commit is green on this machine, and — deliberately — measure the self-test step at the **321-row** size under **foreign load**, which is the one covariate session 52 named as the threat to its 2,400 s subprocess limit but could not vary (session 52 ran on the first machine without foreign compute since session 47). The foreign compute is back: a `theia.analysis.lesion_run` process at ~354% CPU, load1 8.81 at launch, on AC at 100%. Expectation: green, 189 passed, zero skips, and SLOWER than session 52's quiet-machine pair (baseline 1,103.19 s, closing 1,168.37 s) because roughly 3.5 of 14 cores are taken. Session 51 measured the self-test at ~1,920 s at load1 10–51, and session 52 at 1,001.21 s at load1 3–6 for the same 321 rows; this run sits between those loads, so the self-test step is predicted at **1,200–1,900 s** and the whole suite at **1,400–2,200 s**. The margin question is the point: if the self-test step lands above ~1,900 s the 2,400 s limit is genuinely at risk under ordinary foreign load and the limit should be raised; if it lands well below, session 52's reading that the limit is adequate survives a second, adverse covariate. **RESULT: GREEN — 189 passed in 1419.31s (0:23:39), zero failures, zero skips** (PID 48460, 08:21–08:45 CDT 2026-09-20, AC at 100% throughout, load1 8.8 → 14.0, `caffeinate -i -w` held across it). Inside the predicted band, at the bottom of it. **The self-test step took 1,142.03 s at the 321-row size, against 1,001.21 s at the SAME 321 rows on session 52's quiet machine** — the first time this project has varied load with the row count held fixed. Foreign compute costing roughly 3.5 of 14 cores added **140.8 s, +14%**, not the doubling session 52 inferred. That inference came from session 51's ~1,920 s, measured at load1 10–51; this run sat at load1 8–14. So the cost is **not linear in load1**: ordinary foreign load (one multicore job) is cheap, and only the extreme end session 51 hit is expensive. Margin against the 2,400 s limit is **1,258 s, 2.1×**, under an adverse covariate — the limit is adequate and stays unchanged, now on three measurements spanning three load regimes. THE BASELINE OF RECORD for session 53; the inherited commit `9e1d34e` is green. | `pytest tests/ -q --durations=10`, detached, 2026-09-20. |
| `full_suite_20260920_run52b.log` | **ROW WRITTEN BEFORE THE RUN, with the literal word `pending`. THE CLOSING SUITE OF SESSION 52 (ledger G1), run after the last edit to anything the suite reads.** | `pytest tests/ -q --durations=10`, detached under `caffeinate`, in a CLEAN CLONE of the commit that carries this row, with `pipeline/data` symlinked to this tree's. Written at the **189-test** size; no test was added this session, so the size is unchanged and the self-test grew from 314 rows to **321** (three for the cover letter's length, paragraph 4 and the subtraction between them; four for B-30's two scoping options). Intent: the suite of record for session 52's commits. Machine state differs from this session's baseline in one recorded covariate — the laptop was **unplugged for the baseline and is on AC, charging, for this run** — and load1 is 6–11 against 2–4, from this session's own gate work rather than any foreign job. Expectation: green, all 189 passed, zero skips, and somewhat slower than the baseline's 1,103.19 s because the self-test carries seven more rows and the machine is busier: 1,100–1,600 s, with the self-test step still far inside its 2,400 s limit. **RESULT: GREEN — 189 passed in 1168.37s (0:19:28), zero failures, zero skips; PID 22707, 2026-09-20 00:45–01:04 CDT, on AC (charging, 81% → 96%), load1 3.2–6.0, `caffeinate -i -w` held across it.** Inside the predicted band, and the expectation was right this time because it was built from THIS session's own baseline rather than from a suite time of a different vintage. The self-test step took **1,001.21 s at the 321-row size**, against 963.46 s at 314 rows in the baseline: **+37.75 s for 7 rows, about 5.4 s per row**, which is the first per-row cost this project has measured and makes the row-count scaling of S52-8 quantitative rather than directional. Margin against the 2,400 s limit is **1,399 s, 2.4× headroom** — essentially unchanged, and on a quiet machine the limit has room for roughly another 260 rows before it binds. THE GREEN SUITE OF RECORD for session 52. |
| `full_suite_20260920_run52.log` | **ROW WRITTEN BEFORE THE RUN, with the literal word `pending`. SESSION 52's BASELINE (ledger L0.6), taken at the inherited commit before this session's first edit to anything the suite reads.** | `pytest tests/ -q --durations=10`, detached under `caffeinate`, in a CLEAN CLONE of the inherited commit with `pipeline/data` symlinked to this tree's (session 51 measured what happens without it: one failure and three skips for that reason alone). Written at the **189-test** size. macOS, **on BATTERY at 100%**, about 13 h remaining — the first suite of this project measured on a machine with no foreign compute since session 47: the THEIA and wildfire_ai groups that held load1 at 10–51 through sessions 49–51 are gone, and load1 at launch is about 3. Intent: establish whether the inherited commit is still green, and measure the self-test step's margin against its 40-minute limit on a QUIET machine, which no session has yet done at the 314-row size. Expectation: green, all 189 passed, zero skips, and much faster than session 51's 2,276.96 s — session 49 measured ~280 s at the 174-test size on a quiet machine, and session 48 measured battery runs at about 1.79× AC, so 400–900 s, with the self-test step well under its limit. **RESULT: GREEN — 189 passed in 1103.19s (0:18:23), zero failures, zero skips; PID 94670, 2026-09-20 00:05–00:24 CDT, on BATTERY throughout (100% → 88%, discharging, ~8.5 h left at the end), load1 2.3–4.4 for most of the run with no foreign compute, `caffeinate -i -w` held across it.** **THE EXPECTATION WAS WRONG AND THE RUN IS THE FINDING.** 1,103 s is outside the predicted 400–900 s band, and the reason is not the machine: `test_number_checker_is_not_vacuous` alone took **963.46 s, 87.3% of the whole suite**. The band was anchored on session 49's ~280 s at the 174-test size, when the self-test carried about 131 rows; it now carries 314. What scales is the self-test's ROW COUNT, not the suite's test count, and any prediction made from a total suite time of a different vintage will keep being wrong. **The margin question this row was written to answer is now answered: 963 s against the test's 2,400 s subprocess limit — 1,437 s spare, 2.5× headroom.** Session 51 measured about 1,920 s of that limit at load1 10–51 and recorded the ~8-minute margin as thin. Both readings are right, and together they say the limit is not too small for 314 rows — **foreign load, not row count, is what threatens it**, roughly doubling the step. The limit is left unchanged, now on two measurements instead of one. THE GREEN SUITE OF RECORD for the inherited commit, and the first suite this project has measured with no foreign compute on the machine since session 47. |
| `full_suite_20260918_run51.log` | **ROW WRITTEN BEFORE THE RUN, with the literal word `pending`. THE CLOSING SUITE OF SESSION 51 (ledger G1), run after the last edit to anything the suite reads.** | `pytest tests/ -q --durations=10`, detached, in a CLEAN CLONE of the commit that carries this row (session 51's second commit), with `pipeline/data` symlinked to this tree's read-only (without it the checker's live site count has no authority and one test fails, which is what session 51's baseline measured), copied here afterwards. macOS on AC power, `caffeinate` holding the machine awake. Written at the **189-test** size. Two foreign jobs were running at launch (a THEIA run and several wildfire_ai processes, load1 about 10-37 through the session), recorded rather than stopped. Expectation: green, all 189 passed, and slow: session 51's baseline took 1,735.84 s at this load, so 1,500-2,500 s. **RESULT: GREEN — 189 passed in 2276.96s (0:37:56), zero failures, zero skips; PID 75448, 2026-09-18 00:12–00:50 CDT, AC throughout (battery 5–10%, not rising under load), load1 7–51, available memory 2.9–7.6 GiB, swap 11.4–14.9 GB used (77 samples, kept in the session scratchpad).** Inside the expected band. The self-test step (`test_number_checker_is_not_vacuous`, now at the 314-row size) took about 32 minutes of its 40-minute subprocess limit at this load, against 23 minutes for 280 rows in session 51's first commit gate: the margin is about eight minutes, and a heavier foreign load could breach it, as run50's did at the old 900 s limit. Recorded, not changed: raising the limit again would need its own suite run. |
| `full_suite_20260917_run50b.log` | **ROW WRITTEN BEFORE THE RUN, with the literal word `pending`. THE CLOSING SUITE OF SESSION 50 (ledger G1), re-run after run50's timeout.** | `pytest tests/ -q --durations=10`, detached, in this working tree, macOS, with `caffeinate` holding the machine awake for the run. Written at the **187-test** size. The only change since run50 is the self-test test's subprocess limit (900 s → 2400 s) and run50's own row. The foreign THEIA job was still running at launch (load1 ~27–40), and is recorded rather than stopped: it is not this project's. Expectation: green, 187 passed, and slow — about the 1,620 s of run50 or more. **RESULT: RED — 1 failed, 186 passed in 1340.46 s (0:22:20); PID 8895, 21:49–22:12 CDT, on battery (73% → 26%), load1 10–33.** The expectation was wrong, and usefully: with the limit raised the self-test finished (908.97 s — over the old 900 s limit on its own) and reported exactly one injected error not caught, `abstract headroom (limit minus live count)`. That row's pattern hard-coded the old headroom figure, 85; session 50's counter fix (54c970e) made every document state 88, so the row matched nothing. (Described rather than quoted: quoting the phrase makes this row a live headroom claim, and the checker said so on the first draft.) A defect introduced by this session and gated only by `--strict`, which cannot see a stale self-test row. The row is now derived from the live headroom, like the test-count and snapshot rows, and was re-checked in isolation (caught). Kept as the record. |
| `full_suite_20260917_run50.log` | **ROW WRITTEN BEFORE THE RUN, with the literal word `pending`. THE CLOSING SUITE OF SESSION 50 (ledger G1), run after the last edit to anything the suite reads.** | `pytest tests/ -q --durations=10`, detached, in this working tree, macOS on AC power (the laptop was charging from single digits; load1 about 8-12 from desktop apps and nothing else of this project running). Written at the **187-test** size. Intent: the suite of record for session 50's commits `7549ee2` through `792a148`. The same suite passed at `7d29447` in a clean clone (186-test size, 843.02 s, the long checker test 698.30 s), and the commits since have each passed `--strict` plus the tests they touched. Expectation: green, no failures, a few hundred to about nine hundred seconds depending on the checker test. **RESULT: RED — 1 failed, 186 passed in 1619.77 s (0:26:59); PID 93738, 20:37–21:48 CDT.** The one failure is `test_number_checker_is_not_vacuous`, and it is a TIMEOUT, not a checker finding: its `--self-test` subprocess was killed at the test's 900 s limit (`subprocess.TimeoutExpired`), and nothing it had printed reported a MISSED row. Two conditions the row's expectation did not foresee: the laptop idle-slept twice mid-run (the clock jumped 20:50→21:04 and 21:18→21:33; `caffeinate -i -w 93738` held it awake from 21:33), and a foreign THEIA job (PGID 92260, cwd ~/Downloads/THEIA, six processes at ~95% CPU each) started at ~20:37 and held load1 at 27–40, so ordinary tests took 60–87 s where they take 5–7 s. The real defect it exposed: the self-test grew to ~276 rows in session 50 and took 698 s at 252 rows on a quiet machine (gate50b), so 900 s no longer leaves room for any load. The limit is raised to 2400 s with that measurement in a comment; the assertion is unchanged. Kept as the record; `full_suite_20260917_run50b.log` is the re-run. |
| `full_suite_20260917_run49h.log` | **ROW WRITTEN BEFORE THE RUN, with the literal word `pending`. THE CLOSING SUITE OF SESSION 49 (ledger G1), re-run after run49g's single failure was fixed.** | `pytest tests/ -q`, detached, macOS on AC, same machine and same foreign load as run49g. Written at the **182-test** size. The only change since run49g is the wording of run49g's own row. Expectation: green; run49g's 181 passes plus the row fix. **RESULT: GREEN — 182 passed in 572.74 s, exit 0** (PID 2412, 2026-09-17 17:53–18:03). THE GREEN SUITE OF RECORD for session 49. |
| `full_suite_20260917_run49g.log` | **ROW WRITTEN BEFORE THE RUN, with the literal word `pending`. THE CLOSING SUITE OF SESSION 49 (ledger G1), run after the last edit.** | `pytest tests/ -q`, detached via `tools/detach.py`, macOS on AC, at load1 ~25–100 from two foreign jobs (THEIA and wildfire_ai). PID 99154, 2026-09-17 17:38–17:48. Written at the **182-test** size. Expectation: green. **RESULT: RED — 1 failed, 181 passed in 584.52 s, and the failure was THIS ROW.** `test_historical_run_rows_state_no_live_test_count` caught the row's own first draft, which stated the expected count in the live form the guard forbids in a historical run row, because such a row goes red the moment the suite grows. The phrase is described rather than quoted here, since quoting it trips the same guard — which it did on the first rewrite. Session 48's predecessor made the same mistake in its own row and session 42 made it twice; the guard exists for exactly this. Rewritten in the hyphenated form, nothing else touched, and the suite re-run: see `full_suite_20260917_run49h.log`. The 181 passes are the real signal — no pipeline test failed. |
| `full_suite_20260916_run49b.log`, `wrapper_20260916_run49b.log`, `memsample_20260916_run49b.log` | **ROWS WRITTEN BEFORE THE RUN, with the literal word `pending`.** Intent: session 49's first checkpoint, after three new tests (the partition-sweep merge in `27_partition_sweep.py`, its exact float reads, and `12_partition_variance.py`'s failure kinds), a comment in `models.py`, the A14 record, and the live test and staged-file counts moved to match. Each new test was mutation-checked from scratch copies of the scripts (five mutations, five caught for the intended reason) before this run. Expectation: `SUITE_EXIT=0`, zero failures, three more tests than `_run49`. **RESULT: `168 passed in 287.98s (0:04:47)`, ZERO failures, `SUITE_EXIT=0`, `TOTAL_ELAPSED_SECONDS=289`, START 20:45:52, END 20:50:41 CDT, AC power, battery 62% to 63% charging. Slowest: `test_number_checker_is_not_vacuous` 163.77 s. Memory sampler: 24 samples 20:45:52-20:57:23; the 10 inside the suite read 2.577-7.575 GiB available (mean 5.986, 0 above 8 GiB); swap total ['10240', '11264'] MB, peak use 97.3%; load1 7.84-15.07 inside the suite; power ['AC_Power'], battery 62% to 65%.** | `pytest tests/ -q --durations=25`, detached behind the power-stamping wrapper with the memory sampler beside it, 2026-09-16. |
| `full_suite_20260916_run49.log`, `wrapper_20260916_run49.log`, `memsample_20260916_run49.log` | **ROWS WRITTEN BEFORE THE RUN, with the literal word `pending`.** Intent: session 49's BASELINE at the inherited commit `ef9ab7d`, before any edit to a tested file (this row is the only change). Session 48's inherited commit was red on a guard its predecessor believed green, so the baseline is run, not assumed. Launched on AC power, battery 47% charging, load1 3.57, with a foreign MPS training group (`~/Aarav IEEE/wildfire_ai`, PGID 99019) running. Expectation: `SUITE_EXIT=0`, zero failures, the same size as `_run48c`. **RESULT: `165 passed in 280.97s (0:04:40)`, ZERO failures, `SUITE_EXIT=0`, `TOTAL_ELAPSED_SECONDS=282`, START 20:19:28, END 20:24:10 CDT, AC power, battery 49% to 51% charging. The inherited commit is green. Slowest: `test_number_checker_is_not_vacuous` 151.32 s (54% of the run). Memory sampler: 26 samples 20:19:28-20:31:59; the 10 inside the suite read 2.888-7.579 GiB available (mean 5.993, none above 8 GiB) -- 2.888 is the lowest floor this instrument has recorded; two samples after the suite reached 8.007 and 8.343; swap total 11,264 then 12,288 MB, peak use 95.2%; load1 3.10-9.42 inside the suite; AC throughout, battery 49% to 56%.** | `pytest tests/ -q --durations=25`, detached behind the power-stamping wrapper, with the 30-second memory and power sampler beside it, 2026-09-16. |
| `full_suite_20260916_run48c.log`, `wrapper_20260916_run48c.log` | **ROWS WRITTEN BEFORE THE RUN, with the literal word `pending`.** Intent: the first full run at the 165-test size, after the shared-mask fast path and its two tests. Launched on battery at 13% with about 11 minutes left, so it may be killed by sleep before it finishes; if the log ends without `SUITE_EXIT`, that is what happened. Expectation: `SUITE_EXIT=0`, ZERO failures. **RESULT: `165 passed in 388.93s (0:06:28)`, ZERO failures, `SUITE_EXIT=0`, `TOTAL_ELAPSED_SECONDS=390`, START 18:57:54, END 19:04:24 CDT, battery 13% to 3%. THE GREEN SUITE OF RECORD at the 165-test size.** | `pytest tests/ -q --durations=25`, detached behind the power-stamping wrapper, 2026-09-16. |
| `selftest_20260916_run48.log` | **ROW WRITTEN BEFORE THE RUN, with the INTENT and the EXPECTATION and the literal word `pending` where the result goes.** Intent: `19_check_numbers.py`'s two reference-count mutations were re-pointed on 2026-09-16 (the list grew to thirteen with Bengio and Grandvalet, so the old count-word sentence is no longer in the manuscript and the old injections would have had nothing to mutate). A mutation that cannot apply is a vacuous self-test row. Expectation: the injected-error count stays at 129 — two rows re-pointed, none added or removed — and the exit status is 0. **RESULT: `SELF-TEST PASSED: all 131 injected errors were caught.`, `SELFTEST_EXIT=0`, `TOTAL_ELAPSED_SECONDS=154`, START 18:51:02, END 18:53:36 CDT, on battery (21% to 18%). THE EXPECTATION WAS WRONG, and the run was right: session 47's LAST self-test, `selftest_20260916_run47b.log`, already reported 131 (it added two rows after `_run47` reported 129); the 129 came from a carried handoff line that predates that. The count did not change in session 48, which is what the expectation meant to predict.** A CONCURRENT CPU LOAD: a 3-second synthetic prototype ran during the last seconds of this run. | `19_check_numbers.py --self-test`, detached behind a wrapper that writes `SELFTEST_EXIT`, POWER_START and POWER_END into this same log. |
| `full_suite_20260916_run48b.log` | **ROW WRITTEN BEFORE THE RUN, with the INTENT and the EXPECTATION and the literal word `pending` where the result goes.** Intent: the checkpoint after session 48's first block — the two live test-count phrasings rewritten, `12_partition_variance.py` given `--isi-only` and a failure KIND on every partition it cannot compute, and four new files under `cluster/`. Taken so that the commit a clean HPC4 clone reproduces is a tested one. Expectation: still the 163-test size (no test was added), `SUITE_EXIT=0`, ZERO failures. **RESULT: `163 passed in 465.81s (0:07:45)`, ZERO failures, `SUITE_EXIT=0`, `TOTAL_ELAPSED_SECONDS=467`. GREEN.** Run ENTIRELY ON BATTERY (50% to 41%), and 1.79× the AC-powered `_run48` of seventy minutes earlier (259.83 s) on a tree differing by prose and one script no test executes. One pairing, recorded as the COVARIATE axis asks and not read as a cause — and power is not the only thing that moved: load1 peaked at 47.39 during this run against 11.82 during `_run48`, both from the foreign `wildfire_ai` session, so two covariates changed at once and this pair cannot separate them. | `pytest tests/ -q --durations=25`, detached behind `wrapper_20260916_run48b.log`, 2026-09-16. Power and load recorded by the wrapper and by `memsample_20260916_run48b.log`. |
| `wrapper_20260916_run48b.log` | **DIAGNOSTIC, not a result.** The timing wrapper's own log for `full_suite_20260916_run48b.log`, stamping POWER_START and POWER_END. **RESULT: `START 2026-09-16 18:27:09 CDT`, `STAGE_END pytest 18:34:56`, `SUITE_EXIT=0`, `TOTAL_ELAPSED_SECONDS=467`; `POWER_START` battery 50% discharging, `POWER_END` battery 41% discharging.** | `bash` wrapper, same as `wrapper_20260916_run48a.log`. |
| `memsample_20260916_run48b.log` | **DIAGNOSTIC, not a result.** Memory and power sampled during `full_suite_20260916_run48b.log`, same columns as `memsample_20260916_run48.log`. **RESULT: 16 samples 18:27:09–18:34:40; 16 inside the suite at 3.824–10.598 GiB available (mean 6.948, 6 above 8 GiB); swap total [29696, 30720, 31744, 32768] MB, peak use 97.8%; load1 10.19–47.39; power ['Battery_Power'], battery 50% to 41%.** | 30-second CSV sampler, detached, 16 samples. |
| `full_suite_20260916_run48.log` | **ROW WRITTEN BEFORE THE RUN, with the INTENT and the EXPECTATION and the literal word `pending` where the result goes.** Intent: session 48's BASELINE suite at the inherited commit `5ab570a`, before this session's first edit to any tested file. The only change in the tree is two new, untracked files under `cluster/`, which the snapshot builder excludes wholesale. Expectation: the 163-test size, `SUITE_EXIT=0`, ZERO failures. **RESULT: RED — `1 failed, 162 passed in 259.83s (0:04:19)`, `SUITE_EXIT=1`, `TOTAL_ELAPSED_SECONDS=261`. KEPT DELIBERATELY: the one failure, `test_historical_run_rows_state_no_live_test_count`, is a TRUE POSITIVE with TWO offenders.** (1) INHERITED: session 47's `_run47h` row, whose result sentence was written after `_run47h` ran and used the live test-count form — so session 47's final commit `5ab570a` was red although its message says green. Confirmed by running that single test on a clean `git worktree` of `5ab570a`: `1 failed, 162 deselected in 1.56s`. (2) MINE: this very row's expectation clause, written in the live form minutes before launch. Both rewritten to the hyphenated form with no number changed; the test then passed alone in 1.01 s. `test_number_checker_is_not_vacuous` took 148.67 s, 57.2% of the total — its share holds again. | `pytest tests/ -q --durations=25`, detached behind `wrapper_20260916_run48a.log`, 2026-09-16, on AC power. Load at launch: a foreign `wildfire_ai` session in `~/Aarav IEEE/` (PGIDs 73334 and 92600, ten python processes on MPS), load1 12.14; HPC4 diagnostic job 129294 running remotely at no local cost. |
| `wrapper_20260916_run48a.log` | **DIAGNOSTIC, not a result.** The timing wrapper's own log for `full_suite_20260916_run48.log`. Single-stage, and the first wrapper in this project to stamp the POWER SOURCE at START and END (`POWER_START` / `POWER_END`), which closes the COVARIATE gap session 47 named for this instrument. **RESULT: `START 2026-09-16 15:14:18 CDT`, `STAGE_END pytest 15:18:39`, `SUITE_EXIT=1`, `TOTAL_ELAPSED_SECONDS=261`; `POWER_START` AC at 49% charging, `POWER_END` AC at 54% charging.** | `bash` wrapper printing START, POWER_START, STAGE_BEGIN, STAGE_END, SUITE_EXIT, POWER_END, END and TOTAL_ELAPSED_SECONDS. |
| `memsample_20260916_run48.log` | **DIAGNOSTIC, not a result.** Memory sampled DURING `full_suite_20260916_run48.log`: same instrument and columns as `memsample_20260915_run47.log` plus two new ones, `power_source` and `battery_pct`, read from `pmset -g batt` at every sample. **RESULT: 16 samples, 15:14:18–15:21:48. Nine fell inside the suite: available 4.586–13.415 GiB, mean 8.150, four of nine above 8 GiB. Over all 16: 4.260–13.415 GiB, eight above 8 GiB — the first window in several sessions with ANY sample above 8. Swap total took FIVE values in seven minutes (27,648 / 28,672 / 29,696 / 30,720 / 31,744 MB), peak use 98.1%. load1 7.28–11.82. Power: AC for the first fifteen samples (battery 49% to 58%), BATTERY for the last — the machine was unplugged at about 15:21, after the suite had finished. Adjacent samples differ by up to 8.8 GiB, so a floor or a ceiling from one window describes that window and nothing else.** | 30-second CSV sampler, detached, 16 samples. |
| `full_suite_20260916_run47h.log`, `wrapper_20260916_run47h.log` | **ROWS WRITTEN BEFORE THE RUN, with the literal word `pending`.** Intent: the run after session 47's FINAL block — Supplementary Table S5, the ssGSEA sort benchmark, the 24-partition NSCLC results, and the science backlog added to the master prompt. Still the **163-test** size; that block added no tests. Expectation: `SUITE_EXIT=0`, ZERO failures. **RESULT: `163 passed in 244.51s (0:04:04)`, ZERO failures, `SUITE_EXIT=0`, `TOTAL_ELAPSED_SECONDS=244`. THE GREEN SUITE OF RECORD**, taken after session 47's last edit of any kind. **Session 47's six full-suite runs at the 157- to 163-test sizes span 230.51–267.92 s — a 16% spread across four test-count changes, a battery that went 37% to mains, and an intermittent foreign benchmark. Session 46's 608.60 s still stands alone against all of it, and memory, swap, test count, cross-repository CPU and power source are each now excluded by measurement rather than by argument.** | `pytest tests/ -q`, detached behind the single-stage timing wrapper, 2026-09-16, on AC power. Load: HPC4 sweeps running remotely at no local cost. |
| `full_suite_20260916_run47g.log`, `wrapper_20260916_run47g.log` | **ROWS WRITTEN BEFORE THE RUN, with the literal word `pending`.** Intent: the re-run after `_run47f` went red on the third instance of one guard in a single session — first the live form in a row, then the same live form inside the sentence describing it. Fixed by describing the phrase rather than showing it. Expectation: `SUITE_EXIT=0`, ZERO failures, **163-test** size. **RESULT: `163 passed in 230.51s (0:03:50)`, ZERO failures, `SUITE_EXIT=0`, `TOTAL_ELAPSED_SECONDS=231`. THE GREEN SUITE OF RECORD.** Taken after session 47's last edit of any kind. **The three-run sequence `_run47e` RED → `_run47f` RED → `_run47g` GREEN is kept in full**, because what it records is one guard catching three successive forms of the same defect from the session that was adding guards: the live count in a row, then the live count inside the sentence describing it, then clean. | `pytest tests/ -q`, detached behind the single-stage timing wrapper, 2026-09-16, on AC power. |
| `full_suite_20260916_run47f.log`, `wrapper_20260916_run47f.log` | **ROWS WRITTEN BEFORE THE RUN, with the INTENT and the EXPECTATION and the literal word `pending`.** Intent: the re-run after `_run47e` came back RED with **two TRUE POSITIVES, both defects this session introduced**. (1) `test_results_readme_classifies_every_artefact_that_exists` caught `selftest_20260916_run47b.log` with no row — this file's own convention is that the row precedes the run, and it was forgotten. (2) `test_historical_run_rows_state_no_live_test_count` caught the live (unhyphenated) form of the test count inside the `_run47e` row — a historical record written in the grammar of a live claim, which session 45 did too and which is why the guard exists. **The offending phrase is deliberately NOT reproduced here.** Quoting it inside a run row re-creates the defect, the handoff warns about exactly that, and session 47 walked into it anyway on the first attempt at this very row — so this sentence describes the phrase instead of showing it. **Both were fixed at source, not by loosening a guard.** Expectation: `SUITE_EXIT=0`, ZERO failures, still **163-test** size. **RESULT: RED — `SUITE_EXIT=1`, one failure, and it was the SAME guard firing on the fix for itself.** The `_run47f` row quoted the offending phrase verbatim while describing it, which re-creates the defect inside the record of the defect. The handoff warns about precisely this, session 45 did it, and session 47 did it too. Superseded by `_run47g`. **Kept, not overwritten: a red log that caught a real defect is evidence, and rule 8 forbids deleting it.** | `pytest tests/ -q`, detached behind the single-stage timing wrapper, 2026-09-16, on AC power. |
| `selftest_20260916_run47b.log` | **ROW WRITTEN AFTER THE RUN, which is a RULE VIOLATION and is recorded as one.** This repository's convention is that every log gets its row BEFORE the run finishes, with `pending` where the result goes; this one was launched and the row was forgotten, and `test_results_readme_classifies_every_artefact_that_exists` is what caught it — the guard working exactly as designed, on the session that was adding guards. Intent: re-run the self-test after two `pcsortaudit_*` authorities and their two injection rows were added, to confirm the new rows actually fire. **RESULT: `SELF-TEST PASSED: all 131 injected errors were caught.`** Up from 129, which is the +2 those rows should contribute and therefore evidence they are live rather than inert. **THE SELF-TEST OF RECORD**, superseding `selftest_20260916_run47.log`. Those four sortaudit rows share their regexes (`ISI of`, `moves by`) and are separated only by their `WRAP:` paragraph anchors, so this count is also the proof that the re-scoping done when the pan-cancer paragraph landed actually works: collapse the anchors and two of the four stop being caught. **No `SELFTEST_EXIT` line** — detached with a bare `tools/detach.py`, which does not capture its child's exit status; the PASSED line is the evidence. | `19_check_numbers.py --self-test`, detached, 2026-09-16, on AC power. |
| `full_suite_20260916_run47e.log` | **ROW WRITTEN BEFORE THE RUN, with the INTENT and the EXPECTATION and the literal word `pending` where the result goes.** Intent: the run after session 47's SECOND block of work — the pan-cancer landing, the sort-audit dead-code fix, `--start-seed`/`--shard`, and Supplementary Table S5. Still the **163-test** size: this block modified existing scripts and added a supplementary table, and added no tests, so the nine documents that state the suite size did NOT move. Expectation: `SUITE_EXIT=0`, ZERO failures. **RESULT: RED — `2 failed, 161 passed in 225.70s`, `SUITE_EXIT=1`. BOTH TRUE POSITIVES, both defects this session introduced, and both fixed at source rather than by loosening a guard.** (1) A self-test log launched with no `results/README.md` row — this file's own convention, forgotten by the session enforcing it. (2) A test count written in the live form inside a historical run row. **Kept, not overwritten: rule 8, and a red log that caught a real defect is evidence.** | `pytest tests/ -q`, detached behind the single-stage timing wrapper, 2026-09-16, on AC power. Load: two HPC4 sweeps running remotely (no local cost) and an intermittent foreign `pytest` in another repository. |
| `wrapper_20260916_run47e.log` | **DIAGNOSTIC, not a result.** The timing wrapper's own log for `full_suite_20260916_run47e.log`. Row written before the run. **RESULT: pending — this clause is filled in from the log, not from expectation.** *[Filled 2026-09-21 by session 54, from the log; it never had been.]* **RESULT: `SUITE_EXIT=1`, `TOTAL_ELAPSED_SECONDS=226`**, START 09:55:46 and END 09:59:32 CDT 2026-09-16 — agreeing with its suite row, which records the same run RED at `2 failed, 161 passed in 225.70s`. | `bash` wrapper printing START, STAGE_BEGIN, STAGE_END, SUITE_EXIT, END and TOTAL_ELAPSED_SECONDS. |
| `full_suite_20260916_run47d.log` | **ROW WRITTEN BEFORE THE RUN, with the INTENT and the EXPECTATION and the literal word `pending` where the result goes.** Intent: the run after session 47's genuinely final edit, at the **163-test** size — up two more by `test_committed_figure_pdfs_are_free_of_british_spelling` and its can-fail, the FOURTH blind sync test to get an independent companion and the first check in this project that reads a RENDERED ARTEFACT rather than a source file. Expectation: `SUITE_EXIT=0`, ZERO failures. **Contention expected and stated before the fact:** a foreign `pytest` benchmark from `~/Aarav IEEE/wildfire_ai` has been running intermittently and took the deposit run to 793.70 s, so this wall clock is not an idle figure. **RESULT: `163 passed in 255.58s (0:04:15)`, ZERO failures, `SUITE_EXIT=0`, `TOTAL_ELAPSED_SECONDS=256`.** **THE GREEN SUITE OF RECORD.** **And it sharpens §0d further:** session 47's four full-suite runs are 255.00 (157), 257.49 (159), 267.92 (161) and **255.58 (163)** — a **5.1% spread across four runs, a growth of six tests, a battery that went 37% → 21% → mains, and a foreign `pytest` benchmark running intermittently.** Session 46's 608.60 s at the **157-test** size stands alone against all of it. Whatever explains that run, it is not memory, not swap, not test count, not cross-repository CPU and not power source — all five are now excluded by measurement rather than by argument. | `pytest tests/ -q --durations=25`, detached behind the single-stage timing wrapper, 2026-09-16, on AC power. |
| `wrapper_20260916_run47d.log` | **DIAGNOSTIC, not a result.** The timing wrapper's own log for `full_suite_20260916_run47d.log`. Row written before the run. **RESULT: `START 02:17:52`, `STAGE_BEGIN pytest 02:17:52`, `STAGE_END pytest 02:22:08`, `SUITE_EXIT=0`, `TOTAL_ELAPSED_SECONDS=256`** — agreeing with the suite's own `255.58s` to within rounding. | `bash` wrapper printing START, STAGE_BEGIN, STAGE_END, SUITE_EXIT, END and TOTAL_ELAPSED_SECONDS. |
| `reproduce_check_20260916_run47.log` | **ROW WRITTEN BEFORE THE RUN, with the INTENT and the EXPECTATION and the literal word `pending` where the result goes.** Intent: workstream 2 task 4, re-timing `reproduce.sh --check`. The recorded 247 s idle / 1,069 s contended dates from session 34 and predates BOTH `--coverage --strict` being added to this path and five suite growths (146 → 161). **It also has a consumer nobody re-checked: `snapshot/README.md` — the first page a reader of the PUBLIC repository sees — says `--check` "takes about four minutes and exits 0".** That is very likely false now, and a lower bound from already-measured components says so before this run starts: the suite alone was 267.92 s today and `--check` runs it plus `19_check_numbers.py --self-test` (136 s), `26_sort_audit.py --self-test`, the provenance manifest, the version check, split-determinism, the sort audit, a figure re-render, the abstract recount, the number check and `--coverage --strict`. **Expectation: comfortably over four minutes.** **CONTENDED, and recorded as such before the fact:** a FOREIGN `pytest` from `~/Aarav IEEE/wildfire_ai` (PGID 52738) was running a CPU benchmark at ~377% when this was launched, so this is a contended figure, not an idle one. **RESULT: `CHECK PASSED`, `CHECK_EXIT=0`, `TOTAL_ELAPSED_SECONDS=1283`** — 21.4 minutes, `START 2026-09-16 00:42:55 CDT`, `END 2026-09-16 01:04:18 CDT`. **5.2× the 247 s idle figure session 34 recorded, and 5.3× the "about four minutes" `snapshot/README.md` promised a public reader.** That claim was a real measurement that went stale, and it has been corrected in `snapshot/README.md` to a dated, condition-stated figure. **What this run does NOT settle:** an idle wall clock for this path. The foreign benchmark ran throughout, and the suite inside `--check` visibly crawled — it reached 44% in about eleven minutes against 267.92 s for the whole suite uncontended earlier the same night. A lower bound from measured components puts the idle figure near 450–500 s, but that is arithmetic, not a measurement, and is labelled as such. **Figures restored afterwards with `git checkout -- pipeline/results/figures/`: exactly the five `.pdf`s changed, as documented, and the `.png`s did not.** | `bash reproduce.sh --check`, detached, 2026-09-16, on AC power. `git checkout -- pipeline/results/figures/` afterwards, because line ~157 re-renders and rewrites five `.pdf`s with a new `/CreationDate`. |
| `full_suite_20260916_run47c.log` | **ROW WRITTEN BEFORE THE RUN, with the INTENT and the EXPECTATION and the literal word `pending` where the result goes.** Intent: the run after session 47's genuinely last edit, at the **161-test** size — up two more from 159 by `test_environment_md_versions_agree_with_the_lockfile` and its can-fail, the independent companion for the second blind sync test this session closed. Expectation: `SUITE_EXIT=0`, ZERO failures. **This is the run that answers the caveat on `full_suite_20260915_run47b.log`**, which was green at 159 but predated this session's prose edits. **On AC power** — the first suite in this session's record not taken on battery, which matters because power source is the tenth axis and a value is only useful if every run carries one. **RESULT: `161 passed in 267.92s (0:04:27)`, ZERO failures, `SUITE_EXIT=0`, `TOTAL_ELAPSED_SECONDS=268`.** **THE GREEN SUITE OF RECORD.** Taken after this session's last edit of any kind, so it carries no caveat. **Three timings now sit beside each other with the tenth axis recorded on every one:** 255.00 s at 157 on battery 37%, 257.49 s at 159 on battery 21%, 267.92 s at 161 on AC. The spread across all three is 5.1% while the machine went from 37% battery to mains and the suite grew by four tests — **so on this evidence power source does not explain the timing either, and nobody should promote it to a cause on the strength of having just measured it.** Session 46's 608.60 s remains the outlier and remains unexplained. **One contention note recorded honestly:** a FOREIGN `pytest` was found scheduled in another repository (`~/Aarav IEEE/wildfire_ai`, PGID 37992, behind a `sleep 420`) about a minute before this run started. Whether it actually ran during these 268 s was not observed — it was absent from `ps` afterwards. Treat this run's wall clock as possibly contended rather than certainly idle. | `pytest tests/ -q --durations=25`, detached behind the single-stage timing wrapper, 2026-09-16. |
| `wrapper_20260916_run47c.log` | **DIAGNOSTIC, not a result.** The timing wrapper's own log for `full_suite_20260916_run47c.log`. Single-stage, with `STAGE_BEGIN`/`STAGE_END`. Row written before the run. **RESULT: `START 2026-09-16 00:31:39 CDT`, `STAGE_BEGIN pytest 00:31:39`, `STAGE_END pytest 00:36:07`, `SUITE_EXIT=0`, `TOTAL_ELAPSED_SECONDS=268`** — agreeing with the suite's own `267.92s` to within rounding. | `bash` wrapper printing START, STAGE_BEGIN, STAGE_END, SUITE_EXIT, END and TOTAL_ELAPSED_SECONDS. |
| `selftest_20260916_run47.log` | **ROW WRITTEN BEFORE THE RUN, with the INTENT and the EXPECTATION and the literal word `pending` where the result goes.** Intent: session 47 did not change `19_check_numbers.py`, so `selftest_20260915_run46.log` was still formally current — but it added two tests and edited the live test count in nine documents, and the honest question is whether the injected-error count moved. **Expectation, stated before the run and worth checking against the outcome:** it should NOT move. Session 46 predicted a rise and was wrong for a structural reason worth re-testing — the self-test injects errors into AUTHORITY rows, and adding tests or editing a document's copy of a number changes neither the authority table nor the injectable patterns. If the count moves, that reasoning is wrong and the self-test is sensitive to something nobody has characterised. **RESULT: `SELF-TEST PASSED: all 129 injected errors were caught.` The count did NOT move, and this time that was PREDICTED in this row before the run rather than explained afterwards.** The structural reason holds: injections go into AUTHORITY rows, so adding tests and editing nine documents' copies of a number changes the claims checked but not the patterns injected. **Two honest limitations of this record.** First, `grep -c CAUGHT` over this log returns **130** against the tool's own **129**, because the explanatory header contains the word — the same line-versus-occurrence over-count session 46 hit, and the tool's summary is the authority. Second, **no `SELFTEST_EXIT` line appears in this log**: it was detached with a bare `tools/detach.py` rather than behind the timing wrapper, and `detach.py` does not record its child's exit status. The PASSED line is the evidence here; a future run should use the wrapper so the exit code is captured too. | `19_check_numbers.py --self-test`, detached, 2026-09-16, on AC power (the machine was plugged in at 00:28 after reaching 11%). |
| `full_suite_20260915_run47b.log` | **ROW WRITTEN BEFORE THE RUN, with the INTENT and the EXPECTATION and the literal word `pending` where the result goes.** Intent: the run after session 47's LAST code edit, at the **159-test** size — up two from the inherited 157 by `test_the_readme_document_table_covers_the_deposit_in_both_directions` and its can-fail companion. Expectation: `SUITE_EXIT=0`, ZERO failures. **Second purpose, and the reason it carries its own sampler:** one paired memory/timing measurement is a single point, and session 47's first pair already falsified session 46's memory hypothesis. A second pair tests whether that falsification replicates rather than resting on one run. **RESULT: `159 passed in 257.49s (0:04:17)`, ZERO failures, `SUITE_EXIT=0`, `TOTAL_ELAPSED_SECONDS=258`.** GREEN at the new 159-test size. **And the falsification replicates:** mean available memory 4.391 GiB and swap 98.5% used — worse than session 46's 4.80 GiB and 94.1% — for a wall clock within 1.0% of the first run's. Session 46's 608.60 s remains unexplained, but memory pressure is no longer a candidate for it. | `pytest tests/ -q --durations=25`, detached behind the single-stage timing wrapper, 2026-09-16. **On BATTERY at 21%** — recorded because session 47 made power source a tracked variable and a value is only useful if every run carries one. |
| `memsample_20260915_run47b.log` | **DIAGNOSTIC, not a result.** The second concurrent memory sample, same instrument and columns as `memsample_20260915_run47.log`. Its job is replication: the first pair showed a 255.00 s suite at mean 4.620 GiB available, against session 46's 608.60 s at 4.80 GiB, which breaks the memory explanation. A second pair at a known battery level either reproduces that or complicates it. Row written before the run. **RESULT: it REPLICATES, and more sharply than the first.** During the 257.49 s suite (8 samples inside 23:58:20–00:02:38) available memory was mean **4.391 GiB**, min **3.870** — lower than the first pair — and swap was **98.5%** used against session 46's 94.1%. **So the second run was under WORSE memory pressure than session 46's 608.60 s run on every axis, and finished in 257.49 s.** Two paired measurements 33 minutes apart, 255.00 s and 257.49 s, a 1.0% spread. The window's own floor/ceiling were 3.87/5.47 GiB over 24 samples, none above 8 GiB. | `/opt/anaconda3/bin/python3 memsample.py 24 30` (24 samples at 30 s = 12 min), detached, launched immediately before the suite. |
| `wrapper_20260915_run47b.log` | **DIAGNOSTIC, not a result.** The timing wrapper's own log for `full_suite_20260915_run47b.log`. Single-stage, with `STAGE_BEGIN`/`STAGE_END` timestamps. Row written before the run. **RESULT: `START 2026-09-15 23:58:20 CDT`, `STAGE_BEGIN pytest 23:58:20`, `STAGE_END pytest 00:02:38`, `SUITE_EXIT=0`, `END 2026-09-16 00:02:38 CDT`, `TOTAL_ELAPSED_SECONDS=258`** — the run crosses local midnight, which is why its log name carries the start date and its END carries the next one. | `bash` wrapper printing START, STAGE_BEGIN, STAGE_END, SUITE_EXIT, END and TOTAL_ELAPSED_SECONDS. |
| `full_suite_20260915_run47.log` | **ROW WRITTEN BEFORE THE RUN, with the INTENT and the EXPECTATION and the literal word `pending` where the result goes.** Intent: session 47's BASELINE suite, run at the inherited commit `4209470` before this session's first edit to any tested file, so that a defect found later can be attributed rather than guessed at. Run with `--durations=25` — more than the usual 15 — because this run is one half of workstream 2 task 2, the paired measurement that has never been taken: a memory sampler runs concurrently under `memsample_20260915_run47.log`, so per-test durations can be correlated against memory pressure sampled every 30 s. Expectation: `SUITE_EXIT=0`, ZERO failures, at the **157-test** size session 46 handed over. **RESULT: `157 passed in 255.00s (0:04:14)`, ZERO failures, `SUITE_EXIT=0`, `TOTAL_ELAPSED_SECONDS=255`.** Session 46's commit `4209470` is GREEN, so the inherited state is sound and any later defect is attributable. **And the timing is the finding: 255.00 s at the **157-test** size, against session 46's 608.60 s at that SAME size 42 minutes earlier — the third 157-test measurement, and it lands beside the 265.22 s one, which makes 608.60 s the outlier rather than the new normal.** Top duration `test_number_checker_is_not_vacuous` at **141.86 s** — 55.6% of the suite, the same share session 43 measured idle (115.65 s of 209.21 s), so that test's dominance is stable even when the total is not. | `pytest tests/ -q --durations=25`, detached via `tools/detach.py` behind a single-stage timing wrapper that timestamps its stage boundary (PID 87852, verified PPID 1, PGID == PID, STAT `Ss`, child at 1213% CPU), 2026-09-15 23:25:24–23:29:39 CDT. Load: no Codex, no VM, no foreign `pytest`, no Blender; two `esbuild` processes from an unrelated repository. **On BATTERY power at 37%** — the first time any session has recorded the machine's power source alongside a timing. |
| `memsample_20260915_run47.log` | **DIAGNOSTIC, not a result — and the instrument workstream 2 task 2 asks for.** CSV, one row every 30 s: available memory by the project's fixed definition (free + inactive + speculative + purgeable at a 16384 B page), the four components separately, active/wired/compressor, `sysctl vm.swapusage`'s total/used/free, cumulative swapins/swapouts/pageins/pageouts, and the 1-minute load average. Its purpose is to answer, for the first time, whether the suite's wall-clock spread tracks memory pressure: session 46 measured 265.22 s and 608.60 s twenty minutes apart on trees differing by 1.65 s of tests, excluded test count and cross-repository CPU by measurement, and was left with memory pressure as the only surviving candidate — pointed at, but never sampled during a run. This log is the sample. Row written before the run. **RESULT: 40 samples, 23:24:33–23:44:03 CDT. Available-memory FLOOR 4.22 GiB, CEILING 5.01 GiB, median 4.71, swing 1.19× — the tightest window any session has recorded, and ZERO of 40 samples above 8 GiB for the third session running. Swap total 18,432 MB (it has now been 7,168 → 19,456 → 17,408 → 18,432 across four readings; the OS resizes it continuously and no single value should be quoted as "the" total), peak 92.6% used.** **AND THE PAIRED MEASUREMENT, WHICH IS THE POINT: during the 255.00 s suite (9 samples inside 23:25:24–23:29:39) available memory was mean 4.620 GiB, min 4.216, max 4.947, swap 92.6% used.** Session 46 measured **4.80 GiB** five minutes after its **608.60 s** run and was left with memory pressure as the only candidate it had not excluded by measurement. Session 47 ran that SAME **157-test** suite **2.39× faster at a mean 4.620 GiB — at or below that level — with swap equally full.** **So memory pressure at this level is NOT sufficient to cause the slowdown, and the one correlated pair that pointed at it is broken by a controlled sample.** That is a negative result and it is the honest one: the spread is again unexplained, but it is unexplained with one fewer live candidate rather than with a false one. What session 47 adds in its place is a state variable NO session has ever recorded alongside a timing — the machine's POWER SOURCE. This run was on **battery at 37%**, and whether any earlier run was is unknown and now unknowable. | `/opt/anaconda3/bin/python3 memsample.py 40 30` (40 samples at 30 s = 20 min), detached (PID 86955, verified PPID 1, PGID == PID, STAT `Ss`), launched immediately before the suite so it brackets the whole run. |
| `wrapper_20260915_run47a.log` | **DIAGNOSTIC, not a result.** The timing wrapper's own log for `full_suite_20260915_run47.log`. **Single-stage**, so its `TOTAL_ELAPSED_SECONDS` should match the suite's own wall clock to within a second or two. Unlike session 46's `_run46a` it also echoes `STAGE_BEGIN`/`STAGE_END` timestamps around the one stage, which is the fix that handoff asked for: a wrapper should let its reader read a stage's wall clock out of the instrument instead of recovering it from a log file's mtime. Row written before the run. **RESULT: `START 23:25:24`, `STAGE_BEGIN pytest 23:25:24`, `STAGE_END pytest 23:29:39`, `SUITE_EXIT=0`, `END 23:29:39`, `TOTAL_ELAPSED_SECONDS=255`** — and the stage boundary agrees with the suite's own `255.00s` to within rounding, which is the cross-check a single-stage wrapper exists to provide. **A defect in the FIRST version of this instrument, recorded because it was caught rather than because it survived:** the wrapper initially ran `pytest` with no redirection, so the suite's output would have landed in the wrapper's own log and `full_suite_20260915_run47.log` would never have existed. It was caught ten seconds after launch, the process group was killed, `pytest` was redirected (not piped — a pipe would have made `$?` the pipeline's), and the run was restarted under the same log names, which were still free. | `bash` wrapper printing START, STAGE_BEGIN, STAGE_END, SUITE_EXIT, END and TOTAL_ELAPSED_SECONDS. |
| `wrapper_20260915_run46a.log` | **DIAGNOSTIC, not a result.** The timing wrapper's own log for `selftest_20260915_run46.log` and `full_suite_20260915_run46.log`. **TWO-STAGE**, unlike session 45's four wrappers — so its `TOTAL_ELAPSED_SECONDS` covers the self-test AND the suite and must NOT be read as either one's wall clock. Row written before the run, because a wrapper writes its own log into `results/` and that log needs a row too. **RESULT: `TOTAL_ELAPSED_SECONDS=402`**, which is 136 s of self-test plus 265 s of suite plus about a second of overhead — the three numbers sum, which is the check a two-stage wrapper exists to make possible. **A flaw in this instrument, recorded rather than hidden:** it stamps START and END but not the stage boundary, so the 136 s figure is not read from this log at all. It was recovered from the self-test log's mtime against START, and a future wrapper should echo a timestamp between stages instead of making its reader do that. | `bash` wrapper printing START, END and ELAPSED_SECONDS around both commands. |
| `wrapper_20260909_run45a.log` | **DIAGNOSTIC, not a result.** The timing wrapper's own log for `full_suite_20260909_run45.log`. Single-stage, so `TOTAL_ELAPSED_SECONDS` should match the suite's own wall clock to within a second or two; a larger gap means interpreter start-up or collection, not tests. | `bash` wrapper printing START, END and ELAPSED_SECONDS. |
| `full_suite_20260909_run2.log` | **COMPLETE — `148 passed in 215.86s (0:03:35)`, ZERO failures, `SUITE_EXIT=0`, `ELAPSED_SECONDS=216`.** **THE GREEN SUITE OF RECORD.** Row was written BEFORE the run finished, deliberately, then corrected from the log. Green because all three of `full_suite_20260909.log`'s failures were addressed at source rather than silenced: the offending durations sentence was rewritten into the hyphenated form (and its number corrected — it had stated the wrong run's size), and the nine documents stating the suite size were updated together. `19_check_numbers.py --strict` and `--coverage --strict` were both re-run to exit 0 beforehand. **215.86 s against the previous run's 215.78 s — 0.08 s apart at the same size on the same load, which is the tightest repeat this project has ever recorded and is worth more than either number alone: it says the suite's wall clock is highly reproducible when the load is, and therefore that the historical 6x spread is a property of the machine's state, not of the suite.** | `pytest tests/ -q --durations=15`, detached with the timing wrapper (PID 72413, verified PPID 1, PGID == PID, STAT `Ss`), 2026-09-09 00:18:27-00:22:03 CDT. Load: no Blender, no foreign `pytest`; Codex resident. |
| `wrapper_20260909_run43c.log` | **DIAGNOSTIC — the eighth timing wrapper's own log, not a result.** Single-stage; `TOTAL_ELAPSED_SECONDS=216` against the suite's `215.86s`. Row written before the run. | `tools/detach.py` + `wrap43c.sh`, 2026-09-09 00:18:27-00:22:03 CDT. |
| `wrapper_20260908_run43a.log` | **DIAGNOSTIC — the sixth timing wrapper's own log, not a result.** Single-stage (`pytest` alone), so its total and the suite figure agree to within a second: `TOTAL_ELAPSED_SECONDS=210` against the suite's own `209.21s`. Row written before the run, because a wrapper writes its own log into `results/` and that log needs a row too — forgetting exactly this turned `full_suite_20260908_run5.log` red. | `tools/detach.py` + `wrap43a.sh`, 2026-09-08 23:51:17-23:54:47 CDT. |
| `full_suite_20260908_run8.log` | **COMPLETE and RED — `1 failed, 145 passed in 233.55s (0:03:53)`, `SUITE_EXIT=1`, `ELAPSED_SECONDS=234`.** **KEPT DELIBERATELY, and it is the best evidence in this file that a guard was needed.** The single failure is `test_historical_run_rows_state_no_live_test_count` — the guard session 42 wrote earlier the same evening — and it fired on THIS ROW, because the row stated its expected size in the live-claim form (`Expected N tests`, unhyphenated) rather than the hyphenated form this file requires. Note this sentence deliberately writes the offending phrase GENERICALLY: quoting it verbatim re-introduces the defect into the very row that documents it, which is how the first correction of this row failed. That is the **third** instance session 42 committed personally: eight were inherited from #41 and fixed, two more were written an hour later and caught by the checker, and this one was written an hour after THAT and caught by the guard built in response. A convention everyone agreed with, violated three times in one evening by the person enforcing it. Superseded by `full_suite_20260908_run9.log`. The suite held **a 146-test count** at launch; session 42's second half added ten tests: the figure fresh-render sweep and its can-fail companion, the Okabe-Ito palette pin, the A0 print-legibility ratchet and its can-fail companion, the historical-run-row guard and its companion, the bold-balance scan and its companion, and the NextGen counter test. | `pytest tests/ -q --durations=15`, detached with the timing wrapper, run in sequence with `selftest_20260908_run3.log` in ONE wrapper, 2026-09-08 ~22:2x CDT. **CONTENDED** — one Blender render resident at ~210% CPU. |
| `selftest_20260908_run3.log` | **COMPLETE — PASSED, all 129 injected errors caught, `SELFTEST_EXIT=0`, `ELAPSED_SECONDS=121`.** **The self-test of record**, superseding `_run2`. The injected-error count did NOT move, as predicted. Eight contended figures now exist for this path — 107 / 111 / 117 / 117 / 117 / 121 / 134 / 262 s — and still no idle one. Re-run because `19_check_numbers.py` CHANGED again this session: `submission/ABSTRACT-PORTAL-DRY-RUN.md` was added to `DOCS` (the fourteenth scanned document) and given a `COVERAGE_FLOOR` entry. The injected-error row count is not expected to move — a document was added to the scan, not an authority to the table. | `19_check_numbers.py --self-test`, first of two commands in the sequence, 2026-09-08 ~22:2x CDT. **CONTENDED**, same load as `full_suite_20260908_run8.log`. |
| `wrapper_20260908_run42d.log` | **DIAGNOSTIC — the fourth timing wrapper's own log, not a result.** Two-stage (self-test then suite), so its total is the SEQUENCE and is not a suite figure — the same trap as `wrapper_20260908_run42.log`. Row written before the run, which is now the standing rule: a wrapper means THREE rows, not two. | `tools/detach.py` + `run42d.sh`, 2026-09-08 ~22:2x CDT. |
| `full_suite_20260908_run7.log` | **COMPLETE — `136 passed in 191.45s (0:03:11)`, ZERO failures, `SUITE_EXIT=0`, `ELAPSED_SECONDS=192`.** **THE GREEN SUITE OF RECORD.** Row was written BEFORE the run finished, deliberately, then corrected from the log. **191.45 s is the FASTEST figure ever recorded for this suite, at the LARGEST test count** — a 136-test run against the 104-test run that previously held the record at 120.55 s. The count did not move from `_run6`; the test count does not move because no test was added after `_run6`. Run because four files were edited AFTER `_run6` finished at 21:32:54: `results/README.md`, `pipeline/README.md` (the Prov-GigaPath model-vs-dataset licence disambiguation and the KEGG measured-no-op qualification) and `09-PAPER-DRAFT.md` (measured raw-input sizes in the Data Availability Statement). **A passing suite does not cover edits made after it started** — the same rule that produced `_run5` and `_run6`. | `pytest tests/ -q --durations=15`, detached with the timing wrapper, 2026-09-08 21:35:00-21:38:12 CDT. **CONTENDED** — one Blender render and the Codex desktop app resident; the other Claude session's `pytest` had finished. **Twelve timed runs now span 191 / 217 / 226 / 231 / 239 / 250 / 281 / 297 / 309 / 368 / 591 / 852 / 1,145 s** across 113-to-136-test runs, a 6.0x spread on ONE machine. Still NOT idle, so even 191 s is an upper bound. |
| `wrapper_20260908_run42c.log` | **DIAGNOSTIC — the third timing wrapper's own log, not a result.** Single-stage (`pytest` alone), so its total and the suite figure should agree to a second or two. Row written BEFORE the run, as `wrapper_20260908_run42.log` taught. | `tools/detach.py` + `run42c.sh`, 2026-09-08 ~21:4x CDT. |
| `full_suite_20260908_run6.log` | **COMPLETE — `136 passed in 249.67s (0:04:09)`, ZERO failures, `SUITE_EXIT=0`, `ELAPSED_SECONDS=251`.** **The green suite of record**, superseding the RED `_run5`. Row was written BEFORE the run finished, deliberately, then corrected from the log. The suite held **136**, and the count did NOT move from `_run5` because the only test-file change was a vacuity floor added to an existing test. Run because `full_suite_20260908_run5.log` went RED on a true positive (an unclassified wrapper log) and because seven files were edited after it started: `results/README.md`, `tests/test_pipeline.py` (a vacuity floor on the classification test — no new test, so the count does NOT move), `pipeline/README.md` (the KEGG measured-no-op qualification) and `09-PAPER-DRAFT.md` (the pan-cancer decline paragraph in Limitations 8). **A passing suite does not cover edits made after it started.** | `pytest tests/ -q --durations=15`, detached with the timing wrapper, 2026-09-08 21:28:43-21:32:54 CDT. **CONTENDED** — same load as `_run5`, plus a second Claude session's `pytest` in `~/Aarav IEEE/wildfire_ai` running concurrently. **Eleven timed runs now span 217 / 226 / 231 / 239 / 250 / 281 / 297 / 309 / 368 / 591 / 852 / 1,145 s** across 113-to-136-test runs; `_run5`'s 239 s and this run's 250 s are the two largest test counts and both sit near the fast end, which is the durations breakdown's point — a handful of tests dominate and the spread is contention on those. Still NOT idle. The self-test was NOT re-run in this sequence: `19_check_numbers.py` has not changed since `selftest_20260908_run2.log`, so re-running it would prove nothing. |
| `wrapper_20260908_run42b.log` | **DIAGNOSTIC — the second timing wrapper's own log, not a result.** Holds only `START`, `END` and `TOTAL_ELAPSED_SECONDS=251` for a single-stage sequence (`pytest` alone), so here the wrapper total and the suite figure agree to 2 s — unlike `wrapper_20260908_run42.log`, whose 356 s covers TWO stages and is not a suite figure. Its row exists BEFORE the run this time, which is the whole lesson of `wrapper_20260908_run42.log`. | `tools/detach.py` + `run42b.sh`, 2026-09-08 ~21:4x CDT. |
| `wrapper_20260908_run42.log` | **DIAGNOSTIC — the timing wrapper's own log, not a result.** Holds only `START`, the two stage banners, `END` and `TOTAL_ELAPSED_SECONDS=356` for the sequence `19_check_numbers.py --self-test` then `pytest tests/ -q --durations=15`. The 356 s is the SEQUENCE (117 s + 239 s), never a suite figure — reading it as one would overstate the suite by 49%. **This file is why `full_suite_20260908_run5.log` went red:** session 42 classified the two logs the wrapper produces and forgot the wrapper's own, and `test_results_readme_classifies_every_artefact_that_exists` caught it. Future sessions using a wrapper must write THREE rows, not two. | `tools/detach.py` + `run42.sh`, 2026-09-08 21:18:49-21:24:45 CDT. |
| `full_suite_20260908_run5.log` | **COMPLETE and RED — `1 failed, 135 passed in 238.72s (0:03:58)`, `SUITE_EXIT=1`, `ELAPSED_SECONDS=239`.** **KEPT DELIBERATELY as the record of a true positive.** The single failure is `test_results_readme_classifies_every_artefact_that_exists`, and it was CORRECT: the timing wrapper writes its own `wrapper_20260908_run42.log` into `results/`, and session 42 wrote rows for the two logs the wrapper PRODUCES while forgetting the wrapper's own. The guard did exactly its job — an unclassified log is how a stub becomes a result six sessions later. Superseded by `full_suite_20260908_run6.log`. Row was written BEFORE the run finished, deliberately, then corrected from the log. The suite held **136** at launch: session 42 added five tests — two for the GENERATED-ARTEFACT axis (`test_committed_generated_artefacts_match_a_fresh_render` and its can-fail companion) and three for `08_count_abstract.py`, which had never been tested at all. Run because nine documents, `19_check_numbers.py`'s `COVERAGE_FLOOR` and `tests/test_pipeline.py` were all edited this session. | `pytest tests/ -q`, detached with the timing wrapper, run in sequence with `selftest_20260908_run2.log` in ONE wrapper so the two do not contend with each other, 2026-09-08 ~21:3x CDT. **CONTENDED** — a Blender render at ~8% MEM and a foreign `pytest` belonging to a DIFFERENT repository (`~/Aarav IEEE/wildfire_ai`, confirmed by `lsof` on its cwd, so it threatened CPU but not this tree's files) were both resident, as was the ChatGPT/Codex desktop app at ~25% CPU. An idle figure at this size does not exist. **This run carries the FIRST per-test durations ever recorded for this suite** (`--durations=15`), and they are lopsided: `test_number_checker_is_not_vacuous` alone took **121.70 s of 238.72 s — 51% of the whole suite** — because it runs the entire `--self-test`; `test_every_library_module_imports_inside_the_deposit` 22.91 s; `test_the_snapshot_figure_check_can_fail` 12.78 s; `test_the_public_snapshot_can_actually_render_the_figures` 12.10 s; then 7.28 / 5.61 / 5.14 / 5.07 / 4.89 / 4.72 / 4.57 / 4.49 / 3.72 / 3.12 / 1.91 s. **The slowest 15 of that 136-test run are 220.0 s of 238.7 s, i.e. 92% of the wall clock**, so suite-level timings are dominated by a handful of tests and the 5.3x spread across ten historical runs is contention on those few, not breadth. |
| `selftest_20260908_run2.log` | **COMPLETE — PASSED, all 129 injected errors caught, `SELFTEST_EXIT=0`, `ELAPSED_SECONDS=117`.** **The self-test of record**, superseding `selftest_20260908.log`. Row was written BEFORE the run finished, deliberately, then corrected from the log. The injected-error count did NOT move, as predicted: no authority was added or removed. Re-run because `19_check_numbers.py` CHANGED this session: `COVERAGE_FLOOR["pipeline/results/README.md"]` was lowered 58 → 56 with the reason recorded in place. The injected-error row count is not expected to move, because no authority was added or removed — only a floor. | `19_check_numbers.py --self-test`, detached with the timing wrapper, first of the two commands in the sequence, 2026-09-08 21:18:49-21:20:46 CDT. **CONTENDED**, same load as `full_suite_20260908_run5.log`. **117 s.** Seven contended figures now exist for this path — 107 / 111 / 117 / 117 / 117 / 134 / 262 s — and still no idle one; the spread is 2.4x and it measures the machine, not this path. Note `grep -c CAUGHT` on this log returns one MORE than the true count: line 6 is a header containing both CAUGHT and MISSED. |
| `full_suite_20260908_run4.log` | **COMPLETE — `131 passed in 226.61s (0:03:46)`, ZERO failures, `SUITE_EXIT=0`, `ELAPSED_SECONDS=229`.** **The green suite of record**, superseding `_run3`. Row was written BEFORE the run finished, deliberately, then corrected from the log. **the suite held 131 at the time**, unchanged from `_run3`. Run because three files were edited AFTER `_run3` finished at 20:21:36: `23_export_environment.py` and the regenerated `ENVIRONMENT.md` (two drifted `file.py:NNN` pointers in the seed table, now named), and `results/README.md` itself. **A passing suite does not cover edits made after it started**, and treating `_run3` as though it did would be the same error as quoting a carried-forward timing. | `pytest tests/ -q`, detached with the timing wrapper, 2026-09-08 ~20:4x CDT. **CONTENDED.** Swap still ~96% of 12,288 MB used; two `Blender` renders resident at ~7-9% MEM each; the author's heavy `theia` job had already ended during `_run3`. **226.61 s is the SECOND-FASTEST figure ever recorded for this suite and it happened at the LARGEST test count** — a 131-test run against the 104-test run that holds the 120.55 s record. Read together with `_run3`'s 309.24 s and `_run2`'s 851.59 s, the three runs are 852 → 309 → 227 s across 125-, 131- and 131-test runs as the author's `theia` job wound down, which is as clean a dose-response between contention and wall clock as this project will ever get. **Ten timed runs now span 217 / 226 / 231 / 281 / 297 / 309 / 368 / 591 / 852 / 1,145 s across 113-to-131-test runs.** Still NOT idle — two Blender renders were resident throughout — so an idle figure at 131-test size still does not exist. |
| `full_suite_20260908_run3.log` | **COMPLETE — `131 passed in 309.24s (0:05:09)`, ZERO failures, `SUITE_EXIT=0`, `ELAPSED_SECONDS=310`.** The green suite of record, superseding `full_suite_20260908_run2.log`. Row was written BEFORE the run finished, deliberately, then corrected from the log. The first suite run at the **131-test** size: session 41 added six tests — `test_no_deposited_prose_points_the_reader_at_an_undeposited_document`, `test_the_undeposited_reference_check_can_fail`, `test_deposited_sibling_documents_match_the_readme_table`, `test_the_readme_document_table_check_can_fail`, `test_prose_documents_have_balanced_delimiters` and `test_the_delimiter_balance_check_can_fail`. It is the only run covering session 41's edits to `tests/test_pipeline.py`, `24_split_determinism.py`, `25_build_supplementary.py`, `pipeline/README.md`, `CITATION.cff`, the manuscript and the nine documents whose test count moved 125 → 131. | `pytest tests/ -q`, detached with the timing wrapper, 2026-09-08 ~20:1x CDT. **CONTENDED, and worse than any prior launch — do not read its wall clock as a cost.** At launch: available memory sampled 16 times over 14 minutes with a floor of **2.42 GiB** and a ceiling of 10.17 GiB (a 4.2x swing inside one session), and **swap at 11,863 of 12,288 MB = 96.5% used** — the total having grown 6,144 → 12,288 MB during the session. Non-project load: the author's `python -m theia.analysis.site_decomposition` under PGID 60454 (`bash scripts/reproduce.sh` in `~/Downloads/THEIA`, launched from a DIFFERENT Claude session) at 627-990% CPU, plus three-to-four concurrent `Blender` renders at ~7-9% MEM each. A near-full swap is itself a compute failure mode, so this figure measures a machine under memory pressure. **309.24 s is 2.75x FASTER than run2's 851.59 s with SIX MORE TESTS and much worse swap, and the cause was observed rather than argued**: the author's `theia.analysis.site_decomposition` job ended partway through this run — `ps` showed PGID 60454 replaced by a light `make_anon_mirror.sh` — so the heavy CPU competitor went away mid-suite. Nine timed runs now span 217 / 231 / 281 / 297 / 309 / 368 / 591 / 852 / 1,145 s across 113-to-131-test runs, a 5.3x range on one machine. An idle figure still does not exist at any size. |
| `full_suite_20260908_run2.log` | **COMPLETE — `125 passed in 851.59s (0:14:11)`, ZERO failures, `SUITE_EXIT=0`, `ELAPSED_SECONDS=853`.** The green suite of record, superseding `full_suite_20260908.log`. Row was written BEFORE the run finished, deliberately, then corrected from the log. The first suite run at **125-test** size: session 40 added `test_the_public_snapshot_can_build_the_supplementary_tables`, `test_the_snapshot_supplementary_check_can_fail` and `test_every_library_module_imports_inside_the_deposit`. It is the only run covering session 40's edits to `tests/test_pipeline.py` and the nine documents whose test count moved 122 → 125. **The suite held 125 at the time.** | `pytest tests/ -q`, detached with the timing wrapper, 2026-09-08 ~18:5x CDT. **CONTENDED — do not read its wall clock as a cost.** At launch: available memory 9.37 GiB, swap 3,679 of 5,120 MB used, and two non-project jobs resident — `python -m theia.analysis.faithfulness` under PGID 25672 at ~393% CPU and 10.9% MEM (the author's other work, launched from a different session), plus a second `python` at ~100%. Judge liveness by PGID: this session watched the theia PIDs change under a constant group, and this one watched the whole GROUP be replaced between sessions. **851.59 s is the second-worst figure on record and is NOT a cost**: eight timed runs now span 217 / 231 / 281 / 297 / 368 / 591 / 852 / 1,145 s across 113-to-125-test runs, and the FASTEST had nine more tests than one of the slowest. An idle figure at 131-test size still does not exist. |
| `full_suite_20260908.log` | **COMPLETE — `122 passed in 591.07s (0:09:51)`, ZERO failures, `SUITE_EXIT=0`.** The green suite of record, superseding run11. The post-push verification suite: the author pushed the public snapshot at 18:08 CDT on 2026-09-08 and this run covers the document updates that followed (the manuscript's Code Availability note, the submission checklist's blocking item, and the restored snapshot-file-count guard). **The suite held 122 at the time** — a historical count, not the live one, which the checker reads from a live pytest collection. | `pytest tests/ -q`, detached with the timing wrapper, 2026-09-08 ~18:2x CDT. **The machine was REBOOTED between run11 and this one**, so a comparison against run11's 281.59 s compares two different machine states, not two code states. Two `theia.engine.train` jobs under PGID 14533 were resident at ~275-305% CPU each. **591.07 s is 2.1x run11's 281.59 s with identical code and MORE free memory (floor 9.40 GiB against a pre-reboot 3.49 GiB)** -- which is the cleanest available proof that these figures measure contention and not the suite. Seven timed runs now span 217 / 231 / 281 / 297 / 368 / 591 / 1,145 s. An idle figure at 131-test size still does not exist. |
| `full_suite_20260907_run11.log` | **COMPLETE — `122 passed in 281.59s (0:04:41)`, ZERO failures, `SUITE_EXIT=0`.** The green suite of record for session 39, superseding run10, and the first at **122-test** size: session 39 added `test_the_coverage_floor_can_fail_and_gates_on_counts_not_percentages` on top of run10's four. | `pytest tests/ -q`, detached with the timing wrapper, 2026-09-07 ~22:4x CDT, run immediately after `selftest_20260907_run7.log` in the same wrapper so the two do not contend with each other. The two `theia.engine.train` jobs (PGID 64458) were resident throughout, so 281.59 s is CONTENDED and is not this suite's cost. Six timed runs now span 217 / 231 / 281 / 297 / 368 / 1,145 s — and run11 is FASTER than run10 with one more test, on the same machine within twenty minutes, which is the clearest statement yet that these figures measure the machine. |
| `selftest_20260907_run7.log` | **COMPLETE — PASSED, all 127 injected errors caught, `SELFTEST_EXIT=0`, `ELAPSED_SECONDS=117`.** The self-test of record for session 39, re-run because `19_check_numbers.py` changed: `--coverage` gained a floor gate and the missing-artefact refusal learned to name curation rather than tampering. Supersedes `selftest_20260907_run6.log`, which is kept — six timed runs of this path are the only evidence of its spread. | `19_check_numbers.py --self-test`, detached with the timing wrapper, 2026-09-07 ~22:4x CDT. CONTENDED. |
| `full_suite_20260907_run10.log` | **COMPLETE — `121 passed in 368.85s (0:06:08)`, ZERO failures, `SUITE_EXIT=0`.** The green suite of record for session 39, and the first at **121-test** size: session 39 added `test_compare_live_detects_a_deposit_that_is_missing_a_staged_file`, `test_compare_live_gates_on_the_file_set_and_not_on_content`, `test_the_number_checker_names_curation_rather_than_tampering_in_a_deposit` and `test_the_deposit_wording_fires_when_the_builder_is_absent`. | `pytest tests/ -q`, detached with the timing wrapper, 2026-09-07 ~22:3x CDT. Two `theia.engine.train` jobs (PGID 64458) were resident throughout at ~108% and ~158% CPU, so 368.85 s is CONTENDED and is **not** this suite's cost. Five timed runs now span 217 / 231 / 297 / 368 / 1,145 s across 113-to-121-test runs — a 5.3x range on one machine, and the FASTEST had more tests than the slowest. An idle figure at 131-test size still does not exist. |
| `full_suite_20260907_run9.log` | **COMPLETE — `117 passed in 217.35s (0:03:37)`, ZERO failures, `SUITE_EXIT=0`.** The green suite of record for session 38. | `pytest tests/ -q`, detached with the timing wrapper, 2026-09-07 ~21:1x CDT. Re-run of `_run8` after fixing the two British spellings that failed it, plus this session's Methods addition (the two split schemes and the covariate baselines). Same contention as `_run8`: the two non-project `theia.engine.train` jobs resident throughout. |
| `full_suite_20260907_run8.log` | **COMPLETE — RED. `1 failed, 116 passed in 231.00s (0:03:51)`, `SUITE_EXIT=1`.** The failure was MINE and the test was right: `test_submitted_documents_use_american_spelling` caught `artefact`/`artefacts` at 09-PAPER-DRAFT.md:940 and :956, introduced by this session's own new Code Availability blockquote. Fixed to `artifact`; superseded by `_run9`. Kept as the record of a guard firing on the session that wrote it. | `pytest tests/ -q`, detached with the timing wrapper, 2026-09-07 ~21:0x CDT. The first suite run at **117-test** size: session 38 added `test_the_public_snapshot_can_verify_its_own_provenance`, `test_the_snapshot_provenance_check_can_fail`, `test_an_undeposited_artefact_is_still_required_on_the_working_tree` and `test_pattern_reach_is_recorded_and_can_detect_a_lost_document`. It is the only run covering session 38's edits to `21_provenance_manifest.py` (the `deposited` flag), `19_check_numbers.py` (the pattern-reach report) and `tests/test_pipeline.py`. Contention at launch: the two non-project `theia.engine.train` jobs (PGID 64458) resident at ~300% CPU, available memory **5.38 GiB**, swap total **21,504 MB**. Any wall clock from this run is a contention measurement, not a cost. |
| `full_suite_20260907_run7.log` | **COMPLETE — `113 passed in 297.03s (0:04:57)`, ZERO failures, `TIMED-SUITE EXIT=0 ELAPSED_SECONDS=299`.** Row was written BEFORE the run finished, deliberately, then corrected from the log. | `pytest tests/ -q`, PID 50251, detached and verified (PPID 1, PGID 50251, STAT `Ss`, child at 692.7% CPU), 2026-09-07 19:50:19–19:55:18. The first suite run at the **113-test** size: session 37 added `test_the_public_snapshot_can_actually_render_the_figures` and `test_the_snapshot_figure_check_can_fail`. It is the only run covering this session's edits to `10_make_figures.py` (the panel-size deposit, its four new FATAL branches and the figure-2 join assertion), `19_check_numbers.py` (the live snapshot authority), `21_provenance_manifest.py` (`gene_set_sizes.csv`) and `tests/test_pipeline.py`. **297 s is a THIRD contended figure for this suite and it breaks the pattern the first two established.** The 111-test suite ran 748.63 s (18:20) and 1,145.32 s (18:47); this ran **297.03 s with TWO MORE TESTS**, and the two non-project `theia.engine.train` jobs were resident throughout at 259% and 236% CPU — verified by `ps` immediately afterwards, not assumed. The three figures span **3.9×** on one machine for one near-identical suite. **The cause of the speed-up is NOT established.** One concrete difference is recorded so it can be tested rather than argued: this run exported `MPLCONFIGDIR="$TMPDIR/mpl"` and the two earlier ones did not, and several tests spawn subprocesses that import matplotlib — a per-subprocess font-cache rebuild would be invisible in the test names and expensive. **That is a hypothesis with no measurement behind it; do not repeat it as a finding.** What the three figures DO establish, without any hypothesis, is the standing rule: a contended wall clock measures the machine, not the code, and no single one of these is a cost. An idle figure at 131-test size still does not exist. |
| `full_suite_20260907_run6.log` | **COMPLETE — `111 passed in 1145.32s (0:19:05)`, ZERO failures.** | `pytest tests/ -q`, PID 9239, detached and verified (PPID 1, PGID 9239, STAT `Rs`), 2026-09-07 18:47–19:06. Row written BEFORE the run finished, then corrected. Run because `reproduce_check_20260907_run4.log`'s green suite validated the tree **as it stood at 18:20–18:38**, and three source files were edited AFTER it finished: `10_make_figures.py` (the Figure 1B fallback and its two closed silent holes), `22_build_public_snapshot.py` (`RESULT_KEEP_NAMES`) and the poster. **A passing check does not cover edits made after it started**, and treating it as though it did would be the same error as quoting a carried-forward timing. **1145.32 s is the WORST contended figure on record and is NOT a cost.** Against the ~120 s idle baseline it is **9.5×**, and against this session's own earlier contended suite — 748.63 s inside `reproduce_check_..._run4`, the same 111-test suite, ninety minutes earlier — it is **1.53× slower again**, because the two non-project `theia.engine.train` jobs (PGID 64458) were deeper into their run. Two contended measurements of the same suite at the same test count differing by half is the sharpest available demonstration that **a contended wall clock measures the machine, not the code.** An idle figure has still never been obtained at this suite size, nor at the 113-test size the suite reached later the same day. **The two counts on this row are a HISTORICAL record of what these runs measured, and are deliberately written in a hyphenated form the checker does not read as a current claim** — the suite grew to 113 after both runs finished, and retro-fitting that number here would falsify the record rather than update it. |
| `reproduce_check_20260907_run4.log` | **COMPLETE — `TIMED-CHECK EXIT=0 ELAPSED_SECONDS=1069`. `reproduce.sh --check` PASSES on the current tree, and the suite inside it was `111 passed in 748.63s`, ZERO failures.** | `bash reproduce.sh --check` under the `TIMED-CHECK` wrapper, PID 85660, detached and verified by PROCESS GROUP (PPID 1, PGID 85660, parent 0.0% CPU in `Ss` with a child at 590%), 2026-09-07 18:20:19–18:38:08. Run because `--check` had not been executed since `_run3` while the tree changed materially: `19_check_numbers.py` gained four DOCS entries and a reachable EXEMPT table, one test was added, and eleven documents moved. **DO NOT QUOTE 1069 s AS THE COST OF `--check`. It is a CONTENTION measurement.** `_run3`'s **247 s** remains the figure, measured with nothing else running. Two non-project `python -m theia.engine.train` jobs (PIDs 64468/64469, PGID 64458) held ~143% and ~227% CPU with ~7% MEM each throughout, plus a `bun` at ~32%. **The contention factor is the useful number here:** 1069 / 247 = **4.3×** for `--check` end to end, and the embedded suite ran **748.63 s against the ~120 s idle baseline = 6.2×**, at a test count only 7 higher. Consistent with this project's repeated finding that the historical wall-clock spread is contention almost end to end, and a third independent confirmation of it. |
| `selftest_20260907_run6.log` | **COMPLETE — PASSED, all 127 injected errors caught. Current self-test of record; `_run5` is superseded.** | `19_check_numbers.py --self-test`, PID 308, detached with the timing wrapper, 2026-09-07, `SELFTEST_EXIT=0 ELAPSED_SECONDS=117`. Row count unchanged at 127: session 38 added a pattern-REACH report, and a report is not a claim authority. **A FOURTH contended timing: 134 / 107 / 111 / 117 s.** An idle figure for this path still does not exist. |
| `selftest_20260907_run5.log` | **COMPLETE — PASSED, all 127 injected errors caught. This is the current self-test of record; `_run4` is superseded.** | `19_check_numbers.py --self-test`, PID 60230, detached and verified (PPID 1, PGID 60230, STAT `Ss`, child running), 2026-09-07, `SELFTEST_EXIT=0 ELAPSED_SECONDS=111`, 20:47:21 → 20:49:12 CDT. Row count unchanged at 127. **A THIRD contended timing for this path: 134 s (`_run3`), 107 s (`_run4`), 111 s (`_run5`).** The three span 25% with nothing changed between them, which is the useful part; none of them is a cost. Contention at launch was the two non-project `theia.engine.train` jobs (PGID 64458) at 326% and 301% CPU, 2 h 36 m into their run, with available memory at **5.38 GiB** and swap total grown to **21,504 MB**. An idle figure for the self-test still does not exist. |
| `selftest_20260907_run4.log` | **COMPLETE — PASSED, all 127 injected errors caught. This is the current self-test of record; `_run3` is superseded.** | `19_check_numbers.py --self-test`, PID 48807, detached and verified (PPID 1, PGID 48807, STAT `Ss`, child at 98.2% CPU), 2026-09-07, `TIMED-SELFTEST EXIT=0 ELAPSED_SECONDS=107`. **126 → 127**: the new row is `public snapshot file count (live rebuild)`, and it reports **2 substitutions** — it fires in `09-PAPER-DRAFT.md` *and* `submission/SUBMISSION-CHECKLIST.md`, which is the point, since the same pair had gone stale in both. **Two timed runs of this path now exist and they differ by 25%: 134 s (`_run3`, 19:30) and 107 s (`_run4`, 19:50), same machine, same contention sources.** Neither is a cost; both are contention measurements, and the spread between them is the useful part. An idle figure for the self-test still does not exist. |
| `selftest_20260907_run3.log` | **COMPLETE — PASSED, all 126 injected errors caught. Superseded by `_run4`, which adds the snapshot-file-count row.** | `19_check_numbers.py --self-test`, PID 29126, detached and verified (PPID 1, PGID 29126, STAT `Ss`, child at 184.6% CPU), 2026-09-07. **The first TIMED run of this path: `TIMED-SELFTEST EXIT=0 ELAPSED_SECONDS=134`, 19:30:34 → 19:32:48 CDT**, wrapped so the launch is timestamped in the log itself rather than reconstructed afterwards. This retires the **`~103 s`** figure, which had been carried since #34 and **was never a measurement** — `_run2` could only be bounded as "under two minutes" because nobody recorded when it started. **134 s is CONTENDED**, not a cost: both non-project `theia.engine.train` jobs (PGID 64458) were resident and 1 h 18 m into their run at launch, and available memory was 4.16 GiB. An idle figure for this path still does not exist. Row count unchanged at 126, as expected — session 36 added pytest tests, and a pytest test is not a claim authority. |
| `selftest_20260907_run2.log` | **COMPLETE — PASSED, all 126 injected errors caught. Superseded by `_run3`, which is the same result with a wall clock.** | `19_check_numbers.py --self-test`, PID 66315, detached and verified (PPID 1, PGID 66315, STAT `Rs`, 96.6% CPU), 2026-09-07, finished 18:12:00. Row written BEFORE the run finished, deliberately, then corrected. **The row count did NOT change: 126, exactly as in `selftest_20260907.log`.** Handoff #35 called this run "overdue at its new size" on the grounds that session 35 added four tests — but those were *pytest* tests, and a pytest test is not a claim authority, so the self-test's size was never touched. Worth recording as the distinction it is: the suite grew, the checker's coverage did not. **Wall clock is NOT reported here** — the launch was not timestamped, so the only honest bound is under two minutes; the `~103 s` figure is still carried-forward, not measured. **A grep trap, recorded because it is easy to repeat:** `grep -c CAUGHT` on this log returns **127** and `grep -c MISSED` returns **1**, which reads like a failure and is not one. Line 6 is the header sentence *explaining* the format and contains both words. 126 genuine `CAUGHT` rows, zero genuine `MISSED`. Read what a grep matched before drawing a conclusion from its count. |
| `full_suite_20260907_run3.log` | **COMPLETE — 104 passed in 120.55 s (2 m 0.6 s), ZERO failures. The fastest full-suite run on record by 3.4×, and the closest to a CPU-idle wall clock this project has ever measured.** | `pytest tests/ -q`, PID 54637, detached and verified (PPID 1, PGID 54637, STAT `Rs`), 2026-09-07 15:50, after the Discussion/Results partition-variance rewrite and the A5 site-count correction. Row written BEFORE the run finished, deliberately: `test_results_readme_classifies_every_artefact_that_exists` fails on any log here without a row, the suite's own included. **What this measures.** Every prior figure at that same test count — 412.84 s and 406.27 s in #33 — was taken with four non-project `theia` jobs at ~913% CPU. Those are gone. Nothing else was computing: the top CPU consumers during the run were `WindowServer` (47%), a non-project `bun` (37%) and WebKit (34%), i.e. a desktop session, not a workload. So the 291.95 / 588.16 / 412.84 / 406.27 s spread was **contention almost end to end**, and the suite's own cost at that size is ~120 s. Called "CPU-idle", NOT "idle": the machine was still under memory pressure (12.5 GiB in the compressor, 3.88 GiB available), which this suite is small enough not to feel. |
| `reproduce_check_20260907_run3.log` | **COMPLETE — `TIMED-CHECK EXIT=0 ELAPSED_SECONDS=247`. `reproduce.sh --check` PASSES, and 247 s (4 m 7 s) is THE figure to quote for it.** | Same wrapper and command, PID 61235, detached, 2026-09-07 15:58:14–16:02:21, nothing else running. Confirms the `PYTHONHASHSEED` fix end to end: the entry point now exits 0 on the platform it validates. **Read this row's number, not run 2's.** `--check` runs the full suite AND `19_check_numbers.py --self-test` AND `26_sort_audit.py --self-test` AND `--coverage`; run 2 died at the suite step and never reached the self-tests, so its 137 s is a **time-to-failure**, not a cost. The gap between them — 247 s against 137 s — is almost exactly the self-test stage. The pre-session-28 **72 s** figure is superseded and was never re-measured after `--check` gained five steps; do not quote it. |
| `reproduce_check_20260907_run2.log` | **TIME-TO-FAILURE, not a timing — and the run that EXPOSED A REAL DEFECT.** Its 137 s is **not** the cost of `reproduce.sh --check` (that is 247 s, see `_run3`): the script aborted at the failing suite step and never ran the self-test stages. Quote it only as "how long until it failed"; its exit 1 was a genuine failure, now fixed. | Same wrapper and command as run 1, PID 59130, detached, 2026-09-07 15:54:01–15:56:18, launched with **nothing else running**. **What it found.** One test failed — `test_environment_md_matches_a_fresh_render_on_this_platform` — while passing standalone in 7.49 s. Cause: `reproduce.sh` line 50 exports `PYTHONHASHSEED=0` for determinism, so a fresh `render()` from inside it reports `0` where the committed `ENVIRONMENT.md`, generated interactively, records `(unset)`. Both rows are correct; the variable describes how the interpreter was launched, not the machine. The consequence was that **`reproduce.sh --check` could never exit 0** — the repository's one-command reproduction entry point, cited in the paper's §Reproducibility and in `pipeline/README.md`, failed by construction, and had done since `--check` gained the suite step in session 28, undetected because nobody ran it end to end again until now. Fixed by excluding that one row from `_strip_volatile`, the same treatment the git-commit row already gets; the variable is still written to the file. Verified in `_run3`. |
| `reproduce_check_20260907.log` | **CONTAMINATED — 145 s, exit 1, and BOTH the timing and the exit code are unusable. Superseded by `_run2`. Kept as the record of how it was contaminated.** | `bash reproduce.sh --check` under a wrapper printing `TIMED-CHECK EXIT=<rc> ELAPSED_SECONDS=<n>`, PID 56440, detached, 2026-09-07 15:48:44–15:51:09. The wrapper exists because `time` output and `tee \| tail` both report the pipeline's status rather than the script's — a trap hit in at least six sessions. **Why it is unusable, recorded because the mistake is easy to repeat:** it was launched while `full_suite_20260907_run3.log` was *still running* — two concurrent pytest processes — and while the working tree was mid-edit, two tests having just been added without the document counts yet updated. So the 145 s overlaps a concurrent 120 s suite, and the two failures (`test_environment_md_matches_a_fresh_render_on_this_platform`, `test_no_claim_pattern_is_vacuous_on_the_real_tree`) are artefacts of that, not defects: both were re-run alone immediately afterwards and **passed in 7.49 s**. `reproduce.sh --check` runs the full suite as one of its steps, so it must never be launched alongside another pytest run. The superseded pre-session-28 figure is **72 s**; do not quote that either. [2026-09-23, session 58: what follows was a FOURTH cell under this three-column header, so GitHub never rendered it. It is the method note written before the run, kept as written; its "~15:56" disagrees with the log, whose START and END lines read 15:48:44 and 15:51:09.] `bash reproduce.sh --check` under a wrapper that prints `TIMED-CHECK EXIT=<rc> ELAPSED_SECONDS=<n>` as the final line, PID 56440, detached, 2026-09-07 ~15:56. The wrapper exists because `time` output and `tee \| tail` both report the wrong thing here — the pipeline's status rather than the script's — a trap this project has hit in at least six sessions. The superseded figure is the pre-session-28 **72 s**; do not quote it. Read the last line of this log for both the exit code and the wall clock. |
| `full_suite_20260907_run4.log` | **COMPLETE — 106 passed in 119.92 s (1 m 59.9 s), ZERO failures.** The state this session is committed in. Row written before completion, per the rule above. | `pytest tests/ -q`, detached, 2026-09-07 ~16:10. 104 -> 106: `test_seeded_defaults_agree_with_the_audit_config_seed` and `test_seed_default_check_can_fail`, which make the three unwired RNG default seeds LOUD rather than merely documented. Also the first suite run after the `PYTHONHASHSEED` exclusion in `_strip_volatile` — see the `reproduce_check_20260907_run2.log` row for why that was a real defect and not a nuisance. Machine: no theia jobs, a non-project `bun` that released ~12 GiB during the session, ~3.9 GiB available. |
| `full_suite_20260907_run5.log` | **COMPLETE — 110 passed in 245.16 s (4 m 5 s), ZERO failures.** The state this session is committed in. Row was written BEFORE the run, per the rule above. **The wall clock is 2.0x the ~120 s idle baseline and that gap is contention, not regression** — see the machine note at the end of this cell. Quoting 245 s as the suite's cost would repeat the error that made the historical 291-588 s spread look like a property of the suite. | `pytest tests/ -q`, detached, 2026-09-07 ~16:40. 106 -> 110: `test_submitted_documents_use_american_spelling` and `test_american_spelling_check_can_fail`, which make the 2026-09-06 US-spelling conversion enforceable rather than a one-off find-and-replace; and `test_science_gaps_reads_a_real_fingerprint_from_a_real_directory` and `test_science_gaps_fingerprint_reader_can_fail`, which drive the cohort-fingerprint reader over a fixture results directory so its match and mismatch branches execute for the first time outside a unit test. **Machine: heavily contended** — two non-project `theia` jobs at ~265% CPU each, a non-project `pytest` from another repository at ~108%, and the usual `bun`. Available memory sampled every 60 s for six minutes: floor 3.47 GiB, ceiling 4.30 GiB. This wall clock is NOT an idle measurement and must not be quoted as one. |
| `science_gaps_stablesort_run4.log` | **COMPLETE — the regeneration that removed a publication leak** | `11_close_science_gaps.py --results results/nsclc_v3_stablesort`, PID 33596, detached, 2026-09-07. Run because `22_build_public_snapshot.py` **refused to stage the snapshot**: `science_gaps.json` recorded `cohort_path` as an absolute path under the author's home directory, added when the cohort-scope fields landed 2026-09-06 and invisible until the first rebuild after that. Fixed at SOURCE (`repo_relative()`), then the artefact was **regenerated rather than hand-edited**, so it is genuinely what the fixed code produces. **Verified reproduction:** of 21 keys, exactly two changed — `cohort_path` (now `data/interim/meta.parquet`) and the new `cohort_fingerprint` block (`status: absent`, `observed_n: 944`, the first fingerprint ever written into a real artefact). `immune_excess.csv`, `outcome_arm.csv`, `summary.json` and `config.json` came back **byte-identical**, which is an independent determinism check on this script. `stage_failures: []`. The directory was backed up before the run; peak ~23% MEM (~5.6 GiB). PROVENANCE re-written: one hash, one byte count, one file. |
| `full_suite_20260907_run2.log` | **DIAGNOSTIC — the re-run after the classification row landed** | `pytest tests/ -q --durations=3`, PID 92734, detached, 2026-09-07 ~02:05. Launched specifically to clear the one self-referential failure in `full_suite_20260907.log`: that run's only red test was the results/README.md classifier, firing because the log had no row yet. **This row was written BEFORE the run finished, on purpose** — otherwise the same test fails again on this log and the suite can never be observed fully green. **RESULT: 104 passed in 406.27 s (6 m 46 s), ZERO failures** — the suite fully green at the new count, and the row above was updated only after reading that line. Still contended: the four unrelated `theia` jobs were resident throughout, so this is not an idle timing either. Slowest: `test_number_checker_is_not_vacuous` 106.17 s (it runs the checker's full `--self-test`), `test_fix_pooled_isi_ci_is_patient_clustered_not_signature_resampled` 40.32 s. |
| `scorer_ssgsea_stable.log` | **COMPLETE.** The run finished 2026-09-06 02:43; PID 6503 is gone. | The log for `scorer_sensitivity_ssgsea_stable/` above, PID 6503. Already carries two MEASURED numbers worth keeping whatever the run does: ssGSEA scoring costs **0.0347 ms per patient-set**, against the **6.26 ms** its own docstring quotes for the pre-optimisation loop — a 180x speedup — and the script's own extrapolation for the whole run is **523 s per arm**. The docstring's "~8 minutes per scorer per arm" is nonetheless WRONG on the wall clock: the two arms actually cost **6,288.9 s** (`ssgsea__disattenuated`) and **3,630.8 s** (`ssgsea__uncorrected`), 2 h 45 m in total on a contended machine — roughly 30x the docstring's figure, because the ISI machinery dominates the scoring the estimate was extrapolated from. Do not quote the docstring. |
| `full_suite_20260906.log` | **DIAGNOSTIC — a CONTENTION measurement, not a clean suite timing** | `pytest tests/ -q --durations=5`, PID 96473, detached, 2026-09-06 00:31-01:03. **96 passed, 2 failed in 1906.42 s (31 m 46 s).** Compare the 48.7 s the suite took idle when it held 94 of them: this ran with the ssGSEA job resident and four other sessions' `.venv` pythons on the machine, at load average 38-71 on 14 cores, so the wall clock measures the CONTENTION and not the suite. The slowest single test was `test_run_audit_primary_is_none_without_axis` at 185.26 s. **Both failures were real and both are fixed:** `test_results_readme_classifies_every_artefact_that_exists` failed because THIS LOG did not yet have the row you are reading, and `test_environment_md_matches_a_fresh_render_on_this_platform` failed on a PRE-EXISTING mismatch — session 29 corrected `23_export_environment.py`'s seed-audit date to 2026-09-05 but never re-ran `--write`, so the committed `ENVIRONMENT.md` still said 2026-09-06. **Session 29 did not run the suite to completion, which is exactly why nobody saw it.** |
| `pancancer_v3_stablesort.log` | **DEAD — killed, carries NO result. Do not read the null-scoring lines as one.** | A pan-cancer stable-sort run (the A9/A10 counterpart to `nsclc_v3_stablesort/`), launched 2026-09-06 by a CONCURRENT session. **Verified dead 2026-09-06 19:53** the way this file insists on — by the ABSENCE of `results/pancancer_v3_stablesort/` and by the log being byte-identical (3,261 b) across a 20 s re-read, last written 14:30, with no pipeline python alive. It reached `null ... 1000 sets scored` for all 16 signatures and then stopped without a traceback, which is the SIGKILL signature already recorded for `partition_variance_pancancer.log`: pan-cancer working sets meet a machine with no memory headroom. **This run was launched without the measurement the handoff asks for** — time one pan-cancer partition first (measured range 3,364-11,404 s), and ask before anything over 2 h. If relaunched, use a FRESH log name; `detach.py` opens with `O_TRUNC` and would destroy this record. **DECIDED 2026-09-08 (session 42): this run is DECLINED, not pending.** At that point it had been blocked on available memory for eight consecutive sessions; the floor was sampled twenty times on 2026-09-08 at 3.59 GiB (ceiling 6.19) against an 8 GiB requirement, with swap growing 7,168 -> 8,192 MB inside the window. **Re-measured twice since, and the decline holds.** Twenty samples on 2026-09-08/09 (session 43) gave a floor of 7.69 GiB and a ceiling of 9.31 — the closest this machine has come, with 18 of the 20 samples above 8 GiB but not the floor. Twenty more on 2026-09-09 (session 44) gave a floor of 6.18 GiB and a ceiling of 8.65, with only 6 of the 20 samples above 8 GiB — so the floor FELL by 1.51 GiB between consecutive sessions and the trend is not monotone recovery. Each is a dated observation of a machine, not a running tally: the requirement is a floor sustained across the hours the run needs, and no window has yet delivered one. The manuscript now carries the decline explicitly in Limitations item 8 rather than leaving it implied — the correction is reported as measured in NSCLC and unmeasured in pan-TCGA. A future session MAY still run it if the machine goes quiet, and it would then be an improvement rather than a dependency; what is no longer true is that the paper needs it. |
| `full_suite_20260906_run2.log` | **DIAGNOSTIC — the verification re-run, ALL GREEN** | `pytest tests/ -q`, PID 31599, detached, 2026-09-06 01:15-01:35, launched after both failures above were fixed and after `19_check_numbers.py` gained `--strict`, `WRAP:` guards and exponent-scaled tolerances. **99 passed in 1216.85 s (20 m 17 s), zero failures.** A fresh log name because `detach.py` opens with `O_TRUNC` and reusing the first would have destroyed the record of the 96/2 run. Still CONTENDED — the ssGSEA job was resident throughout — so 1,216.85 s is 25 times the 48.7 s idle baseline and measures the machine, not the suite. It is a correctness result, not a timing one. |
| `partition_variance_pancancer_20260905_run3.log` | **COMPLETE, finished 2026-09-05 20:49** | `12_partition_variance.py --cohort pancancer --partitions 6`, PID **71818**, detached and verified (PPID 1, PGID 71818). **It finished.** Verified 2026-09-05 the way this file insists on: by the presence of `partition_variance_pancancer/summary.json` and the `PARTITION VARIANCE — pancancer, 5 usable partitions` banner in the log, not by a tail. Five partitions usable, `split_seed=5` refused by the degeneracy guard (`SVD did not converge`). Results are recorded against `partition_variance_pancancer/` above. **Per-partition wall clocks, all six now measured:** 6,266 s, 3,541 s, 11,404 s, 3,374 s, 3,364 s — a 3.4× spread between the fastest and slowest partition, total 7.8 h. This **retires the "2–4 h for six partitions" estimate three handoffs carried**, and also retires the 7–10 h refinement made mid-run from the first two partitions: the true figure is 7.8 h, and the mid-run estimate was only right by luck, because partition 2 alone took 3 h 10 m. |
| `partition_variance_pancancer_20260905.log` | **DEAD — killed, 0 partitions.** Verified 2026-09-05: the file is **65 bytes, one line** (the header alone), PID 46508 is gone from `ps`, and no `summary.json` was written. This entry previously read "IN FLIGHT at time of writing" and was stale for a day; the run it describes never produced a number. **Carries no result. Do not read the header as one.** | `12_partition_variance.py --cohort pancancer --partitions 3`, PID 46508, detached (PPID 1, PGID 46508, STAT `Rs`). Deliberately **3** partitions, not 6: three is the minimum that yields a `summary.json` at all, so it converts an unmeasured six-partition gamble into a measured three-partition result. A **fresh log name**, because `detach.py` opens with `O_TRUNC` and reusing `partition_variance_pancancer.log` would have destroyed the record of the failed run. Writes to `partition_variance_pancancer/`, which was empty. Setup measured at 112 s (95 s of it `compute_global_axis`). **Check it finished by looking for `PARTITION VARIANCE — pancancer` in the log and a `summary.json` in the output directory — not by the log's tail, which is silent between partitions.** |
| `reproduce_check_20260905.log` | **DIAGNOSTIC — the repo gate, run under load** | `bash reproduce.sh --check`, PID 72604, detached, 2026-09-05. Run *while* PID 46508 held the pan-cancer working set, so its wall clock is a contention figure and must not be compared with the 72 s and 43 s idle runs of 2026-09-04. |

`partition_variance_pancancer.log` was mentioned in handoffs before #22 as
"exists but empty", and as of 2026-09-02 it did not exist at all. **It exists,
it is DEAD, and it is not in flight** — that phrasing was carried forward from
#23 and was wrong by the time #24 was written. The live run is
`partition_variance_pancancer_20260905.log`, a separate file with a separate
PID; `detach.py` opens its log with `O_TRUNC`, so reusing the old name would
have destroyed the only record of the failure.
`scorer_local.log`'s salvageable line and the dead stubs are unchanged.

---

## Rules

1. **Never overwrite a FROZEN directory.** The abstract and the paper quote it.
2. **Pick a fresh log name** rather than overwriting a previous run's record,
   including for failed runs — a failure is evidence about a bug.
3. **`tee | tail` reports the pipeline's exit status, not Python's.** A killed
   run once reported "exit code 0" because `tail` succeeded. Confirm a log ends
   with its expected final line before believing a run finished.
4. **Arrow prints four `sysctlbyname ... Operation not permitted` lines** at
   every `read_parquet` inside the agent sandbox. Harmless. Filter with
   `grep -v sysctlbyname`.
5. **`scorer_sensitivity_hpc4/` is still not quotable**, and the macOS arms have
   now landed, so the reason is sharper than it was: its own control arm failed
   on that machine while the identical script passed on macOS. That makes it
   evidence **about A9**, not about scorers. Quote `scorer_sensitivity_local/`.
6. **A `.npz` that is half of a two-machine experiment is not a result.**
   `bisect_macos.npz` is complete, correct, and answers nothing on its own.
   Do not write a conclusion from one half.
7. **DECIDED 2026-09-07 — the two `bisect_*.npz` files STAY, and the question is
   closed.** It had been carried unresolved through handoffs #29, #30, #31 and
   #32 as "2.18 MB each and git-tracked, decide whether they should stay now
   that they no longer carry the explanation". They do still carry something,
   and it is not sentimental: `19_check_numbers.py` **opens both files** (at the
   `bisect_macos.npz` / `bisect_hpc4.npz` reads) and turns them into three
   checked authorities — `bisect_macos_pooled`, `bisect_hpc4_pooled`, and
   `bisect_platform_delta`, which is DERIVED from the pair rather than typed, so
   it cannot drift from the two numbers it is the difference of. Deleting them
   would not merely free 4.37 MB; it would move three full-precision quotes in
   `14-SCIENCE-AUDIT.md` and in this file from **verified** to **exempted**,
   which is the exact downgrade the full-precision rule exists to prevent. The
   size argument was also never strong: 4.37 MB against a `predictions.csv.gz`
   of 14.9 MB already tracked in this directory. Re-open this only if those
   three authorities are removed from the checker first — and note that the
   files' **byte-for-byte identical size** (2,182,700 b each) is itself the
   cheapest structural check that the two halves match, so it is worth keeping
   as well.
